# บันทึกความคืบหน้า — ระบบ HR 7-Eleven (Supabase)

อัปเดตล่าสุด: 18 ส.ค. 2026

## สถานะปัจจุบัน: ใช้งานได้ปกติ ✅ (ตรวจ logic ที่แก้ทุกจุดผ่านในแซนด์บ็อกซ์)

### 🎨 นิดา: แชทเต็มจอ ธีม Orb (พาสเทลสว่าง) + หน้าประวัติ (19 ส.ค.)
- **ทิศทางใหม่ (ผู้ใช้เลือก):** ธีมสีจากออร์บ (ม่วง-ฟ้า-พีช พาสเทลสว่าง) ใช้ทั้งแอป HR (พนักงานคงเดิม) · ผ่าน preview `_preview-nida-fullchat.html`
- **แชทเต็มจอ** (`#asstPop` inset:0 ธีมออร์บ · คงทุก ID เดิม): หัวมี**ออร์บจริง** (nida-orb.webp) + ปุ่มประวัติ/ล้าง/ปิด
- **หน้าโฮม** (`#asst_home`): ออร์บใหญ่ + ทักทาย + การ์ดสั่งงานด่วน 2×2 (กดแล้วเติมคำถาม) — โชว์เมื่อยังไม่มีข้อความ
- **หน้าประวัติ** (`#asst_hist`, localStorage): แท็บ แชท/เอกสารที่ขอ + ค้นหา + **ลบทีละอัน** · แชทกดโหลดกลับมาต่อได้ · เอกสารที่ขอ (get_document) ถูก log อัตโนมัติ
- **คงฟีเจอร์เดิมครบ:** ส่ง/การ์ดเอกสาร/คัดลอก/แนบไฟล์(PDF)/เสียง · เสียงเริ่มจากปุ่มคลื่นในแชท · **ออร์บแตะ = เปิดแชทเต็มจอ** (เดิมเด้งเข้าเสียงเลย)
- **ตรวจ:** hr JS 0 error · employee/payroll-review/supabase.js ผ่าน
- **ไฟล์ deploy:** `hr/index.html` (ธีม+ประวัติ เป็น client ล้วน)
- **ยังเหลือ:** ไล่ธีมพาสเทลออร์บ "ทั้งแอป HR" (รีแมปตัวแปรสีกลาง ทีละหน้า) + ให้นิดาสร้าง/แก้ Word/Excel จริง

### ➕ นิดา: ของเสริม 4 อย่าง + แก้บั๊กเช็คอิน + วันทำงานตรง (19 ส.ค.)
- **บั๊กหน้าเช็คอิน:** `employee/index.html` วาดหมุด SVG เป็นข้อความบน canvas (`ctx.fillText('<svg...')`) → เปลี่ยนเป็นวาดหมุดด้วย canvas primitives (สีส้ม)
- **วันทำงานหน้าตรวจ = รายงาน:** `supabase.js reviewShift` เพิ่ม is_manager + ให้ att_days เครดิตควบตามตาราง (non-manager) เหมือน hrPayrollRun (จำลองผ่าน: non-mgr +1, mgr ไม่บวก)
- **#1 PDF จัดหน้ากระดาษ:** `asstPdfFromCanvas` — เรนเดอร์กว้าง A4 (794px) + ตัดหน้าที่ "เส้นพื้นว่าง" ไม่ตัดกลางบรรทัด → พิมพ์ได้จริง (ใช้ใน report + ack_form)
- **#2 ปุ่มคัดลอก:** ใต้ข้อความผู้ช่วยในแชท (navigator.clipboard + fallback execCommand)
- **#3 ปุ่มแนบไฟล์:** `#asst_file` รับ image + **PDF** (ส่งเป็น inline_data → Gemini อ่าน PDF ได้ตรง ๆ) · thumbnail แสดง chip PDF · ≤6MB
- **#4 นำทางเปิดเมนู:** edge `open_menu(menu)` map คำ→แท็บ (29 เมนู) → การ์ดปุ่ม "เปิดเมนู" → client showTab · SYS สอนให้ใช้เมื่อผู้ใช้หาเมนูไม่เจอ
- **ตรวจ:** hr JS 0 error · edge parse OK · employee 0 error · supabase.js OK
- **ไฟล์ deploy:** `supabase/functions/hr-assistant` (edge) + `hr/index.html` + `employee/index.html` + `shared/supabase.js`
- **ยังเหลือ (เฟสถัดไป):** แชทเต็มจอโฉมใหม่ + ประวัติแชท/เอกสาร (ลบได้) + ให้นิดาสร้าง/แก้ไฟล์ Word/Excel จริง (ก้อนใหญ่)

### 🔍 ตรวจสอบทั้งระบบ + เตรียมแก้ (19 ส.ค. กลางคืน) — ดู `BUG-AUDIT.md`
- Validate ทุกหน้า+shared+edge ผ่านหมด · review นิดาด้วย subagent: โครงครบ (ACTIONS/DECLS/actionSummary/runAction, TOOLS↔DECLS), วินัยผ่านเสียงปลอดภัย, การ์ดเอกสารครบ, ไม่มี emoji ใหม่
- **แก้แล้วคืนนี้ (ปลอดภัย):** (1) การ์ดเอกสารไม่โผล่ — ตัวกัน `asstCardsHtml` เช็ค groups แต่ documents ใช้ documents → ยกเว้นให้ (2) รายงาน PDF ไม่ออก — asstDlReport เพิ่มตัวเลือก emp เอง+แจ้ง error (3) `knowledge_search` .or() strip อักขระพิเศษ (4) sw.js → v6
- **รอคุณตัดสินใจ (ไม่แก้เงียบ):** วันทำงานหน้าตรวจ (att_days พึ่งสแกนจริง) ≠ รายงาน/เงินเดือน (เครดิตควบตามตาราง) — ต่างที่ควบ 1 วัน · การแก้พร้อมกด (เติมเครดิตควบใน reviewShift ให้ non-manager ตามกฎ) รอยืนยันเพราะใกล้เงินเดือน · + เฝ้าดูรูปในรายงาน PDF (CORS) มีทางแก้พร้อม

### 📝 นิดาออก "ใบเซ็นรับทราบ" ได้ทุกขั้นวินัย (ack_form) (19 ส.ค.)
- **ขอ:** นิดาต้องออกเอกสารใบเซ็นรับทราบให้ได้ทุกขั้น (ไม่งั้นไม่มีเอกสารให้เซ็น→ถ่ายเป็นหลักฐาน)
- **Edge** `get_document` เพิ่ม `kind='ack_form'` (action_type: verbal/written/warning1/2/3 + reason) → คืนข้อมูลเอกสาร (โหลด employee เอง) · DECL + SYS อัปเดตให้ใช้ ack_form เป็นใบให้เซ็นก่อนบันทึก/ก่อนออกใบเตือน
- **Client** `buildAckDocHtml(d)` — สร้างหนังสือตักเตือนของขั้นนั้น reuse `docHeadHTML/docFootHTML/DOC_CFG` + ตาราง + เหตุ + คำเตือน (มาตรา 119(4)) + **ช่องเซ็น 3 ช่อง (ผู้ตักเตือน/พยาน/พนักงานผู้รับทราบ)** · `asstDlAckForm` render offscreen → html2canvas+jsPDF (หลายหน้า) → ดาวน์โหลด · ปุ่ม "ใบเซ็นรับทราบ PDF" ในการ์ด (ส่งข้อมูลผ่าน base64)
- **โฟลว์:** "ขอใบเซ็นรับทราบลายลักษณ์ของ [ชื่อ] เหตุผล..." → นิดาส่ง PDF → พิมพ์ → พนักงานเซ็น → ถ่ายแนบเป็นหลักฐานตอน issue_discipline
- **ตรวจ:** hr JS 0 error · edge parse OK · deploy: `supabase/functions/hr-assistant` + `hr/index.html`

### 🔎 ค้นหาพนักงานด้วยชื่อให้แม่นขึ้น (โดยเฉพาะจากเสียง) (19 ส.ค.)
- **อาการ:** พูดชื่อพนักงาน นิดาชอบบอก "ไม่มีในระบบ" ขอรหัสแทน (ชื่อจากเสียงถอดเพี้ยน + search_employees เดิม ilike ทั้งก้อน จับพลาดง่าย)
- **แก้ `search_employees` (edge):** แยกคำค้น → OR match ชื่อจริง/นามสกุล/ชื่อเล่นแยกคำ (กันสลับคำ/เพี้ยนบางคำ) · ตัดคำนำหน้า (นาย/นาง/คุณ/น้อง…) + อักขระที่ทำ .or() พัง · จัดอันดับตามความตรง · คืน count+query_used + โน้ตแนะวิธีค้นซ้ำเมื่อไม่เจอ
- **SYS:** สั่งนิดาค้นแบบใจกว้าง — ไม่เจอให้ลองชื่อจริงคำเดียว/ชื่อเล่น/นามสกุลแยกกันก่อน + เสนอรายชื่อใกล้เคียงให้เลือก · ห้ามรีบบอก "ไม่มีในระบบ"/ขอรหัส จนกว่าค้นครบทุกแบบ
- **ตรวจ:** edge parse OK · deploy: `supabase/functions/hr-assistant` (edge อย่างเดียว)

### ⚖️ นิดาออกตักเตือน วาจา/ลายลักษณ์ ได้จากแชท + กันการโม้ (19 ส.ค.)
- **ปัญหาเดิม:** นิดาพูดว่า "ออกใบเตือน/ตักเตือนให้แล้ว" ทั้งที่ไม่มีเครื่องมือ → ไม่ได้บันทึกอะไรจริง (hallucinate)
- **แก้ + เพิ่มความสามารถ (ผู้ใช้เลือก: วาจา+ลายลักษณ์):**
  - **Edge** `issue_discipline` (action, วาจา/ลายลักษณ์เท่านั้น) → คืน pendingAction (ไม่ทำที่ edge) · DECLS + actionSummary + fallback กันหลุดมา edge · SYS แยกชัด: วาจา/ลายลักษณ์ทำได้ผ่าน issue_discipline (บังคับหลักฐาน+ยืนยัน) · ใบเตือนทางการ 1/2/3 ยังทำในหน้าวินัยเท่านั้น · ห้ามอ้างว่าทำแล้วก่อนยืนยันสำเร็จ · adjust_score ≠ ออกใบเตือน
  - **Client** `asstConfirm` intercept `issue_discipline` → `asstIssueDiscipline`: หารูปหลักฐานจากข้อความที่ผู้ใช้แนบล่าสุด → เรียก `hr_disc_action_add` เดิม (ใช้ตรรกะพิสูจน์แล้ว: บังคับหลักฐาน + ตรวจลำดับขั้น + แจ้งพนักงานกดรับทราบ) · ไม่มีรูป = ไม่ให้บันทึก บอกให้แนบก่อน
- **โฟลว์:** พิมพ์ "ออกลายลักษณ์ให้ [ชื่อ] เหตุผล..." + แนบรูปเอกสารเซ็น → นิดาสรุป+ขอยืนยัน → บันทึกจริง โผล่ในหน้าวินัย
- **ตรวจ:** hr JS 0 error · edge parse OK
- **ไฟล์ deploy:** `supabase/functions/hr-assistant` + `hr/index.html`
- **เพิ่ม (19 ส.ค.): รายงานรายบุคคล → PDF จริงในแชท** — `asstDoc('report')` เปลี่ยนจาก "เปิดหน้า" เป็น `asstDlReport`: showTab empsum → genEmpSum → html2canvas+jsPDF (แบ่งหลายหน้า) → ดาวน์โหลด · รายงานนี้มีช่อง "ผู้รับทราบ ___" ในตัว = ใช้เป็น **ใบให้พนักงานเซ็นรับทราบ** ได้เลย (พิมพ์→เซ็น→ถ่าย→แนบเป็นหลักฐานตอน issue_discipline) · SYS แนะให้ใช้ get_document(kind='report') ออกใบเซ็นก่อน

### 🧠 นิดาเรียนรู้+จำได้ (คลังความรู้ · เฟส A) (19 ส.ค.)
- **แนวคิด:** ไม่ใช่เทรนโมเดลใหม่ (ทำไม่ได้บนเว็บ) แต่ใช้ "คลังความรู้ (retrieval)" — นิดาเขียนจำ + ดึงมาแนบทุกครั้ง → ฉลาดขึ้นเรื่อย ๆ และตรวจสอบได้
- **SQL** `supabase/nida_knowledge.sql`: ตาราง `nida_knowledge` (category: policy/standard/correction/faq/note + title/content/tags/source/active) · RLS เปิดแต่ไม่ให้ anon (เข้าถึงเฉพาะ edge/service_role) · trigger updated_at
- **Edge** `hr-assistant/index.ts`:
  - `remember` (action ต้องยืนยัน) — จำความรู้ใหม่เมื่อ HR สั่ง "จำไว้ว่า..." / บอกนโยบายใหม่ / แก้สิ่งที่นิดาเข้าใจผิด
  - `knowledge_search` (tool) — ค้นคลังเชิงลึก / เตรียมสอน-ออกข้อสอบ
  - `knowledgeDigest()` — ฉีดสรุปคลังความรู้ (active, ≤4200 ตัวอักษร, cache 60 วิ) เข้า system_instruction **ทุกครั้ง** → นิดาจำนโยบาย/มาตรฐาน/ข้อผิดพลาดเดิมอัตโนมัติ
  - `nida_knowledge` เข้า WRITE_TABLES → แก้/ยกเลิกความรู้ผ่าน db_update/db_delete (active=false) ได้
- **กันจำผิด:** remember เป็น action ต้อง HR ยืนยันก่อนบันทึกทุกครั้ง (ไม่จำอัตโนมัติมั่ว ๆ)
- **ตรวจ:** edge esbuild parse OK
- **ไฟล์ deploy:** รัน `supabase/nida_knowledge.sql` (ครั้งเดียว) + deploy `supabase/functions/hr-assistant`
- **เฟส B (เอกสาร→จดหมายเวียนกดรับทราบ) — เสร็จ:** ขยาย `add_announcement` รองรับ title/priority(normal|important=ต้องกดรับทราบ|mandatory=รับทราบ+ตอบคำถาม)/branch_ids/quiz_q,quiz_choices,quiz_answer/ack_deadline_h (ต่อระบบ announcements+announcement_acks+cron เตือนของเดิม) · flow ใน SYS: อ่านนโยบาย→remember→ร่างจดหมายเวียน→ประกาศบังคับรับทราบ · ตรวจใครยังไม่รับทราบผ่าน announcement_acks · ไม่ต้องแก้หน้าเว็บ (แอปพนักงานแสดง/กดรับทราบอยู่แล้ว)
- **เฟส C (สอน/ออกข้อสอบ) — เสร็จ:** เพิ่มหมวดความรู้ exam(ชุดข้อสอบ)/training(สื่อสอน) (ไม่ฉีดเข้า digest ทุกครั้ง เก็บไว้ค้นด้วย knowledge_search) · SYS สอนให้สร้างสื่อสอน/ชุดข้อสอบจากคลังความรู้+คู่มือ + เก็บซ้ำด้วย remember(category=exam) + แปลงเป็น quiz จดหมายเวียนได้
- **ตรวจ B+C:** edge esbuild parse OK · deploy: `supabase/functions/hr-assistant` (ฝั่ง edge อย่างเดียว)

### 📎 นิดา: ร่างข้อความสื่อสารพนักงาน + ดึงเอกสารดาวน์โหลดในแชท (19 ส.ค.)
- **ร่างข้อความ:** SYS เปิดทางให้นิดาร่างข้อความสื่อสารพนักงานทุกแบบ (แจ้ง/ตักเตือน/ชี้แจง/ประกาศ/ตอบแชท/หนังสือทางการ) + สั่งส่งผ่าน chat_send/chat_broadcast/add_announcement (ยืนยันก่อนส่ง)
- **เอกสารในแชท:** เครื่องมือใหม่ `get_document` (edge) คืน "การ์ดเอกสาร" → แชทเรนเดอร์ปุ่มดาวน์โหลด/เปิด
  - **สลิปเงินเดือน (PDF):** `asstDlSlip` โหลดรอบเอง (hr_payroll_run) → `plMakePdf` → ดาวน์โหลด (ไม่ต้องเปิดหน้าเงินเดือน)
  - **ใบเตือน:** `openWarningView(warning_id)` เปิดเอกสารทางการ (มีปุ่มพิมพ์/PDF ในตัว)
  - **เอกสารเซ็นแนบ:** ลิงก์ดาวน์โหลดตรงจาก disc_actions.doc_url
  - **รายงานสรุปรายบุคคล:** เปิดวิว empsum + genEmpSum(emp) แล้วกดพิมพ์
- **โครง:** edge `get_document`(kind=payslip/warning/signed_doc/report)+TOOLS+DECLS+cards type='documents'+SYS · client `asstCardsHtml` type='documents' + `asstDoc`/`asstDlSlip`
- **ตรวจ:** edge parse OK · hr JS 0 error
- **ไฟล์ deploy:** `supabase/functions/hr-assistant` (deploy edge) + `hr/index.html`

### 🤖 นิดา: ตอบวันลาให้ครบ + ยืนยันเข้าถึงทุกตาราง (19 ส.ค.)
- **อาการ:** นิดาตอบวันลาไม่ครบ ตกหล่น
- **สาเหตุ:** module `leaves` ดีฟอลต์กรองเฉพาะ "รอบปัจจุบัน" (start=cycle.start) + limit 50 → ใบลานอกเดือน/เกิน 50 หลุด · `pending_leaves` limit 50
- **แก้ (`hr-assistant/index.ts`):** ถามรายคน (มี emp_id ไม่ระบุช่วง) = คืน **ทุกใบของคนนั้น ไม่ตัดตามรอบ** (`wide` mode) · เพิ่มลิมิต (รายคน 500 / ภาพรวม 200 / เพดาน 1000) · เพิ่ม field `count` · `pending_leaves` 50→300 · เสริม SYS สอนให้ใส่ emp_id/ช่วงกว้างเมื่อถามประวัติทั้งหมด + ห้ามตัดทอนเอง
- **ยืนยันสถาปัตยกรรม:** นิดาเข้าถึง **ทุกตาราง (~90)** ได้อยู่แล้วผ่าน `list_tables`/`describe_table`/`run_sql`/`query_table` (ไม่มี whitelist จำกัดการอ่าน) — **แต่ต้องรัน `supabase/nida_full_access.sql` ใน Supabase ก่อน** (สร้าง RPC nida_tables/nida_columns/nida_sql) ไม่งั้นเครื่องมือ generic จะใช้ไม่ได้ = ตอบได้แค่หัวข้อที่มี tool เฉพาะ
- **ตรวจ:** edge TS esbuild parse OK
- **ไฟล์ deploy:** `supabase/functions/hr-assistant` (deploy edge) · ถ้ายังไม่เคยรัน `supabase/nida_full_access.sql` ให้รันใน SQL Editor ด้วย

### 🎙️ นิดา: แชทโฉมใหม่ (glass) + เสียงเรียลไทม์ Gemini Live (เฟส 1–3) (18 ส.ค.)
- **เฟส 1 — แชทโฉมใหม่** (`hr/index.html`): หน้าต่าง `#asstPop` เป็น glassmorphism (โปร่ง เบลอ ขอบกระจก มุมมน) คงเครื่องมือครบ (แนบรูป/ส่ง/ล้าง/ชิป) · avatar หัวแชท = orb · เพิ่ม **ปุ่มคลื่นเสียง** `#asst_voice` (แท่งคลื่นเคลื่อนไหว ไม่ใช้ emoji) · แนบรูป/วิเคราะห์รูปใช้ของเดิม (Gemini multimodal)
- **เฟส 2 — เสียงเรียลไทม์ Gemini Live** (โมดูล `window.NIDALive` ท้าย `hr/index.html`): ขอ ephemeral token จาก edge → `ai.live.connect` (SDK `@google/genai`) · ไมค์ 16kHz (ScriptProcessor→PCM→ส่ง) · เสียงออก 24kHz (คิวเล่นต่อเนื่อง) · overlay + คลื่นเสียงขยับตามระดับเสียงจริง · **ออร์บแตะ = เริ่มสนทนาทันที** (ปุ่มคลื่นเสียงในแชทก็เริ่มได้) · ล้มเหลว→fallback เปิดแชทพิมพ์
- **เฟส 3 — สั่งงานด้วยเสียง**: Live เรียก tool เดียว `ask_nida(question)` → พร็อกซีเข้า**สมองนิดาข้อความเดิม** (เครื่องมือ HR ครบ) → ตอบด้วยเสียง (ไม่ประกาศเครื่องมือซ้ำ = เสถียร)
- **คุมค่าใช้จ่าย:** เงียบเกิน 15 วิ (ทั้งคน+นิดาไม่พูด) = ตัดสายอัตโนมัติ · เพดาน 5 นาที/สาย · token ใช้ครั้งเดียว หมดใน 30 นาที เปิดสายใน 1 นาที
- **Edge** `hr-assistant/index.ts`: `mode:'live_token'` มินต์ token ผ่าน **REST v1beta** `auth_tokens?key=` (ไม่ใช้ SDK) · secret ปรับได้ `GEMINI_LIVE_MODEL` (ค่าเริ่ม `gemini-3.1-flash-live-preview`)
- **แก้บั๊ก WebSocket ปิดทันที (code 1008 · model not found for v1main):** เทียบแอปต้นแบบที่ใช้จริง `E:\Friday\web\live.html` — จุดชี้ขาดคือ **apiVersion ต้องเป็น `v1beta`** (ไม่ใช่ v1alpha) + โมเดล `gemini-3.1-flash-live-preview` + มินต์ token ผ่าน REST v1beta · ปรับ NIDALive ตรงต้นแบบเป๊ะ (AudioContext ปกติ ไม่บังคับ sampleRate · ถอดเสียง little-endian · SDK ไม่ pin เวอร์ชัน · กันลูปไมค์ยิง socket ที่ปิดแล้ว)
- **ตรวจ:** hr JS inline 0 error · edge TS esbuild parse OK · โครงตรงกับ Friday (proven-working)
- **ไฟล์ deploy:** `supabase/functions/hr-assistant` (deploy edge) + `hr/index.html` + `sw.js` (v5) · secret ที่ต้องมี: `GEMINI_API_KEY` (มีแล้ว), ออปชัน `GEMINI_LIVE_MODEL`

### 🔎 ตัวกรองใบลา + ธงวันหยุดบริษัทหน้าตรวจ (18 ส.ค.)
- **ตัวกรองใบลา** (`hr/index.html`): เพิ่มใน toolbar ใบลา — ค้นหาชื่อ/รหัส (`lv_q`) · กรองรอบ (`lv_cycle`: ทุกรอบ/ปัจจุบัน/ก่อนหน้า/2รอบก่อน) · กรองประเภท (`lv_type` เติมจากชนิดลาจริง) · กรอง client-side ใน `loadLeaves(refetch)` (ไม่ยิงเน็ตซ้ำ) · helper `lvCycleRange` คำนวณรอบ 21–20 (ตรวจข้ามปีผ่าน)
- **ธงวันหยุดบริษัท หน้าตรวจ (ผจก.)**: `supabase.js reviewShiftDetail` โหลด `holidays` → `is_holiday/holiday_name` ต่อวัน · `payroll-review` แถววันหยุด = พื้นแดง+กรอบซ้าย+ธง "วันหยุดบริษัท"
- ตรวจ: inline scripts 0 error · supabase syntax OK
- **ไฟล์ deploy:** `hr/index.html` (ตัวกรองใบลา) + `shared/supabase.js` & `payroll-review/index.html` (ธงวันหยุด)

### 📱 จัดระเบียบมือถือเพิ่ม: toolbar/รูป/KPI แดชบอร์ด (18 ส.ค.)
- **หัวข้อ toolbar** (ทุกหน้า): มือถือให้ h3 ขึ้นบรรทัดเต็ม (กันตกบรรทัดทีละคำ เช่น บอร์ด/เข้า/เวร) + ลดเป็น 19px · ควบคุมไหลลงล่าง — มีผลกับแดชบอร์ด ("ภาพรวม") ด้วย
- **รูปในหน้าตรวจ**: คลาส `.imgrow` — มือถือสไลด์แนวนอนแถวเดียว (nowrap+overflow-x) · จอคอมเรียงปกติ · แปลง 6 จุด (Temp Card/รูปหลักฐาน/markup)
- **แดชบอร์ด KPI "รอบปัจจุบัน"**: เพิ่ม `.kpi-wide` → มือถือเต็มความกว้าง บรรทัดเดียว (เดิมตกยาว 4 บรรทัด)
- ตรวจ inline scripts 0 error · ไฟล์ `hr/index.html`

### ⚖️ วินัย: เลื่อนขั้นเฉพาะเมื่อ "เอกสารสมบูรณ์ + ผิดใหม่หลังจากนั้น" (Rule A + doc) (18 ส.ค.)
- **ปัญหา:** ออกลายลักษณ์เมื่อวาน วันนี้ระบบเด้งให้ออกใบเตือนทันที (เลื่อนขั้นเร็วเกิน ไม่ให้เวลาปรับตัว)
- **สเปคที่ผู้ใช้ยืนยัน:** "รับทราบจริง" = HR แนบเอกสารเซ็น+คุยจนสมบูรณ์ (ไม่ใช่แค่พนักงานกดรับทราบตอนลงเวลา) · **บังคับแนบรูปเอกสาร** เก็บเป็นหลักฐาน · เริ่มนับความผิดครั้งถัดไปหลังวันปิดเอกสาร
- **ทำครบ 4 ส่วน:**
  1. **SQL** `supabase/disc_doc_complete.sql` — เพิ่ม `disc_actions.doc_url/doc_at/doc_by`
  2. **API** `hr-api.js` — `hrDiscDocComplete` (บังคับแนบรูป→doc_url, set doc_at) + dispatch `hr_disc_doc_complete` · `hrDiscipline` เพิ่ม `late_dates/absent_dates` + ตรรกะ observation (ยังไม่ปิดเอกสาร=`await_doc`, ปิดแล้วยังไม่ผิดใหม่=`observing`, ปิดแล้ว+ผิดใหม่หลัง doc_at=เลื่อนขั้น) · timeline คืน doc fields · todo รวม await_doc
  3. **UI** `hr/index.html` — ไทม์ไลน์: ปุ่ม "แนบเอกสารสมบูรณ์ (บังคับ)" + `discDocComplete()` อัปโหลด · แสดง "✔ เอกสารสมบูรณ์" / "รอ HR ทำเอกสาร" · actPill+discActionPill รองรับ await_doc/observing
- **ตรวจ:** node --check ผ่าน · จำลอง 4 กรณีถูกต้อง (กิตติพงษ์→await_doc ไม่เด้งใบเตือน · ปิดเอกสาร+ผิดใหม่→เลื่อนขั้น · ผิดครั้งแรก→วาจา)
- **หมายเหตุ deploy:** ไม่ backfill — ตักเตือนเก่า (ก่อนมีฟีเจอร์) จะขึ้น "รอ HR ทำเอกสาร" → HR แนบเอกสารเซ็นเพื่อปิด (ตามเจตนา) · escalation ถูกกั้นจนกว่าจะปิดเอกสาร
- **ไฟล์ deploy:** `supabase/disc_doc_complete.sql` (รันใน Supabase) + `shared/hr-api.js` + `hr/index.html`

### 📱 Accordion มือถือ: หน้ารายงาน + แดชบอร์ด (ผู้ใช้อนุมัติ) (18 ส.ค.)
- **แนวทาง:** ทำทีละหน้า + พรีวิวมือถือให้ดูก่อน deploy (สร้างจาก `<style>` หน้าจริง)
- **หน้ารายงาน (สรุปต่อคน):** การ์ด `.rcard` มือถือ → เพิ่มคลาส `.rcard.acc` · พับเหลือ หัว = รูป+ชื่อ+จำนวนวัน+ลูกศร · แตะกางกล่องสถิติ (scope เฉพาะการ์ดหน้านี้) · การ์ดพนักงานคงเดิม (แตะ=เปิดโปรไฟล์)
- **แดชบอร์ด (มือถือ ≤640px):** การ์ดรายการยาว (กลุ่มเสี่ยงวินัย/กะวันนี้) พับได้+พับไว้เริ่มต้น · KPI+แจ้งเตือนเปิด · ทำด้วย CSS (`#view_dashboard .card.collapsed`) + คลิกสลับ + MutationObserver ตั้งค่าเริ่มพับ · จอคอมไม่กระทบ · กันกดปุ่ม/ลิงก์ในหัวข้อไม่ให้เผลอพับ
- **ตรวจ:** inline scripts 0 error · ไฟล์เดียว `hr/index.html`
- **พรีวิว:** `_preview-mobile-report.html`, `_preview-mobile-dashboard.html` (ไม่ต้อง deploy)
- **ขยายครบแล้ว (ผู้ใช้อนุมัติทีละหน้า):** วินัย · คะแนน · เงินเดือน (เป็นตาราง → เพิ่มการ์ดมือถือ `.rcard.acc` แบบ `only-mobile` + ตารางเดิม `only-desktop` · ใช้ CSS/JS accordion เดิม · ปุ่มใช้ data-attr เดิม wiring ครอบให้อยู่แล้ว)
  - วินัย: หัวพับโชว์ จุดสีระดับ+คะแนน · กาง=สถิติ+ระดับ+ดำเนินการ+ปุ่ม (`_dcard`)
  - คะแนน: หัวพับโชว์ คะแนน/เต็ม+จุดสีระดับ · กาง=ระดับ+เคส+โบนัส+หักรวม (`_scard`)
  - เงินเดือน: หัวพับโชว์ ยอดจ่ายสุทธิ · กาง=breakdown ครบ (วัน/ฐาน/OT/เบี้ย/รวมรับ/ปกส./หัก/สุทธิ) (`_pcard`)
  - ตรวจ: inline scripts 0 error ทุกครั้ง · พรีวิว `_preview-mobile-*.html`
- **ไฟล์ deploy (accordion ทั้งชุด): `hr/index.html` ไฟล์เดียว**

### 🔤 ฟอนต์ Kanit ทั้งระบบ (จุดที่ตกหล่น) + หน้าวินัยป้ายเป็นก้อน (18 ส.ค.)
- **ต้นเหตุฟอนต์ไม่เปลี่ยนหลายจุด:** ตัวเลข/วันที่/รหัส/สถิติ ใช้ตัวแปร `--mono` = `'IBM Plex Mono'` (ไม่ใช่ Kanit) — เปลี่ยน `--mono` → `'Kanit',sans-serif` ที่ `hr` + `employee` (คง `tabular-nums` ให้เลขเรียงตรง) · IBM Plex Mono เหลือ 0
- **หน้าวินัย (#view_discipline) ป้ายม้วนเป็นก้อนกลม:** `.lvl-pill`/`.pill` radius 999px + ข้อความยาวในคอลัมน์แคบ → ทำเป็นแท็บมุมมน 10px ชิดซ้าย (scope `#view_discipline`)
- **ยังไม่แตะ:** เอกสารพิมพ์ (สลิป/ใบเตือน) ยังเป็น Anuphan/Noto (ฟอนต์ทางการ) — รอผู้ใช้ยืนยันว่าจะเปลี่ยนเป็น Kanit ด้วยไหม
- **ตรวจ:** inline scripts 0 error (hr+employee) · ไฟล์: `hr/index.html` + `employee/index.html`

### 🛠️ แก้วันทำงานเพี้ยน: ผจก. ไม่นับควบกะทุกกรณี + รายงาน=เงินเดือน (18 ส.ค.)
- **อาการ:** ผกามาศ ทาจันทร์ (ผจก.) รายงาน 24.5 แต่หน้าเงินเดือน 23.5 ไม่ตรง · ผจก.ถูกเครดิตควบ (มักถูกจัดกะ ผจก.+กะปกติ = ตารางมี ≥2 กะ/วัน → เครดิตควบเกินจริงทั้งระบบ)
- **แก้:** เติม `is_manager` เข้า select พนักงานใน `hrReport` `hrPayrollRun` `hrScoreGet` แล้ว**กันเครดิตควบให้ ผจก.** ทั้ง 3 จุด (report line ~882, payroll ~6418, score ~2324) — ผจก.นับวันจากลงเวลาจริงเท่านั้น · คนทั่วไปไม่เปลี่ยน
- **ตรวจ:** node --check ผ่าน + จำลอง 3 สูตร (ผจก.→ไม่เครดิต, คนทั่วไป→เครดิต) ผ่านหมด
- **ไฟล์:** `shared/hr-api.js` เดียว → อัป GitHub + Ctrl+Shift+R
- **หมายเหตุ:** งานปรับดีไซน์ (เอา emoji/แต่งหน้าตา) ไม่ได้แตะ hr-api.js — ยืนยันจาก mtime (hr-api 15:08 ก่อนเริ่มดีไซน์)

### 🎨 ปรับดีไซน์ Modern SaaS (เปลี่ยนเฉพาะหน้าตา — ข้อมูล/ฟังก์ชัน/ID เดิมทั้งหมด) (18 ส.ค.)
- **แนวทาง:** ทำ Preview ให้ดูก่อน push จริง → `_preview-newui.html` (HR, อ้างอิงแดชบอร์ดจริง + responsive เมนูบน/มือถือไซด์บาร์), `_preview-emp.html` (พนักงาน), `_preview-hr.html` (ไซด์บาร์) — ผู้ใช้อนุมัติทิศทาง
- **โลโก้/ข้อความ/เนื้อหา:** คงเดิมทุกอย่าง · ไม่แตะหน้า index
- **หน้า HR (`hr/index.html`) — reskin แล้ว:** ฟอนต์ Kanit (สลิป/พิมพ์คง Anuphan) · ไอคอนเส้น (outline SVG) แทน emoji ครบ 8 กลุ่มเมนู + 29 แท็บ · สกินโมเดิร์น (การ์ดมน 16px+เงานุ่ม, ปุ่ม gradient, เมนู navgrp เป็น pill green-soft) scope ≥641px กันชนไซด์บาร์มือถือ · เมนูกลุ่มดรอปดาวน์+แฮมเบอร์เกอร์มีอยู่เดิม ไม่แตะ JS/ID
- **ปุ่มลอยนิดา → orb:** `icons/nida-orb.webp` (ตัดวงกลม พื้นโปร่ง 240px 100เฟรม 10fps 331KB) + เงานุ่มสีม่วง ตัดวงกริ่งเขียวออก (`#asstFab`)
- **หน้าอื่น (reskin แล้ว):** `employee` (ฟอนต์ + ไอคอนเส้น หน้าแรก/กล้อง/สถานะ + สกินการ์ด/ปุ่ม) · `handover` `qa` `shelf` (ฟอนต์ + สกินการ์ด/ปุ่ม/pill) · `payroll-review` (ฟอนต์ + สกินการ์ด/ปุ่ม) · `mgr` `staff` `apply` `help` `rules` (ฟอนต์ Kanit)
- **ตรวจแล้ว (ทุกหน้า):** inline scripts 0 error ทั้ง 11 หน้า · tab_* ID ครบ 29 · Kanit ครบทุกหน้า · ไม่แตะ logic/ข้อมูล
- **กวาด emoji ออกทั้งระบบ (รอบ 3 — ผู้ใช้ยืนยัน "ไม่ต้องการ emoji เลย"):** สแกนเจอ hr มี emoji 122 แบบ 866 จุด → เขียนสคริปต์ /tmp/deemoji.py ทำแผนที่ emoji→ไอคอนเส้น (SVG .ei) + จุดสี (.ed) ครบทุกตัว แล้วกวาดทั้งไฟล์รวดเดียว
  - **จุดปลอดภัย:** emoji ใน JS-string sink (`.textContent/.innerText/toast/alert/confirm/prompt/.value/.placeholder`) = ลบทิ้ง (ใส่ SVG ไม่ได้เพราะจะโชว์เป็น text) · ตรวจแล้ว 0 emoji ใน attribute · 0 SVG หลุดเข้า attribute · 0 จุด logic เปรียบเทียบ emoji · คงลูกศร/เครื่องหมายข้อความ (→←✓✕★) ไว้
  - **ทำครบทุกหน้า:** hr(493) employee(48) handover(45) qa(7) shelf(8) payroll-review(11) help(23) apply(8) staff(1) manual(3) mgr — **เหลือ 0 emoji, ทุกหน้า inline scripts 0 error** (ยกเว้น index.html ที่ผู้ใช้สั่งไม่แตะ)
  - ปุ่มแชท chatFab 💬 → ไอคอนเส้น ด้วย
- **ยกเครื่องให้ตรงพรีวิวจริง (รอบ 4):** ก่อนหน้าแค่ override บาง ๆ ทับของเดิม เลย "เหมือนเดิม" — รอบนี้แก้แกน:
  - **การ์ด KPI ใส่ไอคอนในกล่องสี** (เขียว/ส้ม/แดง/เหลือง) ทุกใบ (วันนี้+รอบ) · หัวข้อ toolbar 22px · การ์ด/spacing/เงา = ชุดเดียวกับพรีวิว
  - **แถบบนเป็นแถวเดียว** (แบรนด์ | เมนู navgrp | ไอคอน แจ้งเตือน/หน้าแรก/ออกจากระบบ + avatar chip) — ย้าย `#tabbar` เข้า `.topbar` ด้วย JS, ปิด logic hdr-fixed เดิมใช้ sticky แทน, setLabel เปลี่ยนเป็น title+class (ไม่ล้างไอคอน), มือถือยังใช้ drawer
  - เก็บ emoji ตกหล่น: 🆕 (นอกช่วงกวาดรอบก่อน) + ลูกศร ◀▶▲▼ → ไอคอนเส้น · เหลือ ▾○△▭✓ (เครื่องหมายขาวดำในเครื่องมือลึก: caret/สถานะ/โปสเตอร์ ไม่ใช่ emoji)
  - พรีวิวหน้าจริง `_preview-hr-after.html` สร้างจาก `<style>` จริง = เห็นผลก่อน deploy
- **ไฟล์ deploy (ชุดดีไซน์):** `hr/ employee/ handover/ qa/ shelf/ payroll-review/ mgr/ staff/ apply/ help/ rules/ index.html` + `icons/nida-orb.webp` `icons/nida-orb.png` → อัป GitHub + Ctrl+Shift+R (ไฟล์พรีวิว `_preview-*.html` ไม่ต้องอัปก็ได้)

### 🚫 ตัดคนสิ้นสุดงานออกจากหน้าวินัย/คะแนน + ไล่สีบอร์ดวินัย (18 ส.ค.)
- **ปัญหา:** บอร์ดวินัย/หน้าคะแนน/เข้าข่ายพิจารณา ยังโชว์คนที่ตั้ง end_date (สิ้นสุดงาน) แล้ว เพราะกรองแค่ active=true
- **แก้ (ตัด end_date < วันนี้ ออก):** `hrDiscipline` query เพิ่ม `.or(end_date.is.null,end_date.gte.today)` · `hrTermCandidates` skip คน end_date ผ่านแล้ว · `hrScoreGet` คืนธง `ended` → หน้าคะแนน (loadScore) กรองออก (ไม่กระทบ payroll ที่ใช้ scMap)
- **บอร์ดวินัย (actPill) "เหมือนยังไม่แก้":** เพิ่มไล่สีตามความรุนแรง (วาจา=เหลือง→ลายลักษณ์=ส้ม→ใบเตือน=แดง→เลิกจ้าง=แดงทึบ) + รองรับสถานะ need_termination (ครบ 3 ใบ) + พับ "· ใบเตือน N/3" + นับ todo รวมเคสครบ 3
- **ไฟล์:** `shared/hr-api.js` + `hr/index.html` → อัป GitHub + Ctrl+Shift+R

### 🙈 หน้า "สถานะของฉัน" (พนักงานเห็นตอนลงเวลา): แสดงแค่รอบปัจจุบัน (18 ส.ค.)
- **ปัญหา:** การ์ดสถานะเป็นรอบปัจจุบันอยู่แล้ว แต่ประวัติ **ใบลา + ข้อเสนอ + แจ้งเตือน** แสดงทั้งหมดตั้งแต่เริ่ม → ยาวเกิน
- **แก้:** ส่ง s.cycle เข้า renderMyLeaves/renderProposalHistory/renderNotifHistory → กรองเฉพาะที่ overlap รอบปัจจุบัน (21–20) + ป้าย "(รอบนี้)"
- **ไฟล์:** `employee/index.html` → อัป GitHub + Ctrl+Shift+R

### 🔧 แก้ 4 จุด: หน้าเข้าข่ายพิจารณา + payroll + ตั้งค่า (18 ส.ค.)
- **1+4) หน้า "เข้าข่ายพิจารณา" (hrTermCandidates):** เดิมนับขาดงาน/ข้อมูลย้อนหลัง **90 วัน** (ข้ามหลายรอบ → ยาว + ไม่ตรงหน้าคะแนน) → เปลี่ยนเป็น **รอบปัจจุบัน** (cyc.start–endEff) · ตอนนี้ "ขาดติดกัน" นับเฉพาะรอบนี้ ตรงกับหน้าอื่น
- **2) หน้า Payroll:** เพิ่มคอลัมน์ **"รวมเงินเพิ่ม"** (OT + เบี้ย/โบนัส + เพิ่มอื่น · ไม่รวมฐานเงินเดือน) + ยอดรวมด้านบน
- **3) หน้าตั้งค่าเกณฑ์วันทำงานขั้นต่ำ:** เพิ่มบรรทัด "📆 รอบปัจจุบันมี X วัน (21 เดือนก่อน–20 เดือนนี้)" + อธิบายนิยามรอบ
- **ไฟล์:** `shared/hr-api.js` + `hr/index.html` → อัป GitHub + Ctrl+Shift+R

### 🤖 อัปเดตนิดา: รู้ควบกะ + วันทำงาน + เงื่อนไขเบี้ยวินัย + อัพเดทวันนี้ทั้งหมด ✅ (13 ส.ค.)
- **read `dual_shift_report`:** รายงานควบกะ+วันทำงานต่อคน (cycle/branch/emp · only_dual) → days_worked (เครดิตควบ 2 กะ=2 วัน) + dual_days + dual_dates (วัน+ชื่อกะ เช่น บ่าย/ดึก) · ถามนิดา "ใครควบวันไหน / ควบกี่วัน / สรุปวันทำงาน" ตอบได้
- **SYS เพิ่มความรู้:** "เบี้ยวินัย" คืออะไร + ได้/ไม่ได้เมื่อไหร่ (dil_off / min_work_days / ขาด-สาย) → ตอบ "ทำไมไม่ได้เบี้ยวินัย" ได้ · เกณฑ์วันทำงานขั้นต่ำ (min_work_days/penalty) · วินัยสะสม rolling window + ครบ 3 ใบเปิด termination_cases · ควบ=2 วัน
- **ไฟล์:** `supabase/functions/hr-assistant/index.ts` · esbuild ผ่าน · Deploy: `supabase functions deploy hr-assistant`

### 📅 เกณฑ์วันทำงานขั้นต่ำต่อรอบ → ตัดเบี้ยวินัย + หักคะแนน ✅ (13 ส.ค.)
- **ต้องการ:** ตั้งจำนวนวันทำงานขั้นต่ำ (เช่น 26) · ถ้าไม่ถึงในรอบ → ไม่ได้รับเบี้ยวินัย + หักคะแนน
- **ตั้งค่า:** `min_work_days` (เกณฑ์วัน · 0=ปิด) + `min_work_days_penalty` (หักกี่คะแนน · 0=ไม่หักคะแนนแต่ยังตัดเบี้ย) — หน้าตั้งค่าคะแนน มีการ์ด "📅 เกณฑ์วันทำงานขั้นต่ำต่อรอบ"
- **เบี้ยวินัย (payroll):** `stats.below_min = days_worked < min_work_days` → `_payrollCompute` ตัดทั้งโบนัสแบนด์ + เบี้ยขยัน (เหมือน dil_off)
- **คะแนน (hrScoreGet):** ไม่ถึงเกณฑ์ → หัก min_work_days_penalty คะแนน (รายการ "วันทำงานไม่ถึงเกณฑ์ (X/26 วัน)") · คืน days_worked/below_min
- **นับควบกะ:** ทั้งสองจุดเครดิตควบตามตารางเวร (ตรงกับหน้ารายงาน/เงินเดือน)
- **ไฟล์:** `shared/hr-api.js` + `hr/index.html` → อัป GitHub + Ctrl+Shift+R · หมายเหตุ: HR override วันทำงานในโมดัลเงินเดือนได้ (จะพ้นเกณฑ์)

### ⚖️ วินัยสะสมข้ามรอบ (Progressive Discipline · rolling window) ✅ (13 ส.ค.)
- **ปัญหาเดิม:** คะแนน+ขั้นวินัยรีเซ็ตทุกรอบ → พนักงานโดน "ใบเตือนระดับ 1" ได้ทุกเดือนไม่มีวันครบ 3 ใบ (ไม่ถึงเลิกจ้าง) + แดชบอร์ดแดงรกทุกเดือน
- **ออกแบบใหม่ (ตามที่เลือก):** สะสมแบบ **rolling window** (ดีฟอลต์ 6 เดือน · ตั้งค่า `disc_window_months`) · **เลื่อนขั้นเมื่อคะแนนรอบนี้ถึงเกณฑ์ (breach)** · **ครบ 3 ใบสะสม → เปิดเคสพิจารณาเลิกจ้างอัตโนมัติ** (HR ตัดสิน ไม่เลิกจ้างเอง)
- **หลักการ:** แบนด์คะแนน = ตัวจับ "รอบนี้ทำผิดถึงเกณฑ์" · ประวัติสะสมใน window = ขั้นที่เคยทำ · ขั้นต่อไป = ขั้นเหนือจากที่เคยทำ (วาจา→ลายลักษณ์→ใบเตือน 1→2→3) **ไม่วนซ้ำขั้นเดิม** · รอบไหนสะอาด (ไม่ breach) = ไม่ขึ้นบอร์ด → แดชบอร์ดไม่รก
- **แก้ (`shared/hr-api.js`):** บอร์ดวินัยดึง disc_actions ช่วง window + คำนวณ verbalDone/writtenDone/warningCount สะสม + `need`/`ladder`/`warning_total`/`near_termination`/`action_status='need_termination'` · บังคับลำดับ (hrDiscActionAdd/hrWarningIssue) ยึด window แทน cycle_start · hrWarningIssue เปิด termination_case เมื่อครบ 3 ใบ
- **UI (`hr/index.html`):** ป้าย "📄 ใบเตือนสะสม N/3" + "⛔ ครบ 3 → พิจารณาเลิกจ้าง" · dashboard todo รวมเคส need_termination
- **ไฟล์:** `shared/hr-api.js` + `hr/index.html` · ต้องมี `supabase/termination.sql` (ติดตั้งแล้ว) · Deploy: push 2 ไฟล์ → Ctrl+Shift+R
- **เฟส 3 (ทำแล้ว):** ✅ หน้าตั้งค่า "ช่วงวินัยสะสม (เดือน)" ในหน้าตั้งค่าคะแนน (`disc_window_months` · hr_settings_save) · ✅ ปิด "ออกใบเตือนอัตโนมัติจากคะแนน" (`hrScoreIssueWarnings` — ขัดกับบันได/หลักฐาน · แจ้งให้ทำทีละขั้น) · ✅ ทดสอบ logic 8 เคสผ่าน (เลื่อนขั้น/ครบ3เปิดเคส/รอบสะอาดไม่ขึ้น/window เคลียร์รีเซ็ต)
- **เฟส 3 ครบ:** ✅ โชว์บันไดสะสมให้พนักงานเห็นตอนกดรับทราบวินัย (`myDisciplineLadder` ใน supabase.js → กล่องม่วงในหน้ารับส่งผลัด: วาจา✓→ลายลักษณ์✓→ใบเตือน N/3 + เตือนครบ3=เลิกจ้าง/ทำดีเกิน window=เคลียร์)
- **ไฟล์เพิ่ม:** `shared/supabase.js` (myDisciplineLadder) + `handover/index.html` (กล่องบันไดในโมดัลรับทราบ)
- **UI ล้างความรก (แดชบอร์ด "กลุ่มเสี่ยงวินัย"):** เดิม 4-5 ป้าย/คน (ระดับแบนด์ + สะสม + สถานะ + timeline + ขั้นต่อไป · บางอันซ้ำ) → เหลือ **1 บรรทัด/คน**: ชื่อ(รหัส·คะแนน·สาย/ขาด) + **ป้ายสถานะเดียว** (พับจำนวนใบเตือนเป็นหาง "· ใบเตือน 2/3") + ปุ่ม 🧾 (ไทม์ไลน์เต็มไปดูข้างใน) · กรองเหลือ**เฉพาะคนที่ต้องดำเนินการจริง** (pending/ครบ3/มีเคส) ไม่ใช่ทุกคนที่คะแนนตกแบนด์

### 🔁 ควบกะสแกนครั้งเดียว: เครดิตวันทำงานตามตารางเวร (ควบ=2 แม้ลงเวลาแถวเดียว) ✅ (13 ส.ค.)
- **ปัญหา:** ป้ายควบจับจาก "ตารางเวร (2 กะ)" แต่ days_worked นับจาก "แถวลงเวลา" · เคส 6 ส.ค. สแกนครั้งเดียว (เข้า 12:34 บ่าย ออก 08:51 ข้ามถึงดึก) → นับแค่ 1 → วันทำงาน 24 ทั้งที่ควรได้ 25
- **แก้ (ให้ตรงกันทั้งรายงาน + Payroll):** วันที่เป็นควบ (จัดเวร ≥2 กะ) + มาทำงานจริง แต่ลงเวลาน้อยกว่าตาราง → **เครดิตวันทำงานตามยอดตารางเวร** (สแกน 1 = เติมเป็น 2)
  - `hrReport` summary: correction pass หลังนับแถวลงเวลา
  - `hrPayrollRun`: เพิ่ม query schedules + เครดิต workedDV + `dual_days`/`dual_dates` จับจากตารางเวร (เฉพาะวันมาทำงาน) → ตรงกับรายงาน
- **ปลอดภัย:** เครดิตเฉพาะ "เพิ่ม" ให้วันควบที่มาทำงานจริง · วันปกติไม่กระทบ · เงื่อนไข sched>att เท่านั้น (2 แถวลงเวลาอยู่แล้ว = ไม่เติมซ้ำ)
- **ไฟล์:** `shared/hr-api.js` → อัป GitHub + Ctrl+Shift+R · รอบเงินเดือนที่ปิดแล้วยังตรึงค่าเดิม (reopen+คำนวณใหม่ถ้าต้องปรับ)

### 🔁 หน้า Payroll: แสดงควบกะ (โน้ตในตาราง + รายการวันในโมดัล) ✅ (13 ส.ค.)
- `hrPayrollRun` เพิ่ม `dual_days` + `dual_dates` (วันที่ลงเวลา ≥2 กะในวันเดียว · ตรงกับที่นับวันทำงานจ่ายจริง) แนบใน outItems (ทั้งรอบเปิด/ปิด)
- **ตาราง Payroll:** ใต้ช่องวันทำงานโชว์ป้าย "🔁 ควบ N" (hover เห็นวันที่+กะ)
- **โมดัลรายคน:** กล่องม่วง "🔁 ควบกะ N วัน" + ลิสต์วันที่ (เช่น 6 ส.ค. (บ่าย/ดึก))
- **ไฟล์:** `shared/hr-api.js` + `hr/index.html` → อัป GitHub + Ctrl+Shift+R
- **หมายเหตุ:** payroll จับควบจาก "ลงเวลา ≥2 กะ" (ตรงกับยอดจ่าย) · ถ้าวันควบลงเวลาแถวเดียว จะนับ 1 วัน → ปรับได้ที่ช่อง "วันทำงาน (แก้เอง)"

### 🔁 หน้ารายงานลงเวลา: ติดป้าย "ควบกะ" ให้เห็นชัดว่าวันไหนควบ ✅ (13 ส.ค.)
- **ปัญหา:** ดูรายละเอียดลงเวลาแล้วไม่รู้ว่าวันไหนควบกะ (เช่น 6 ส.ค. บ่าย/ดึก)
- **แก้:** `hrReport` เพิ่มธง `dual_shift` + `dual_label` — วันควบ = มี ≥2 กะในวันเดียว (จากตารางเวร หรือแถวลงเวลาจริง · ครอบคลุมเคสลงเวลาแถวเดียวแต่จัดควบ 2 กะ) · หน้า "รายละเอียด" โชว์ป้าย 🔁 ควบกะ + ชื่อกะ (เช่น "บ่าย/ดึก") ที่ช่องวันที่
- **ไฟล์:** `shared/hr-api.js` + `hr/index.html` (static) → อัป GitHub + Ctrl+Shift+R

### ⚖️ บันไดวินัยบังคับตามลำดับ + หลักฐานครบทุกขั้น ✅ (13 ส.ค.)
- **ปัญหา:** ระบบเลือก "ขั้นที่ต้องทำ" จากแบนด์คะแนนปัจจุบัน → คะแนนตกทีเดียวกระโดดข้ามไป "ใบเตือนระดับ 1" ทันที ข้ามตักเตือนวาจา/ลายลักษณ์อักษร (ขัดระเบียบ HR ที่ต้องมีหลักฐานครบทุกขั้น)
- **แก้ (บังคับเข้ม + แนบหลักฐาน · ตามที่เลือก):**
  - คะแนนกำหนดแค่ "ปลายทาง" · **ขั้นที่ต้องทำต่อไป = ขั้นแรกในบันไดที่ยังไม่ได้ทำ** (verbal→written→warning) — บอร์ดวินัยคืน `action_needed` เป็นขั้นถัดไป + `ladder` (บันไดทั้งเส้น สถานะทำแล้ว/ค้าง)
  - **บังคับที่ backend:** `hrDiscActionAdd` + `hrWarningIssue` — ออก written ต้องมี verbal ก่อน · ออกใบเตือนต้องมี verbal+written ก่อน · **ทุกขั้นต้องแนบรูปหลักฐาน** (บล็อกถ้าไม่มี)
  - **UI:** โมดัลบันทึกวินัย + โมดัลออกใบเตือน เพิ่มช่องแนบรูปหลักฐาน (บังคับ) + โมดัลใบเตือนโชว์บันไดสถานะ + เตือนขั้นที่ยังขาด
- **ไฟล์:** `shared/hr-api.js` + `hr/index.html` (static · ไม่มี SQL — ใช้ disc_actions.photos ที่มีอยู่) → อัป GitHub + Ctrl+Shift+R
- **หมายเหตุ:** พนักงานได้รับแจ้ง+กดรับทราบทีละขั้นตามลำดับอยู่แล้ว · (เสริมได้: โชว์บันไดทั้งเส้นให้พนักงานเห็นล่วงหน้าในหน้ารับทราบ)

### 🚪 พนักงานลาออกแล้วยังขึ้น "ขาด"/หักคะแนน/ใบเตือน — หยุดที่วันสิ้นสุดงาน ✅ (13 ส.ค.)
- **ปัญหา:** ลาออก + จัดเวรล่วงหน้าไว้ 1 สัปดาห์ · ระบบยังนับวันหลังลาออกเป็น "ขาด" → หักคะแนนต่อเนื่อง + ขึ้นใบเตือน แม้กดสิ้นสุดงาน/ปิดใช้งานแล้ว (กระทบคะแนน+เงิน)
- **แก้ 2 ชั้น:**
  1. **ตัดต้นตอ:** `hrSave` — เมื่อกำหนด end_date (หรือปิดใช้งาน) จะ **ลบตารางเวรหลังวันสิ้นสุด** อัตโนมัติ → ไม่มีวันขาดหลอกในทุกหน้า
  2. **กันเชิงรับ:** `hrScoreGet` — ไม่นับ "ขาด/วันควรทำ" ของวันที่ > end_date (เผื่อยังมีเวรค้าง/ยังไม่ได้ปิดใช้งาน)
- **เคสเก่า:** `supabase/_ล้างเวรอนาคตพนักงานลาออก.sql` (พรีวิว + ลบเวรหลัง end_date/หลังวันนี้ของคนที่ปิดใช้งาน)
- **ไฟล์:** `shared/hr-api.js` (static) → อัป GitHub + Ctrl+Shift+R · หมายเหตุ: รอบเงินเดือนที่ปิดแล้วยังตรึงค่าเดิม (reopen+คำนวณใหม่ถ้าต้องปรับ)

### 🔁 ควบกะให้นับ 2 วันทำงาน (แก้ตัวเลขไม่ตรงกันหลายหน้า) ✅ (13 ส.ค.)
- **ปัญหา:** ตัวเลขวันทำงานควบกะไม่ตรงกัน — หน้ารายงาน/ตรวจวันทำงาน (payroll-review) sum ตาม row = นับ 2 อยู่แล้ว · แต่หน้าเงินเดือน + คะแนน + รายละเอียดพนักงาน เก็บค่า `X[work_date] = day_value` (เขียนทับตามวันที่) = กะที่ 2 ทับกะแรก นับแค่ 1 → ดูแล้วงง
- **แก้ (ตามที่เลือก: ควบ = 2 วัน):** เปลี่ยน 3 จุดจาก "เขียนทับ" เป็น "บวกทุกกะในวัน" ให้ตรงกับหน้าที่เหลือ
  - `hrPayrollRun` (workedDV) — **เงินเดือนจ่ายจริง**: รายวันควบ 2 กะ = 2 วัน มีผลกับค่าแรง
  - หน้าคะแนน/ตรวจวินัย (attDV) · `employee_detail` (attDV)
- **ไม่กระทบ "ขาด":** การนับขาดใช้ "วันที่มีเวรแต่ไม่มา" (เช็คว่ามาหรือไม่มา ไม่ใช่ผลรวม day_value) จึงไม่เปลี่ยน
- **ไฟล์:** `shared/hr-api.js` (static) → อัป GitHub + Ctrl+Shift+R
- **หมายเหตุ:** รอบที่ปิดไปแล้วยังใช้ค่าที่ตรึงไว้ (นับ 1) · รอบใหม่/ยังไม่ปิดจะคำนวณ 2 ให้ · ถ้าอยากให้รอบเก่าปรับด้วยต้อง reopen+คำนวณใหม่ · หน้าพนักงานดูตัวเอง (นับแบบง่าย ไม่ถ่วงครึ่งวัน) ยังคงเดิม

### 📧 แก้ส่งสลิป "ไม่มีอีเมล" ทั้งที่พนักงานกรอกแล้ว ✅ (13 ส.ค.)
- **ปัญหา:** email ถูกเก็บสแนปช็อตลง `payroll_items` ตอนคำนวณรอบ · ถ้ารอบถูกปิดก่อนพนักงานกรอกอีเมล → payroll_items.email ค้างว่าง · รอบปิดแล้ว hrPayrollRun คืนแถวเดิม (บรรทัด 6332) ไม่รีเฟรช → หน้าส่งสลิปขึ้น "ไม่มีอีเมล"
- **แก้:** ตอนสร้าง `outItems` (จุด return เดียว) รีเฟรช **email + bank_name + bank_account** จาก employees ปัจจุบันเสมอ (ข้อมูลติดต่อ/โอน ไม่ใช่ยอดเงินที่ตรึง) — มีผลทั้งดู/ส่ง โดยไม่แตะยอดเงินในรอบที่ปิดแล้ว
- **ไฟล์:** `shared/hr-api.js` (static) → อัป GitHub + Ctrl+Shift+R
- **หมายเหตุ:** ถ้าอีเมลยังไม่ขึ้น = ยังไม่ได้บันทึกลง employees จริง (เช็คว่า HR อนุมัติใบแก้ข้อมูลแล้ว) — นิดาเช็คได้ด้วย incomplete_profiles

### 🛵 แก้ GPS เลขไมล์ขึ้น "นอกเขต" ตอนไรเดอร์ไปวิ่งแทนสาขา ✅ (13 ส.ค.)
- **ปัญหา:** `riderLogOdometer` เช็ก GPS กับ **สาขาประจำของรถ** (`veh.branch_id`) เท่านั้น → ไรเดอร์ที่ถูกจัดไปวิ่งแทนสาขาอื่น อยู่ที่สาขาที่ไปแทนจริง แต่ระบบวัดระยะจากสาขาตัวเอง เลยขึ้น ✕ นอกเขต
- **แก้:** รวมสาขาที่เกี่ยวข้องวันนั้น = สาขาประจำรถ + สาขาที่ถูกจัดกะ (schedules · รวมไปแทน) + สาขาที่เช็กอินจริง (attendance) → `gps_ok=true` ถ้าอยู่ในรัศมี **สาขาใดสาขาหนึ่ง**
- **ไฟล์:** `shared/supabase.js` (static · ไม่มี SQL) → อัป GitHub + Ctrl+Shift+R
- **หมายเหตุ:** มีผลกับการบันทึกใหม่ · แถวเก่าที่ขึ้น ✕ ไปแล้วยังเดิม (HR แก้รายแถวได้ด้วยปุ่ม ✏️ ในประวัติเลขไมล์ ถ้าต้องการ)

### ⛽ เบิกค่าน้ำมัน: เปิดเฉพาะวันจันทร์ · รับเงินวันอังคาร ✅ (12 ส.ค.)
- **กติกา:** เปิดให้ไรเดอร์เบิกได้เฉพาะวันที่กำหนด (ดีฟอลต์ = จันทร์) · รับเงินวันถัดไป (อังคาร)
- **Backend (บังคับจริง):** `riderFuelSubmit` บล็อกถ้าวันนี้ไม่ตรง open_weekday + บันทึก `payout_due_date` = วันถัดไป · `riderFuelQuota` คืน is_open_today/open_day_th/payout_day_th
- **ตั้งค่าได้:** คอลัมน์ `rider_fuel_config.open_weekday` (0-6 · ดีฟอลต์ 1=จันทร์) · HR เปลี่ยนวันได้ในหน้า "⚙️ ตั้งค่าเบิกค่าน้ำมัน" (ดรอปดาวน์วัน) · `hrFuelCfgSave` รับ open_weekday
- **UI ไรเดอร์:** ถ้าไม่ใช่วันเปิด → เด้งกล่อง "ยังไม่เปิดให้เบิกวันนี้ · เบิกได้เฉพาะวันจันทร์ · รับเงินวันอังคาร" · ถ้าเปิด → กล่องข้อมูลบอกวันเปิด+วันรับเงิน
- **ไฟล์:** `supabase/rider_fuel_weekday.sql` (รันครั้งเดียว) + `shared/supabase.js` + `shared/hr-api.js` + `hr/index.html` + `employee/index.html`
- **⚠️ Deploy:** รัน SQL + อัปไฟล์สแตติก (Ctrl+Shift+R)

### 🙈 ซ่อนพนักงานที่ปิดใช้งานจากดรอปดาวน์/รายชื่อเลือกคน ✅ (12 ส.ค.)
- **ปัญหา:** พนักงานที่ปิดใช้งาน (active=false) ยังโผล่ในจุดเลือกคน ทำให้ ผจก.สับสน
- **handover (รับส่งผลัด):** `getShiftBoard` — `members` (คนในกะจากตารางเวร) กรองเหลือเฉพาะ active (เดิม colleagues/all_staff กรองแล้ว แต่ members ไม่กรอง → คนที่ปิดใช้งานแต่ยังมีเวรค้างโผล่ในดรอปดาวน์ "มอบงาน") · ไฟล์ `shared/supabase.js`
- **ดรอปดาวน์ฝั่ง HR/ผจก. ที่ยังไม่กรอง → กรอง active!==false:** คีย์เบิกให้พนักงาน (`openAdvKey`), คีย์เบิกน้ำมันย้อนหลัง (`openRdFuelKey`), เจ้าของรถไรเดอร์ (`openRdVeh` · คงเจ้าของเดิมที่ปิดใช้งานไว้ถ้ากำลังแก้), สรุปพนักงาน (`initEmpSum`), ตรวจวันทำงาน (`initDuty`) · ไฟล์ `hr/index.html`
- **หมายเหตุ:** จุดจัดกะ/ควบผลัด/ตรวจเงินเดือน/มอบงานพิเศษ/เชลฟ์ กรอง active อยู่แล้ว · แผนที่ชื่อสำหรับรายงาน/ประวัติ (สถิติไรเดอร์ ฯลฯ) ไม่แตะ เพราะต้องคงชื่อคนเก่าไว้
- **Deploy:** อัป `shared/supabase.js` + `hr/index.html` → Ctrl+Shift+R

### 🤖 อัปเดตนิดา: ตรวจ+แก้ครบทุกจุดที่ปรับปรุงล่าสุด ✅ (12 ส.ค.)
- **เบิกเงินล่วงหน้า:** read `advance_pending` (คำขอรออนุมัติ + ธงฉุกเฉิน + ยอดรวม) · action `advance_review` (อนุมัติ/ไม่อนุมัติ · ปรับยอดได้ไม่เกินที่ขอ · กำหนดวันโอนเอง: ฉุกเฉิน=N วันทำการ, ปกติ=วันจ่ายรอบ · แจ้งพนักงาน + log) — ครบ ACTIONS/actionSummary/runAction/DECLS
- **WRITE_TABLES เพิ่ม `applicants` + `advance_requests`** → นิดาแก้/ลบผ่าน db_update/db_delete (ยืนยันก่อน)
- **ข้อมูลพนักงานไม่ครบ:** read `incomplete_profiles` — ใครยังไม่กรอกอีเมล/บัญชีธนาคาร (include_all=true เพิ่มเบอร์+เลขบัตร) · สรุป no_email/no_bank
- **ค้นข้อมูลพนักงานละเอียด:** ขยาย `employee_contact` คืนครบทุกช่อง (เบอร์, อีเมล, ที่อยู่, ผู้ติดต่อ+เบอร์ฉุกเฉิน, วันเริ่ม/สิ้นสุดงาน, สาขา, กะ, วันหยุด, ธนาคาร+เลขบัญชี, เลขบัตร, เอกสารที่แนบ, ช่องที่ยังไม่กรอก)
- **SYS เพิ่มวิธี:** กู้เคสรับเข้าค้าง (hired_emp_id ค้าง → set status='hired' หรือ hired_emp_id=null) · ลบใบสมัครซ้ำ/ไม่ผ่าน · ตรวจที่มา "ควบกะ" (note='เพิ่มเข้ากะเฉพาะกิจ' + activity_log) · ค่ากะดึก (ธง night_allowance) · ลบ SYS ค่ากะดึกที่ซ้ำจากตอน merge F/E
- **ไฟล์:** `supabase/functions/hr-assistant/index.ts` · esbuild ผ่าน · Deploy: `supabase functions deploy hr-assistant`

### 🔔 แจ้งเตือน HR เมื่อพนักงานเบิกเงินล่วงหน้า ✅ (12 ส.ค.)
- **ปัญหา:** HR ไม่ได้เปิดหน้าเบิกทุกวัน → หลุดการอนุมัติคำขอพนักงาน
- **3 ชั้น:**
  1. **Push ทันที** — พนักงานส่งคำขอ (`advanceSubmit` ใน supabase.js) → ยิง `hr-notify` ทันที → เด้ง push เข้าเครื่อง HR ที่เปิดแจ้งเตือน
  2. **hr-notify รวมคำขอค้าง** — เพิ่ม `advance_requests` status='submitted' เข้า events (dedup ต่อคำขอด้วย notify_sent) → รอบตรวจปกติก็ทวนซ้ำถ้ายังไม่อนุมัติ
  3. **การ์ด+badge บนแดชบอร์ด HR** — `loadAdvancePending` (action `advance_pending` → `hrAdvancePending`) การ์ดสีเขียว "มีคำขอเบิกเงินรออนุมัติ N รายการ" + badge แดงบนเมนู "การเงิน" · แตะการ์ดไปหน้าเบิกได้เลย · รีเฟรชอัตโนมัติหลังอนุมัติ
- **ไฟล์:** `supabase/functions/hr-notify/index.ts` (deploy แยก) + `shared/supabase.js` + `shared/hr-api.js` + `hr/index.html`
- **⚠️ Deploy:** อัปไฟล์สแตติก (Ctrl+Shift+R) + `supabase functions deploy hr-notify --no-verify-jwt` · HR ต้องกดปุ่ม 🔔 เปิดแจ้งเตือนบนเครื่องด้วยถึงจะได้ push

### 🚨 เบิกฉุกเฉิน push เด่นแยก + เตือนซ้ำทุก 2 ชม. ✅ (12 ส.ค.)
- **ฉุกเฉินแยกเด่น:** คำขอ kind='emergency' ส่ง push **แยกต่างหาก** หัวข้อ "🚨 เบิกเงินฉุกเฉิน — รออนุมัติด่วน" · `tag` ต่อคำขอ (ไม่รวมกับชุด hr-notify) · `requireInteraction` (ค้างจอจนกดปิด) + สั่นแรง `[300,120,300,120,300]` · ปกติยังรวมในชุดแจ้งเตือน HR เหมือนเดิม
- **เตือนซ้ำทุก 2 ชม.:** dedup key ใส่ time-bucket `:b<floor(ชม.ตั้งแต่สร้าง/2)>` → ทุก 2 ชม.คีย์เปลี่ยน = ยิงซ้ำ (ทั้งฉุกเฉินและปกติ) จนกว่าจะอนุมัติ (อนุมัติ→status เปลี่ยน→หลุด query→หยุด) · cron เรียก hr-notify ทุก 5 นาทีอยู่แล้ว (`push_cron.sql`)
- **sw.js:** รองรับ `requireInteraction` + `vibrate` จาก payload (ของเดิม fix ค่าเดียว)
- **ไฟล์:** `supabase/functions/hr-notify/index.ts` + `sw.js` · Deploy: `supabase functions deploy hr-notify --no-verify-jwt` + อัป sw.js (Ctrl+Shift+R)

### 👁 หน้าส่งสลิปอีเมล: ปุ่มพรีวิว + พิมพ์ รายคน ✅ (12 ส.ค.)
- เพิ่มปุ่ม **👁 พรีวิว** (เปิดหน้าต่างสลิป + มีปุ่มพิมพ์ในตัว) และ **🖨 พิมพ์** (สั่งพิมพ์ A4 ผ่าน iframe ซ่อน · เลี่ยงป๊อปอัพ) ต่อแถวในโมดัลส่งสลิป — ใช้ได้ทุกคนแม้ไม่มีอีเมล
- ใช้ตัวสร้างสลิปเดิม (`plSlipInner`/`plSlipDocHtml`/`PL_SLIP_STYLE`) → `plPreviewOne`/`plPrintOne`
- **ไฟล์:** `hr/index.html` (static · ไม่มี SQL) → อัป GitHub + Ctrl+Shift+R

### 🐛 แก้บั๊ก: กด "รับเริ่มงาน" แล้วการ์ดไม่ย้ายไปคอลัมน์รับเริ่มงาน ✅ (12 ส.ค.)
- **อาการ:** กดรับเข้าทำงาน → toast ขึ้น "รับเข้าทำงานแล้ว · รหัส …" แต่การ์ดยังค้างที่ "นัดสัมภาษณ์" กดกี่ครั้งก็ไม่ขยับ (เคส อัญชิษฐา สารี · รหัสค้าง 0000009)
- **ต้นตอ:** `hrApplicantHire` เจอ `a.hired_emp_id` ไม่ว่าง ก็ `return already:true` ทันที โดยไม่แก้ status → ถ้าเคยมี attempt ที่ set hired_emp_id ค้างไว้แต่ไม่มีพนักงานจริง/สถานะไม่อัปเดต การ์ดจะค้างตลอด
- **แก้:** ก่อน return ให้เช็กว่ามีพนักงานจริงตาม hired_emp_id ไหม · **มีจริง** → อัป status='hired' ให้การ์ดย้าย · **ไม่มี (รหัสค้างเปล่า)** → ถือว่ายังไม่รับเข้า แล้วรับเข้าใหม่ด้วยรหัสที่กรอกจริง
- **ไฟล์:** `shared/hr-api.js` (static · ไม่มี SQL/backend) → อัป GitHub + Ctrl+Shift+R
- **ตรวจ/ล้างข้อมูลค้าง:** `supabase/_ตรวจผู้สมัครค้างรับเข้า.sql` (หาเคสค้าง + วิธี UPDATE แก้รายราย)

### 🔗 รับเข้าทำงาน: เชื่อมกับพนักงานที่เพิ่มเองไปแล้ว (รหัสซ้ำ) ✅ (12 ส.ค.)
- **เคส:** กดรับเข้าไม่ได้ HR เลยไปเพิ่มพนักงานเองในหน้าพนักงาน → พอกลับมากดรับเข้า ระบบฟ้อง "รหัสถูกใช้แล้ว" การ์ดค้างตลอด
- **แก้:** `hrApplicantHire` เจอรหัสซ้ำจะคืน `code_exists` → UI โชว์ปุ่ม **"🔗 เชื่อมกับพนักงานรหัสนี้ (เพิ่มเองไปแล้ว)"** · กดแล้วส่ง `link_existing` → เชื่อมใบสมัครกับพนักงานที่มีอยู่ (set status=hired, hired_emp_id) ไม่ insert ซ้ำ · การ์ดย้ายไปรับเริ่มงาน
- **ไฟล์:** `shared/hr-api.js` + `hr/index.html`

### 🗑 ลบใบสมัคร (ใบซ้ำ/ไม่ผ่าน) ✅ (12 ส.ค.)
- **ที่มา:** หน้ารับสมัครรกเพราะมีใบซ้ำ/ไม่ผ่านค้างเยอะ
- **เพิ่ม:** ปุ่ม "🗑 ลบใบสมัคร" ในการ์ดรายละเอียดผู้สมัคร (`hr_applicant_delete` → `hrApplicantDelete`) · ลบได้เฉพาะที่ **ยังไม่รับเข้าทำงาน** (status≠hired และไม่มี hired_emp_id) — คนที่รับเข้าแล้วต้องไปลบที่หน้าพนักงาน กันข้อมูลพนักงานหลุด · ลง activity_log
- **ไฟล์:** `shared/hr-api.js` + `hr/index.html` (static · ไม่มี SQL)

### 🐛 แก้บั๊ก: ปุ่มปิดเบี้ยวินัย "เด้งกลับเขียว" หลังกดบันทึก ✅ (29 ก.ค.)
- **อาการ:** กด toggle ปิดเบี้ยวินัย (chip → 🚫 ปิดไว้) บันทึกลง DB ได้จริง (`reviewSetDil` คืน OK) แต่พอกด **"บันทึก"/"บันทึกทั้งหมด"** แล้วรีเฟรช → chip กลับมาเขียว (dil_off=false)
- **ต้นตอ:** `rowPayload` (payroll-review) **ไม่ได้ใส่ `dil_off`** ในเพย์โหลด → `reviewSave` ทำ `dil_off: f.dil_off===true` = `undefined===true` = **false** → ทุกครั้งที่กดบันทึกแถว/ทั้งหมด เขียนทับ dil_off กลับเป็น false
- **แก้:** `rowPayload` ดึง `dil_off` ปัจจุบันจาก `DATA.rows` (ที่ chip สะท้อน + toggle อัปเดตไว้) มาส่งด้วย → กันเขียนทับ · ครอบคลุมทั้ง `saveRow` และ `saveAll`
- **ไฟล์:** `payroll-review/index.html` (static · ไม่มี SQL/backend) → อัป GitHub + Ctrl+Shift+R
- **ยืนยันสาเหตุระหว่างดีบัก:** `typeof toggleDil`='function' + onclick ผูกแล้ว + `reviewSetDil(...)` คืน `OK {ok:true}` → พิสูจน์ว่า UI/API/DB ดีหมด ปัญหาอยู่ที่ปุ่มบันทึกเขียนทับเท่านั้น

### 🛵 นิดาแก้เลขไมล์ที่พนักงานคีย์ผิดได้ (เฉพาะ HR) ✅ (29 ก.ค.)
- **ความต้องการ:** ให้นิดาแก้เลขไมล์ (odometer) ที่ไรเดอร์บันทึกผิดได้ · แก้ได้เฉพาะ HR
- **"เฉพาะ HR" ได้เองโดยธรรมชาติ:** `hr-assistant` บังคับ `hr_check_password` ทุกครั้ง → พนักงาน/ผจก. (ใช้ mgr_pin คนละช่องทาง) เรียกเครื่องมือนิดาไม่ได้อยู่แล้ว
- **เพิ่ม tool `edit_odometer`** ครบ 4 จุดใน `supabase/functions/hr-assistant/index.ts`: ACTIONS set · actionSummary (กล่องยืนยัน) · DECLS (ประกาศให้ Gemini) · runAction handler
- **Logic:** รับ `emp_id + log_date + new_odo (+ phase start/end)` → หาแถวใน `rider_odometer` (ถ้าวันนั้นมี 2 phase ต้องระบุ · ไม่พบ = แจ้ง) → `update odo` → อัปเดต `rider_vehicles.odo_last` ให้ตรง (ค่าสูงสุดของรถ) → ลง activity_log "แก้เลขไมล์ (นิดา)" · ยืนยัน 2 จังหวะเหมือน action อื่น
- **ตรวจ:** esbuild transform ผ่าน (syntax valid)
- **⚠️ Deploy:** `supabase functions deploy hr-assistant --no-verify-jwt` (Edge Function — ไฟล์ static ไม่เกี่ยว)

### 📝 ป้ายใบลา "ผู้จัดการ"→"HR" + ไรเดอร์เห็นสถานะเบิกน้ำมัน ✅ (29 ก.ค.)
- **ป้ายใบลา:** เปลี่ยน "หมายเหตุจากผู้จัดการ" → **"หมายเหตุจาก HR"** ในสถานะใบลา (ใบลาอนุมัติโดย HR) · ไฟล์ `index.html` + `employee/index.html` · (หมายเหตุ: `handover/index.html` ยังมีคำนี้ในบริบทตรวจงาน — ไม่แตะ)
- **ไรเดอร์เห็นสถานะเบิกน้ำมัน:** เดิมมีแต่ปุ่มส่งเบิก ไม่มีที่ดูสถานะ · เพิ่มปุ่ม **"สถานะ"** ข้างปุ่มเบิกน้ำมัน → เปิด modal `openFuelMine` แสดงรายการเบิกน้ำมันของตัวเอง + สถานะ (รออนุมัติ/อนุมัติ/ไม่อนุมัติ/เข้ารอบเงินเดือน) + หมายเหตุจาก HR ถ้าถูกตีกลับ · ใช้ `riderFuelMyList` (มี+export อยู่แล้ว ไม่ต้องแตะ backend) · ไฟล์ `employee/index.html`
- **Deploy:** อัป `index.html` + `employee/index.html` ขึ้น GitHub → Ctrl+Shift+R (ไม่มี SQL/Edge)

### 🏍️ ปรับปรุงหน้าไรเดอร์ฝั่ง HR (3 จุด) ✅ (30 ก.ค.)
- **1) ทะเบียนรถบอกชื่อคนขับ:** เดิมการ์ดรถไม่บอกว่าเป็นของใคร · `hrRiderVehiclesList` แนบ `emp_name` (join employees) + การ์ดโชว์ "🧍 ชื่อคนขับ" (หรือ "ยังไม่ผูกคนขับ (รถกลาง)") · ไฟล์ `shared/hr-api.js` + `hr/index.html`
- **2) แก้บั๊กธง ⚠ ในประวัติเลขไมล์:** เดิม sort ด้วยสตริง `log_date+phase` → `'end' < 'start'` ตามตัวอักษร ทำให้ "ก่อนงาน" ของทุกวันถูกธง ⚠ "ต่ำกว่าครั้งก่อน" ผิด (แก้ค่ายังไงก็ไม่หาย เพราะไม่เกี่ยวกับค่า) · **แก้:** เรียงให้ start(ก่อนงาน) มาก่อน end(หลังงาน) เสมอ · ทดสอบด้วยข้อมูลจริง: เดิมธงผิด 2 จุด → ใหม่ไม่มีธง (ไมล์เพิ่มตลอด) ✓ · ไฟล์ `hr/index.html` (openRdOdo)
- **3) กดแถวสถิติระยะทางดูรายละเอียดได้:** เพิ่ม `openRdStatDetail` — คลิกแถวไรเดอร์ → modal สรุป (ระยะทาง/ค่าน้ำมัน/฿ต่อกม./ค่าซ่อม + flags) + รายการรถที่ผูก + ปุ่ม "📍 ประวัติเลขไมล์" (เปิด openRdOdo) · ช่วยไล่ดูว่าทำไมระยะทาง=0 (ไม่มีรถผูก/ไม่ได้บันทึกไมล์) · ไฟล์ `hr/index.html`
- **4) กดดูตำแหน่งที่บันทึกเลขไมล์ (GPS):** ช่อง GPS ในประวัติเลขไมล์ เดิมโชว์แค่ ✓/✕ · เพิ่มลิงก์เปิด Google Maps ที่พิกัดจริง (`gps_lat/gps_lng` เก็บอยู่แล้ว) — โดยเฉพาะกรณี ✕ นอกรัศมี HR กดดูได้ว่าบันทึกมาจากที่ไหน · ไฟล์ `hr/index.html` (openRdOdo)
- **Deploy:** อัป `shared/hr-api.js` + `hr/index.html` ขึ้น GitHub → Ctrl+Shift+R (ไม่มี SQL/Edge)

### ✏️ HR แก้/คีย์เลขไมล์ย้อนหลังได้ในหน้าเว็บ + บันทึกผู้แก้ ✅ (30 ก.ค.)
- **ปุ่ม ✏️ แก้รายรายการ:** ในประวัติเลขไมล์ (openRdOdo) แต่ละแถวมีปุ่ม ✏️ → กรอกเลขไมล์ใหม่ → บันทึก (กรณีพนักงานคีย์ผิด)
- **ปุ่ม ➕ เพิ่มเลขไมล์ย้อนหลัง:** ฟอร์ม วันที่/ช่วง(ก่อน-หลังงาน)/เลขไมล์ → คีย์แทนพนักงานที่ลืม · ถ้ามีวัน+ช่วงนั้นแล้วเขียนทับ (upsert)
- **บันทึกผู้แก้:** ทั้ง 2 อย่างเก็บ `edited_by` (= "สำนักงาน (HR)" หรือ "ผจก. <สาขา>" ตามผู้ทำ · เพิ่มย้อนหลังต่อท้าย "· คีย์ย้อนหลัง") + `edited_at` + ลง activity_log · แถวที่ถูกแก้โชว์ป้าย "✏️ <ผู้แก้>" ใต้เลขไมล์ · อัปเดต `rider_vehicles.odo_last` ให้ตรงอัตโนมัติ
- **ไฟล์:** `supabase/rider_odo_hr_edit.sql` (ใหม่ — คอลัมน์ edited_by/edited_at) + `shared/hr-api.js` (`hrRiderOdoEdit`/`hrRiderOdoAdd` + dispatch `rider_odo_edit`/`rider_odo_add` + `_riderSyncOdoLast`) + `hr/index.html` (`rdOdoEdit`/`rdOdoAdd` + ปุ่ม/คอลัมน์)
- **ตรวจ:** node --check + รัน logic ผ่าน (edit/add/validation)
- **⚠️ Deploy:** รัน `supabase/rider_odo_hr_edit.sql` + อัป `shared/hr-api.js`+`hr/index.html` → Ctrl+Shift+R · (นิดาก็มี tool `edit_odometer` อยู่แล้วจากรอบก่อน — 2 ทางเลือก)

### 📅 ระยะทางรายวันไรเดอร์ (จับเลขไมล์ผิดปกติ) ✅ (30 ก.ค.)
- **ที่มา:** ตรวจพบระยะทางมอ'ไซค์ผิดปกติ (พนักงานวิ่งวันละ ~700 กม. — สูงเกินจริงสำหรับส่งของในพื้นที่) · หน้าเดิม "ระยะทางปีนี้" ใช้ max−min รวมทุกอย่างเป็นก้อนเดียว เลยกลบความผิดปกติรายวัน
- **วิเคราะห์:** ระยะทางต่อวัน = เลขไมล์**หลังงาน − ก่อนงาน** ของวันเดียวกัน (ข้อมูลจริง: 27 ก.ค.=715, 28 ก.ค.=536 กม. → ผิดปกติชัดเจน)
- **A) ปุ่ม "📅 ระยะทางรายวัน" ในหน้าประวัติเลขไมล์ (ต่อรถ):** คำนวณจาก rows ที่โหลดมาแล้ว (ไม่เรียก API ใหม่) · โชว์ ก่อน/หลัง/ระยะวันนั้น + ธง
- **B) หน้าใหม่ sub-tab "📅 ระยะทางรายวัน" (จัดกลุ่มตามคนขับ):** เลือกเดือน → แต่ละไรเดอร์เห็นระยะทางรายวัน + ยอดรวม + ธงวันผิดปกติ · API ใหม่ `rider_daily_distance` (`hrRiderDailyDistance`)
- **เกณฑ์ธง:** ระยะ **> 200 กม./วัน** · เลขไมล์ถอยหลัง · ข้อมูลไม่ครบ (มีแค่ก่อน/หลัง) · แถวที่ HR เคยแก้โชว์ ✏️
- **ตรวจ:** รัน logic ด้วยข้อมูลจริง — จับ 715/536 กม. ติดธงถูกต้อง ✓
- **ไฟล์:** `shared/hr-api.js` (`hrRiderDailyDistance` + dispatch) + `hr/index.html` (sub-tab + `loadRdDaily` + `rdOdoDaily` + ปุ่ม) · static ไม่มี SQL/Edge · Deploy: อัป 2 ไฟล์ → Ctrl+Shift+R

### 🌙 ธง "จ่ายค่ากะดึก" ต่อกะ (แทนการเดาจากเวลาเข้า-ออก) ✅
- **ปัญหา:** ระบบเดิมเดา "กะดึก" จากเวลา (เลิก ≤ เข้า = ข้ามคืน) → กะที่เลิกดึกแต่ไม่ใช่กะดึก (เช่น M16 = 16:00–02:00) โดนนับเป็นกะดึกและได้ค่ากะทั้งที่เป็นกะบ่าย (เคส ส้ม สาขาตลาดหล่มสัก)
- **แก้:** คอลัมน์ `shifts.night_allowance` (boolean) · HR ติ๊กเองในหน้า "ตั้งค่ากะ" — ติ๊กเฉพาะกะดึกจริง (เช่น N)
- **ตรรกะ nightSet (3 จุด):** ถ้ามีกะใดตั้งธง `night_allowance=true` แล้ว → ใช้ธงล้วน · ถ้ายังไม่มีกะไหนตั้งเลย → fallback ใช้กฎเวลาเดิม (กันค่ากะหายทันทีก่อนตั้งค่า)
- **จุดที่แก้:** `hr-api.js` hrPayrollRun (select+nightSet+hrShiftSave) · `supabase.js` reviewLoad + reviewShiftControllers · NIDA `night_allowance_summary`
- **UI:** เช็กบ็อกซ์ `sh_nightallow` "🌙 จ่ายค่ากะดึกสำหรับกะนี้" ในฟอร์มแก้กะ (hr/index.html)
- **ไฟล์:** `supabase/shifts_night_allowance.sql` (รันครั้งเดียว — เพิ่มคอลัมน์ + N=true) + `shared/hr-api.js` + `shared/supabase.js` + `hr/index.html` + `supabase/functions/hr-assistant/index.ts`
- **นิดาอธิบายได้:** เพิ่มคำอธิบาย "ค่ากะดึกคืออะไร + กะไหนได้ (ธง night_allowance)" ใน SYS ของ hr-assistant → ถามนิดาแล้วตอบได้ (12 ส.ค.)
- **⚠️ Deploy:** ไฟล์สแตติก (Ctrl+Shift+R) + รัน SQL + `supabase functions deploy hr-assistant`

### 🤖 อัปเดตนิดา: ย้ายรหัส/สาขา + เปิด/ปิดเบี้ยวินัย + dil_off ใน read ✅
- `transfer_emp` (เรียก RPC ย้ายรหัสทุกตาราง) · `set_diligence` (เปิด/ปิดเบี้ยวินัยรายคน/รอบ → upsert payroll_review.dil_off) · app_data payroll_review เพิ่ม dil_off · ลง ACTIONS/actionSummary/runAction/DECLS/SYS · esbuild ผ่าน
- **⚠️ Deploy แยก:** `supabase functions deploy hr-assistant`

### ✏️ ปรับคำ "หัก" + แก้พิมพ์ข้อความบนมือถือ ✅
- **คำในดรอปดาวน์รอบ:** เบิกล่วงหน้า (`_akRoundOpts`) + ค่าน้ำมัน (`_fuelRoundOpts`/`_fuelPayLabel`) เปลี่ยน "จ่าย 1 …" → **"หัก 1 …"** (เพราะเป็นรอบที่หัก) · หน้าเงินเดือน (`_plPayText`) คง "จ่าย" ไว้ (วันจ่ายจริงของรอบ)
- **พิมพ์ข้อความบนมือถือไม่ได้:** ช่องรหัสเข้าหน้าตรวจ (`#pw`) เอา `inputmode="numeric"` ออก (รหัสมีตัวอักษรได้) + ช่อง "ระบุว่าหักอะไร"/"หมายเหตุ" ใส่ `inputmode="text"` บังคับแป้นพิมพ์ตัวอักษรบนมือถือ
- **ไฟล์:** `hr/index.html` + `payroll-review/index.html`

### 🎖️ สวิตช์เปิด/ปิด "เบี้ยวินัย" รายคน (พนักงานใหม่ยังไม่ผ่านประเมิน) ✅
- **ปัญหา:** ระบบบวกเบี้ยวินัยอัตโนมัติทุกคนที่เข้าเกณฑ์ · พนักงานใหม่ที่ยังไม่ผ่านประเมินไม่ควรได้
- **แก้:** คอลัมน์ `payroll_review.dil_off` (ต่อรอบ) · `_payrollCompute`: `if(...&& !stats.dil_off)` → dil_off=true บังคับ diligence=0 · hrPayrollRun ส่ง `rv.dil_off` เข้า stats
- **หน้าตรวจ:** chip "เบี้ยวินัย" กลายเป็น**สวิตช์กดเปิด/ปิด** (ปิด=ส้ม 🚫) · `reviewSetDil` (supabase.js) อัปเฉพาะ dil_off ไม่ทับ override อื่น · `toggleDil` + confirm
- **⚠️ แก้เพิ่ม:** "เบี้ยวินัย" ในระบบนี้ = **โบนัสตามแบนด์คะแนน (`bonus`)** ไม่ใช่ `diligence` · dil_off จึงต้องตัด **ทั้ง bonus + diligence** (`const bonus = stats.dil_off ? 0 : ...`) ไม่งั้นหน้าเงินเดือนยังขึ้นโบนัส 500 อยู่
- **ไฟล์:** `supabase/payroll_review_dil_off.sql` (รันครั้งเดียว) + `shared/hr-api.js` + `shared/supabase.js` + `payroll-review/index.html`

### 🔁 เครื่องมือ "ย้ายรหัส/ย้ายสาขา" (retarget emp_id อัตโนมัติ) ✅
- **ปัญหาราก:** emp_id ผูกรหัสสาขา → ย้ายสาขาต้องเปลี่ยน id → orphan ทุกตาราง
- **แก้:** Postgres function `transfer_emp(old,new,branch)` — auto-discover *ทุกคอลัมน์ emp_id* (information_schema) แล้ว UPDATE old→new ในทรานแซกชันเดียว + กันรหัสใหม่ซ้ำ + คืนสรุปจำนวนแถวต่อตาราง · `supabase/transfer_emp.sql` (ติดตั้งครั้งเดียว)
- **API:** `hr_emp_transfer` (hr-api.js) เรียก `sb().rpc('transfer_emp',...)` + validate
- **UI:** ปุ่ม "🔁 ย้ายรหัส/สาขา" ในโมดัลรายละเอียดพนักงาน → ใส่รหัสใหม่+สาขาใหม่ → ยืนยัน · ซ่อนในโหมด ผจก.
- **ไฟล์:** `supabase/transfer_emp.sql` (รันครั้งเดียว) + `shared/hr-api.js` + `hr/index.html`

### 👻 แก้ "แถวผี" ตอนพนักงานเปลี่ยน emp_id (ย้ายสาขา) ✅
- **เคส:** พิศมัย ย้ายสาขา → emp_id เปลี่ยน `0657355`→`1077584` แต่ใบเบิก/น้ำมัน/payroll_items ยังผูก id เก่า → id เก่ากลายเป็น "ผี" (ไม่มีใน employees) → เงินเดือนดึงแถวเก่าค้างมาโชว์ (3,000+200 ทุกรอบ) ทั้งที่คำนวณสดถูก (debug ยืนยัน เบิก=0/น้ำมัน=100 ในรอบถัดไป)
- **แก้ข้อมูล (SQL):** ย้าย advance_requests/rider_fuel_claims/rider_claims/payroll_installments/rider_vehicles/rider_odometer จาก id เก่า→ใหม่ + อัปสาขา · ลบ payroll_review/payroll_items ของ id ผี
- **แก้โค้ด (กันเกิดอีก):** `hrPayrollRun` หลัง upsert → **ลบแถว payroll_items ของคนที่ไม่อยู่ในรอบนี้แล้ว** (`.not('emp_id','in',keepIds)`) · ลบ debug `_dbg` ออก
- **ไฟล์:** `shared/hr-api.js` + `hr/index.html`
- **⚠️ ตรวจ orphan อื่น:** SQL หา emp_id ในใบเบิก/น้ำมัน ที่ไม่มีใน employees (เผื่อมีคนอื่นย้ายสาขาแบบเดียวกัน)

### 🎯 หักตามรอบ: รอบปัจจุบันตกทอดได้ · รอบอนาคตเป๊ะ (ไม่ทบยอดข้ามรอบ) ✅
- **ปัญหา:** ตรรกะ `≤ runMonth` เดิมทำให้รอบอนาคตทบยอดของรอบก่อนมาโชว์ (ผิดหลักบัญชี — ทั้ง 2 รอบเหมือนกัน)
- **แก้:** `carry = runMonth === bkkToday().slice(0,7)` (รอบที่กำลังจะจ่าย) → รอบปัจจุบันใช้ `≤` (ตกทอดใบเก่าที่ยังไม่หัก) · รอบอนาคตใช้ `=` (เฉพาะใบของรอบนั้น) · ใช้ทั้ง fuelByEmp/advByEmp + finalize (fuel/advance) · ทดสอบตรรกะ 5/5
- **⚠️ อาการ "เหมือนเดิม" หลัง push = เบราว์เซอร์แคช hr-api.js เก่า** → ต้อง **Ctrl+Shift+R** (hard refresh) ทุกครั้งหลัง deploy JS

### 🔎 หน้าค่าน้ำมัน: โชว์วันที่คีย์ + รอบที่จะหัก + สถานะหัก + โมดัลเงินเดือนโชว์รายการอัตโนมัติ ✅
- **โมดัลแก้รายคน (เงินเดือน):** เพิ่มบล็อก "🔒 รายการอัตโนมัติ (อ่านอย่างเดียว)" โชว์ ค่าน้ำมัน/ซ่อม/ค่ากะ/ผ่อน/เบิกล่วงหน้า ที่เดิมถูกซ่อน → เห็นว่า "หักอื่น" ประกอบด้วยอะไร
- **หน้ารายการค่าน้ำมัน:** เพิ่มคอลัมน์ **วันที่คีย์** (created_at เวลาไทย) + **รอบที่จะหัก** (cycle_month → "จ่าย 1 <เดือน>") + **สถานะหัก** (✓หักแล้ว/ยังไม่หัก) → ตรวจสอบได้ว่าใบไหนไปรอบไหน
- **ไฟล์:** `hr/index.html` · node --check ผ่าน
- **⚠️ สาเหตุที่ทั้ง 2 รอบโชว์เท่ากัน = ยังไม่ deploy `shared/hr-api.js`** (ตัวคำนวณหักตามรอบ) — ต้อง push ไฟล์นี้ด้วย ไม่งั้นยังใช้ logic เก่า (หักทุกใบที่ยังไม่หักทุกรอบ)

### 💰 แก้รอบหักเบิก (cycle_month) + คำเรียกรอบอิงวันจ่าย ✅
- **ต้นตอ:** `cycle_month` เดิม = เดือนปฏิทินตอนกดเบิก → ใบที่กดวันที่ 21–31 เพี้ยนไป 1 เดือน · และการหักไม่กรองตามรอบ (หักทุกใบตอนปิดรอบ → ใบรอบถัดไปโดนหักเร็ว)
- **แก้ (หลักหักตามรอบ):** เพิ่ม `cycleMonthOf()` (hr-api) / `_cycleMonth()` (supabase) = เดือนสิ้นรอบ 21–20 · ใช้กับ riderFuelSubmit/hrFuelCreate/hrAdvanceCreate/submitAdvance/quota · หักเฉพาะใบ `cycle_month ≤ เดือนสิ้นรอบที่ปิด` ทั้งตอนคำนวณ (fuelByEmp/advByEmp) และตอนปิดรอบ (finalize) · ทดสอบ cycleMonthOf 9/9 + ตรรกะหัก 4/4 ✅
- **คำเรียกรอบ (payday-oriented):** หน้าเงินเดือน (`pl_which`) + หน้าตรวจ (`which`) → "รอบปัจจุบัน · จ่าย 1 <เดือน>" = รอบที่กำลังจะจ่าย, "รอบถัดไป/รอบก่อน · จ่าย…" · dropdown สร้างสดตามวันที่ (past20 → paying=code'previous') · **ไม่แตะ backend cycle** (ใช้ค่า current/previous เดิม แค่เปลี่ยนป้าย+ดีฟอลต์) · ทดสอบป้าย 3 วันที่ผ่าน — รอบปัจจุบัน=รอบจ่ายถัดไปเสมอ
- **⚠️ ข้อมูลเก่า:** ใบที่กดวันที่ 21–31 *ก่อน*แก้นี้ ยังมี cycle_month แบบเก่า (อาจหักเร็ว 1 รอบ) — ถ้ามีข้อมูลจริง แจ้งได้ ผมทำ SQL re-tag จาก created_at ให้ (ยังไม่ทำอัตโนมัติ กันไปชนใบ backfill ที่ตั้ง cycle_month เอง)
- **HR คีย์เบิกล่วงหน้า เลือกรอบได้:** โมดัล "คีย์เบิกให้พนักงาน" เพิ่ม dropdown "หักในรอบ" (รอบปัจจุบัน/ถัดไป · วันจ่าย · ดีฟอลต์=รอบจ่ายถัดไป) → ส่ง cycle_month · NIDA advance_key ดีฟอลต์ = เดือนสิ้นรอบ (cycle21().end)
- **ไฟล์:** `shared/hr-api.js` + `shared/supabase.js` + `hr/index.html` + `payroll-review/index.html` + edge `hr-assistant` · **Deploy:** push 4 ไฟล์ + `supabase functions deploy hr-assistant`
- **ข้อมูลจริงเก่า:** SQL `_แปลง-cycle_month-ข้อมูลเก่า.sql` (แตะเฉพาะ device='employee' + undeducted) · ใบ HR-keyed เก่าที่ผิด ต้องแก้รายตัว

### 🚫 ยกเลิก "สิทธิ์เลือกกะก่อน" ✅
- ข้อความนี้ไม่ได้อยู่ในโค้ด — เป็นข้อมูลใน **`score_bands.label`** (ป้ายช่วงคะแนน) ที่ถูกดึงไปแสดงทุกจุด (หน้าตรวจ/พนักงาน/สลิป/ใบเตือน) · ไม่มีฟังก์ชันเลือกกะจริง (เป็นข้อความจูงใจ)
- **แก้:** SQL `supabase/_ยกเลิกสิทธิเลือกกะก่อน.sql` — regexp ตัดช่วงข้อความที่มี "เลือกกะก่อน" ออกจาก label + ล้างตัวคั่นค้าง + กันป้ายว่าง · **รันใน Supabase SQL editor** (ไม่ต้อง push ไฟล์) · band_label อ่านสด → หายทุกหน้าทันที

### 📊 สถิติระยะทางไรเดอร์ + เครื่องมือนิดาเพิ่ม + เบอร์โทรในรูปตารางงาน ✅
- **หน้าสถิติระยะทาง:** แท็บ 🏍️ ไรเดอร์ → ซับแท็บใหม่ **"📊 สถิติระยะทาง"** · ตารางเทียบทุกคน: ระยะทาง(กม.) / ค่าน้ำมัน / **฿ต่อกม.** / ค่าซ่อม + ไฮไลต์ **ไม่สอดคล้อง** (เบิกแต่ไม่มีเลขไมล์, ฿/กม.>3) · API `rider_stats` (hr-api.js) + `loadRdStats` (hr/index.html) · เลือกปีได้
- **เครื่องมือนิดาเพิ่ม (hr-assistant edge fn):**
  - action (ยืนยันก่อน): `rider_fuel_review` (อนุมัติ/ปฏิเสธเบิกน้ำมัน), `rider_claim_review` (อนุมัติ/ปฏิเสธเบิกซ่อม +ยอด), `advance_key` (คีย์เบิกล่วงหน้า +อนุมัติทันที), `installment_create` (สร้างแผนผ่อน), `installment_discount` (ลดหนี้ค้าง)
  - read: `night_allowance_summary` (ค่ากะดึกต่อคน: วัน15฿ vs วัน10฿ +ยอด, logic shift_leads), `rider_mileage_check` (เลขไมล์↔เบิกน้ำมัน/ซ่อม ไม่สอดคล้อง)
  - ลง ACTIONS/actionSummary/runAction/DECLS/TOOLS/SYS ครบ · esbuild ผ่าน
- **เบอร์โทรในรูปตารางงาน (แชร์/บันทึกรูป):** `buildScheduleCanvas` เพิ่มบรรทัด "โทร <เบอร์>" ใต้ชื่อ (ขยาย rowH 58/nameW 210) + `buildScheduleText` ใส่ ☎เบอร์ · ไฟล์ `hr/index.html` (คนละจุดกับตารางจัดกะแบบแก้ไขที่ทำก่อนหน้า)
- **⚠️ Deploy:** static push `hr/index.html` + `shared/hr-api.js` · **edge fn แยก:** `supabase functions deploy hr-assistant`

### 🧹 จัดหน้า + ต่อข้อมูลนิดา + เบอร์โทร + รายละเอียดพนักงาน ✅
- **หน้าตรวจ (จัดตาราง):** `table-layout:fixed` + `<colgroup>` กำหนดความกว้าง + `vertical-align:top` (แก้ช่องกรอกลอยกลาง/เหลื่อม) + **ตรึงหัวข้อ** (`thead th{position:sticky;top:60px}`) · ไฟล์ `payroll-review/index.html`
- **นิดา (hr-assistant edge fn):** ต่อตาราง/โมดูลใหม่ — WRITE_TABLES + `db_update`/`db_delete` เพิ่ม shift_leads, shift_controllers, holidays, payroll_review, payroll_installments(+charges), rider_claims, rider_fuel_claims, rider_vehicles, rider_items, rider_odometer, rider_fuel_config · `app_data` เพิ่มโมดูล rider_claims / rider_fuel / rider_vehicles / payroll_review / installments · อัปคำอธิบายเครื่องมือ · **นิดาอ่านได้ทุกตารางอยู่แล้ว (run_sql/app_data) + แก้/ลบพร้อมยืนยันก่อนทำ** · esbuild ผ่าน · **⚠️ Deploy แยก: `supabase functions deploy hr-assistant`**
- **เบอร์โทรในตารางกะ:** `hrSchedWeek` เพิ่ม select `phone` · `renderSchedule` แสดง `📞 tel:` ใต้ชื่อ (โทรตามง่าย) · ไฟล์ `shared/hr-api.js` + `hr/index.html`
- **รายละเอียดพนักงานไม่ครบ:** โมดัลรายละเอียด (openView) ไม่เคยโชว์ **อีเมล** → เพิ่ม `v_email` · หน้ารออนุมัติ (loadSubmissions) เพิ่ม **ชื่อ-สกุล + อีเมล** · ไฟล์ `hr/index.html`
- **⚠️ Deploy:** static push `payroll-review/index.html` + `hr/index.html` + `shared/hr-api.js` + `shared/supabase.js` · **edge fn แยก:** `hr-assistant`

### 🌙 ค่ากะดึก: แก้ให้จับ "หัวหน้าผลัด" จากตารางที่ถูก (shift_leads) ✅
- **สาเหตุจริง (ยืนยันจากข้อมูล viewer):** เดิมจับผู้คุมผลัดจาก `shift_controllers` (1 คน/สาขา/วัน **ไม่แยกกะ**) หลายคนที่เป็น controller เป็นหัวหน้าผลัดกะกลางวัน ไม่มีใบลงเวลากะดึก → จับคู่ไม่ติด ทุกคนเลยได้ 10
- **แก้:** เปลี่ยนไปจับจาก **`shift_leads`** (หัวหน้าผลัด **แยกตามกะ** มี shift_id) จับคู่ (สาขา|วัน|**กลุ่มกะ**) · normalize shift_id→main_shift group ทั้งสองฝั่ง (attendance + shift_leads) เพราะ shift_leads เก็บทั้งดิบ/กลุ่ม
- **จุดที่แก้:** `hrPayrollRun` (hr-api.js) + `reviewLoad` (supabase.js) — เพิ่ม grpOf, leadMap keyed by group
- **viewer ปุ่ม "👥 ผู้คุมผลัด":** อัปเป็นรายคืน (สาขา/กะ/หัวหน้าผลัด/สถานะ ✓ได้15·⚠หน.ผลัดไม่ลงเวลา·✗ไม่มีหน.ผลัด + รายชื่อในผลัด) · สรุปคืนกะดึก/คืนที่ได้ 15
- **ไฟล์:** `shared/hr-api.js` + `shared/supabase.js` + `payroll-review/index.html` · node --check ผ่าน · ไม่มี SQL
- **⚠️ Deploy + ให้ผู้ใช้เปิด viewer ยืนยันว่าขึ้น ✓ ได้ 15 แล้ว**
- **API:** `reviewShiftControllers(which)` (supabase.js) → rows/gaps/night_shift_days/controller_days · **UI:** `openCtrl` โมดัล `ctrlOv`
- **ไฟล์:** `shared/supabase.js` + `payroll-review/index.html` · node --check ผ่าน · ไม่มี SQL
- **⚠️ ยังไม่ได้ทำ (ถ้าต้องการ):** ให้ HR/ผจก. "กำหนดผู้คุมผลัดย้อนหลัง" ต่อคืน เพื่อให้เรท 15 มีผลกับระบบเก่าที่ไม่มีคนกดปุ่ม
- **⚠️ Deploy:** push `shared/supabase.js` + `payroll-review/index.html`

### 🔔 แจ้งเตือน HR: เบิกน้ำมัน/เบิกซ่อม รออนุมัติ ✅
- **การ์ดบนแดชบอร์ด HR:** ถ้ามีคำขอ status='submitted' → การ์ดสีส้ม "มีคำขอไรเดอร์รออนุมัติ N รายการ" (⛽ เบิกน้ำมัน X · 🔧 เบิกซ่อม Y + ยอด) · แตะ → เด้งเข้าแท็บไรเดอร์ซับที่เกี่ยว + โชว์รายชื่อล่าสุด
- **badge บนเมนู "🏍️ ไรเดอร์":** จุดแดงนับรวมค้าง · อัปเดตสดหลังอนุมัติ (loadRiderPending หลัง review)
- **API:** `rider_pending` (นับ+ตัวอย่าง fuel/maint submitted) · **UI:** `loadRiderPending`+`updateRiderBadge` เรียกตอนเข้าแดชบอร์ด
- **ไฟล์:** `shared/hr-api.js` (hrRiderPending) · `hr/index.html` (การ์ด+badge) · node --check ผ่าน · ไม่มี SQL
- **⚠️ Deploy:** push `shared/hr-api.js` + `hr/index.html`

### 🛵 ช่อง Delivery (รายได้เพิ่ม) — หน้าตรวจ + เงินเดือน ✅
- เก็บที่ `payroll_review.delivery` (แหล่งเดียว) → บวกเข้ารายได้เป็นรายการเพิ่ม **"Delivery"** (แสดงรวมในสลิปช่อง "เงินได้อื่นๆ")
- **หน้าตรวจ:** คอลัมน์ "Delivery" (ผจก.กรอก) · **หน้าเงินเดือน:** ช่อง "🛵 Delivery" ในโมดัลแก้รายคน (ดินสอ) → บันทึกลง payroll_review เดียวกัน + คิดใหม่ทันที
- **แยกบรรทัดอัตโนมัติ/มือ:** ตัวแก้บรรทัดในโมดัลแสดงเฉพาะบรรทัดที่ HR ใส่เอง (ไม่มี `src`) · บรรทัดอัตโนมัติ (Delivery/ค่ากะ/ผ่อน/น้ำมัน/ซ่อม) คงไว้ตอนบันทึก (merge auto+manual)
- **ไฟล์:** `supabase/payroll_delivery.sql` · `shared/hr-api.js` (hrPayrollRun+ItemSave) · `shared/supabase.js` (reviewLoad/Save) · `hr/index.html` (โมดัล) · `payroll-review/index.html` (คอลัมน์)
- **ตรวจ:** node --check ผ่าน · logic Delivery/กันซ้ำ/คงบรรทัด 8/8 เคส
- **⚠️ Deploy:** (1) รัน `supabase/payroll_delivery.sql` (2) push `shared/hr-api.js`,`shared/supabase.js`,`hr/index.html`,`payroll-review/index.html`

### 🔧 ค่าซ่อมบำรุงรถ → รายได้ไรเดอร์ (จ่ายพร้อมเงินเดือน) ✅
> ต่างจากค่าน้ำมัน (หัก) — ค่าซ่อมคือบริษัทจ่ายคืน = **รายได้** เข้ารายการเพิ่ม
- **ดึงอัตโนมัติ:** `hrPayrollRun` รวมใบซ่อม `rider_claims` status='approved' (ยังไม่จ่าย) → รายการเพิ่ม **"ค่าซ่อมบำรุงรถ"** src='maint' (เข้ารวมใน "เงินได้อื่นๆ" บนสลิป) · กันซ้ำเมื่อคำนวณซ้ำ
- **ปิดรอบ = จ่ายทันที:** `hrPayrollFinalize` เปลี่ยนใบซ่อม approved → **status='paid'** + paid_at + payout_method='payroll' + payroll_ref · **เปิดรอบใหม่** คืนเป็น approved
- **ไฟล์:** `shared/hr-api.js` (hrPayrollRun + finalize + reopen) · node --check ผ่าน · ปุ่มจ่ายมือ (hrRiderClaimPay) ยังใช้ได้เผื่อจ่ายนอกรอบ
- **⚠️ Deploy:** push `shared/hr-api.js` (ไม่มี SQL — rider_claims มีคอลัมน์ครบแล้ว)

### ⚡ แก้สแกนใบหน้าลงเวลาช้า/หมุนโหลดนาน ✅
- โหลดโมเดล 3 ตัว **พร้อมกัน** (Promise.all) แทนทีละตัว + บังคับ backend WebGL พร้อมก่อน
- ลด `inputSize` TinyFaceDetector: getDescriptor 416→**320** · detectLoop 416→**224** → inference เร็วขึ้นมากบนมือถือ
- **warm-up** full pipeline หลังโหลด (คอมไพล์ shader ล่วงหน้า) → สแกนจริงครั้งแรกไม่ค้าง
- detectLoop 700→900ms (ลดโหลด CPU ต่อเนื่อง)
- ไฟล์: `employee/index.html` (loadModels/getDescriptor/detectLoop/warmUpFace) · node --check ผ่าน
- หมายเหตุ: โหลดโมเดลครั้งแรก ~7MB จาก CDN (เบราว์เซอร์แคชหลังจากนั้น) — ถ้าช้า*ทุกครั้ง* = เบราว์เซอร์ไม่แคช (โหมดไม่ระบุตัวตน/ล้างแคช)
- **⚠️ Deploy:** push `employee/index.html`

> ⚠️ **ธรรมเนียมสำคัญ:** ทุกครั้งที่แก้ไข/เปลี่ยนแปลงโค้ดหรือฟีเจอร์ ให้ **อัปเดตไฟล์นี้เสมอ** (สั่งโดยผู้ใช้ 24 ก.ค.) — บันทึก: ไฟล์ที่แตะ · สิ่งที่ทำ · สิ่งที่ต้อง deploy

### 💰 Payroll: แก้ "วันทำงาน" รายคน (override) ✅ — สำหรับช่วงเพิ่งเริ่มใช้ระบบ
> ปัญหา: เริ่มลงเวลา 21/6/69 → รอบ 21/6–20/7 ข้อมูลลงเวลายังไม่ครบ บางคนได้ 0 วัน
- **วิธีแก้:** ในหน้าเงินเดือน กดดินสอ (แก้รายคน) → มีช่องใหม่ **"วันทำงาน (แก้เอง)"** (เฉพาะรายวัน) · กรอกวันจริง → คิดฐาน = วัน × อัตราใหม่ + ประกันสังคมใหม่ · เว้นว่าง = ใช้ตามลงเวลา
- **คงทน:** `payroll_items.days_override` เก็บค่าไว้ → กด "คำนวณ" ซ้ำก็ไม่หาย (`hrPayrollRun` เช็ค override ก่อน attendance)
- **ไฟล์:** `supabase/payroll_days_override.sql` (คอลัมน์ใหม่) · `shared/hr-api.js` (`hrPayrollItemSave`+`hrPayrollRun`) · `hr/index.html` (`openPlItem` ช่องวัน)
- **ตรวจ:** node --check ผ่าน · สูตร 17×349=5,933 · ปกส.296.65 ตรงกับที่ระบบคิด
- **⚠️ Deploy:** (1) รัน `supabase/payroll_days_override.sql` (2) push `shared/hr-api.js` + `hr/index.html`

### 🕒 Payroll: Backfill ลงเวลาย้อนหลัง ✅ — เติมข้อมูลจริงลงตารางลงเวลา
> ทางเลือกที่ 2 (คู่กับ days_override) · ป้อนให้ทุกระบบ: เงินเดือน/เบิกล่วงหน้า/สถิติ/รายงาน · ต่อคนเลือกใช้อย่างเดียว
- **2 โหมด:** (ก) **จากตารางเวร** — สร้างลงเวลา "มาปกติ" ทุกวันที่มีเวรในช่วง · (ข) **กรอกเอง** — เลือกคน×วัน×กะ (มีปุ่ม "เติมทุกวันในช่วง"/"เว้น อา.")
- **นับมาปกติเท่านั้น:** `late_min=0` `status=CLOSED` `source=backfill` `day_note=HR เติมย้อนหลัง` · ไม่มีรูป/GPS · **ไม่ทับแถวสแกนจริง** (upsert ignoreDuplicates)
- **ไฟล์:** `supabase/attendance_backfill.sql` (คอลัมน์ `source`) · `shared/hr-api.js` (`hrBackfillPreview`/`hrBackfillApply`/`_backfillBuild`) · `hr/index.html` (ปุ่ม "＋ เติมวันย้อนหลัง" ใน toolbar เงินเดือน + โมดัล preview/apply)
- **ตรวจ:** node --check ผ่าน · logic กันซ้ำ/สร้างวัน 8/8 เคส · แก้บั๊ก timezone ใน `bfDatesInRange` (เดิม toISOString เลื่อนวัน) → ผ่านทุก TZ
- **⚠️ Deploy:** (1) รัน `supabase/attendance_backfill.sql` (2) push `shared/hr-api.js` + `hr/index.html` · หลังเติม กด "คำนวณ" ใหม่เพื่อดึงวันเข้าเงินเดือน

### 🧾 หน้าตรวจข้อมูลเข้าเงินเดือน (Payroll Review) ✅ — ผจก.ระดับสูง ไม่เห็นยอดเงิน
> หน้าแยก `/payroll-review/` · รหัสตรวจแยกจาก HR · ตรวจ/แก้ปริมาณให้ HR คิดเงินต่อ
- **ซ่อนหมด:** เงินเดือน/สุทธิ/ยอดรวม · ประกันสังคม · ฐาน+อัตราค่าแรง — โค้ดหน้านี้ไม่มีสูตรคิดเงินเลย
- **แก้ได้:** วัน · OT(ชม.) · เงินเบิก · เบี้ยพิเศษ · หักสินค้าเสื่อม · หักอื่นๆ · หมายเหตุ (1 คน 1 บรรทัด, เว้นว่าง=ใช้ตามระบบ, โชว์ค่าลงเวลาเป็น hint)
- **เชื่อมเข้าเงินเดือน:** ค่ากรอกเก็บใน `payroll_review` → `hrPayrollRun` ดึงมา merge ตอน HR กด "คำนวณ" · **ลำดับ:** หน้าตรวจ > แก้รายคนเดิม(item) > ลงเวลา · เบี้ย/หัก ใช้ป้ายสงวน (ไม่ซ้ำเมื่อรันซ้ำ · คงรายการที่ HR ใส่เอง)
- **รหัส:** RPC `review_check_password`/`review_set_password` (แยกจาก HR/รหัสเงินเดือน) · HR ตั้งได้ที่ปุ่ม "🔑 รหัสหน้าตรวจ" ในแท็บเงินเดือน
- **ไฟล์:** `supabase/payroll_review.sql` · `shared/hr-api.js` (merge ใน hrPayrollRun) · `shared/supabase.js` (`review*` methods) · `payroll-review/index.html` (ใหม่) · `hr/index.html` (ปุ่มตั้งรหัส)
- **ตรวจ:** node --check ผ่านทุกไฟล์ · logic merge/กันซ้ำ/ลำดับความสำคัญ 16/16 เคส
- **⚠️ Deploy:** (1) รัน `supabase/payroll_review.sql` (2) push `shared/hr-api.js`+`shared/supabase.js`+`hr/index.html`+`payroll-review/index.html` (3) HR ตั้งรหัสหน้าตรวจ (ปุ่ม 🔑) (4) ผจก.ระดับสูงเปิด `/payroll-review/` · หลังกรอก HR กด "คำนวณ" เพื่อดึงเข้าเงินเดือน

#### 🔧 ปรับปรุงหน้าตรวจ (รอบ feedback ผู้ใช้) ✅
- **เงินเบิกแสดงยอดอัตโนมัติ:** `reviewLoad` ดึง `auto_advance` (advance paid+undeducted ไม่เลื่อน) → ช่องเงินเบิกโชว์ hint "ระบบเบิก X" · เว้นว่าง=ใช้ auto · กรอก=แทนที่
- **คะแนนวินัย/คน:** พอร์ต `_computeScore`+`_disciplineFromRules` มาที่ `reviewLoad` (batch) → ชิปคะแนน+ป้ายระดับต่อคน
- **สรุป ขาด/ลา/สาย ต่อคน** ในเซลล์ชื่อ (ผจก.ตรวจง่าย)
- **ชื่อจริง+ฉายา** (เดิมโชว์แค่ฉายา) · **แก้หัวตารางทับซ้อน** (เอา sticky ออก)
- **ช่องระบุ "หักอื่นๆ ว่าหักอะไร"** → คอลัมน์ `payroll_review.ded_other_note` · merge ใช้เป็น label (marker `src:'rv'` กันซ้ำทั้งของเก่า/ใหม่ · คงรายการ HR ใส่เอง · เปลี่ยน note ล้างของเก่า) — ทดสอบ 9/9
- **ปุ่ม "📅 กะ" ต่อคน:** modal ตารางกะรายวันในรอบ (มา/สาย/ขาด/ลา) · แก้ได้ "ทำเป็นมา/ขาด" (`reviewShiftDetail`/`reviewMarkDay`) · แก้เฉพาะแถว backfill ไม่แตะสแกนจริง
- **หน้า HR:** เปลี่ยนหมวดเมนู "เบิกเงิน"→**"การเงิน"** + ปุ่ม **"🔗 เปิดหน้าตรวจ"** ในแท็บเงินเดือน (เปิด `/payroll-review/` แท็บใหม่)
- **⚠️ Deploy เพิ่ม:** รัน `supabase/payroll_review.sql` ซ้ำ (มี `alter add ded_other_note`) + push `shared/supabase.js`,`shared/hr-api.js`,`hr/index.html`,`payroll-review/index.html`

#### 🔧 รอบ feedback #2 ✅
- **OT ปัดชั่วโมงเต็ม:** `reviewLoad` อ่านตั้งค่า `ot_whole_day` แล้วปัดลงต่อแถว (เหมือน `otAdj` ในเงินเดือน) → เลข OT ตรงกับที่ระบบคิดจริง (ไม่โชว์เศษถ้าตั้งปัดเต็ม)
- **จัดกะย้อนหลังครบทุกวัน:** โมดัล "📅 กะ" แสดง **ทุกวันในรอบ** (ถึงวันนี้) ไม่ใช่เฉพาะวันมีเวร → กด "ทำเป็นมา" backfill วันไหนก็ได้ (`reviewShiftDetail` สร้าง date range เต็ม)
- **ย้ายปุ่มหน้าตรวจ:** "🔗 เปิดหน้าตรวจ" + "🔑 รหัสหน้าตรวจ" ย้ายจากแท็บเงินเดือน → **แท็บเบิกเงินล่วงหน้า** (adv_reviewopen/adv_reviewpass) ตามที่ผู้ใช้ขอ
- **Deploy:** push `shared/supabase.js` + `hr/index.html` (ไม่มี SQL ใหม่)

#### 🔧 รอบ feedback #3 ✅
- **ลิงก์กลับ:** หน้าตรวจเพิ่มปุ่ม "← หน้า HR" (ลิงก์ `../hr/`) มุมซ้ายบน (เดิมไม่มีทางกลับ)
- **ป้ายเบี้ยวินัย ได้/ไม่ได้:** `reviewLoad` อ่านเกณฑ์ `payroll_config.diligence` (require_no_absent + allow_late_count) → คำนวณ `dil_ok` ต่อคน → ชิปเขียว "เบี้ยวินัย ✓ ได้" / แดง "✗ ไม่ได้" (ตามขาด/สาย) · ผจก.เห็นได้ทันทีว่าใครได้
- **ลิงก์เมนู:** "ตรวจวันทำงาน (ผจก.)" เป็นรายการในดรอปดาวน์ "การเงิน" (buildGroupedNav รองรับ `links`)
- **Deploy:** push `shared/supabase.js` + `payroll-review/index.html` + `hr/index.html`

#### 🐞 แก้: กด "ทำเป็นมา" แล้ววันไม่เพิ่ม ✅
- สาเหตุ: โมดัลกะรีเฟรชแต่ **ตารางหลักไม่รีโหลด** → เลข "วัน" (att_days) ค้างค่าเดิม
- แก้: `markDay` สำเร็จ → เรียก `load()` รีเฟรชตารางหลักทันที + ตั้ง `SH_CHANGED` เผื่อปิดโมดัลแล้วรีโหลดซ้ำ
- หมายเหตุ: ถ้าช่อง "วัน" มีเลข override ที่พิมพ์เอง จะยึด override (ไม่เปลี่ยนตาม) — ต้องล้างช่องให้ว่างจึงใช้ค่านับอัตโนมัติ
- **Deploy:** push `payroll-review/index.html`

### 🌙 ค่ากะดึก (Night shift allowance) ✅
> ผู้คุมผลัด (กดรับผู้คุมผลัด) = 15 บาท/คืน · พนักงานในผลัด = 10 บาท/คืน · คิดต่อวันที่ทำกะดึก
- **กะดึก = กะข้ามคืน** (end_time ≤ start_time) ตรวจอัตโนมัติจากตาราง `shifts` · **ผู้คุมผลัด** จาก `shift_controllers` (branch,work_date)
- **คำนวณ 2 ฝั่งตรงกัน:** `hrPayrollRun` (เข้าเงินเดือนเป็นรายการเพิ่ม "ค่ากะดึก" src='shift') + `reviewLoad` (แสดง auto_shift_allowance) · override ได้ในหน้าตรวจ (`shift_allowance_override`)
- **ตั้งค่าเรท:** HR → เงินเดือน → ตั้งค่า → การ์ด "🌙 ค่ากะดึก" (ผู้คุมผลัด/พนักงานในผลัด บาท/คืน) · เก็บใน `payroll_config.shift_allowance`
- **หน้าตรวจ ผจก.:** คอลัมน์ใหม่ "ค่ากะดึก" (ช่อง override + hint ยอดอัตโนมัติ)
- **ไฟล์:** `supabase/payroll_shift_allowance.sql` · `shared/hr-api.js` (_payrollConfig+hrPayrollRun+_isOvernight) · `shared/supabase.js` (reviewLoad/reviewSave) · `hr/index.html` (การ์ดตั้งค่า) · `payroll-review/index.html` (คอลัมน์)
- **ตรวจ:** node --check ผ่านทุกไฟล์ · logic กะข้ามคืน/คุมผลัด/override 7/7 เคส
- **⚠️ Deploy:** (1) รัน `supabase/payroll_shift_allowance.sql` (2) push `shared/hr-api.js`,`shared/supabase.js`,`hr/index.html`,`payroll-review/index.html` (3) HR ตั้งเรทค่ากะ (ค่าเริ่มต้น 15/10 มาให้แล้ว)

#### 🐞 แก้: ป้ายเบี้ยวินัยไม่ตรงกับคะแนน ✅
- เดิม ป้าย "เบี้ยวินัย ได้/ไม่ได้" คิดจากเกณฑ์ขาด/สาย (เบี้ยขยัน) → ขัดกับป้ายแบนด์คะแนนที่ขึ้น "ได้โบนัสวินัย" (คนคะแนนถึงแต่มีสายเลยขึ้นไม่ได้)
- แก้: ป้ายเบี้ยวินัยอิง **โบนัสตามแบนด์คะแนน** (`sc.bonus>0` — ตัวที่เข้าเงินเดือนจริง) · ถ้ายังไม่ตั้งระบบคะแนน → fallback เกณฑ์ขาด/สายเดิม
- ผล: ป้ายเบี้ยวินัยตรงกับป้ายคะแนนแล้ว (แบนด์ "ได้โบนัสวินัย" = ✓ · "ปกติ" = ✗)
- **Deploy:** push `shared/supabase.js`

### 👋 พนักงานลาออก: ยึด "วันสิ้นสุดงาน" (end_date) แทนการปิดใช้งาน ✅
> ปัญหา: คนออกยังโผล่ในแดชบอร์ด/จัดกะ → ผจก.ต้องปิดใช้งาน (active=false) → หายจากเงินเดือนรอบที่ค้างด้วย
- **แนวทาง:** ตั้ง **`end_date` (วันสิ้นสุดการทำงาน)** ในฟอร์มพนักงาน โดยคง active=true ไว้ — ระบบยึด end_date เอง
- **ซ่อนจากงานปัจจุบัน** (end_date < วันที่ดู): `hrDashboard` · `hrSchedWeek` · `hrScoreGet` · (บอร์ดวันนี้มีตัวกรองอยู่แล้ว)
- **ยังคิดเงินรอบสุดท้าย** (end_date ≥ วันเริ่มรอบ): `hrPayrollRun` + `reviewLoad` → คนออกกลางรอบยังได้เงินตามวันที่ทำจริง แต่หลุดรอบถัดไปอัตโนมัติ
- **สูตร:** งานปัจจุบัน = `active AND (end_date null OR ≥ วันนี้)` · เงินเดือน = `active AND (end_date null OR ≥ วันเริ่มรอบ)`
- **ไฟล์:** `shared/hr-api.js` (dashboard/schedule/score/payroll) + `shared/supabase.js` (reviewLoad) · ไม่มี SQL (มี end_date อยู่แล้ว) · node --check ผ่าน
- **วิธีใช้:** HR ใส่ "วันสิ้นสุดการทำงาน" ในฟอร์มพนักงาน (ไม่ต้องเอาติ๊ก "เปิดใช้งาน" ออก)
- **+ ยึด `start_date` ด้วย (พนักงานเข้าใหม่):** พนักงานที่เริ่มงานหลังรอบจบ ไม่ควรอยู่ในรอบนั้น → เพิ่มตัวกรอง `start_date ≤ วันสิ้นรอบ` (เงินเดือน/คะแนน/หน้าตรวจ) + `start_date ≤ วันที่ดู` (แดชบอร์ด/จัดกะ)
- **สูตรรวม (ช่วงจ้างงานคาบเกี่ยวรอบ):** เงินเดือน = `active AND (end_date null OR ≥ วันเริ่มรอบ) AND (start_date null OR ≤ วันสิ้นรอบ)`
- **⚠️ Deploy:** push `shared/hr-api.js` + `shared/supabase.js`

### 💳 ระบบผ่อนจ่าย (หักเป็นงวด) สำหรับรายการหักอื่นๆ ✅
> ตั้งยอดรวม + หักงวดละ X → ระบบหักอัตโนมัติทุกรอบจนครบ · งวดสุดท้ายหักเท่าที่เหลือ · หยุดเอง
- **โครง 2 ตาราง:** `payroll_installments` (แผน: emp/label/total/per_round/start_period/status) + `payroll_installment_charges` (ledger รายรอบ: amount/finalized) — unique(installment,period) กันซ้ำ
- **คิดหักใน `hrPayrollRun`:** ต่อคน หักงวดนี้ = min(per_round, คงเหลือ) · คงเหลือ = total − ยอดที่ล็อกแล้ว (charges finalized รอบก่อน) · push รายการหัก `src='inst'` "ผ่อน: <รายการ> (เหลือ X)" · recompute ไม่หักซ้ำ (upsert onConflict)
- **ล็อกตอนปิดรอบ (`hrPayrollFinalize`):** charges รอบนี้ finalized=true + ปิดแผนที่ครบ (status done) · **เปิดรอบใหม่ (`hrPayrollReopen`):** ปลดล็อก + คืนแผนเป็น active ถ้ายังไม่ครบ
- **จัดการที่หน้าตรวจ (ทั้ง HR + ผจก.):** ปุ่ม "💳 ผ่อน" ต่อคน → modal (ดูแผน/เพิ่ม/ยกเลิก) + badge "ผ่อน N · หักรอบนี้ X · เหลือ Y" · `HR.installmentList/Create/Cancel`
- **ไฟล์:** `supabase/payroll_installments.sql` · `shared/hr-api.js` (run+finalize+reopen) · `shared/supabase.js` (installment*+reviewLoad) · `payroll-review/index.html` (ปุ่ม+modal+badge)
- **ตรวจ:** node --check ผ่านทุกไฟล์ · logic หักผ่อน/งวดสุดท้าย/กันซ้ำ/ครบหยุด/เศษ 7/7 เคส
- **⚠️ Deploy:** (1) รัน `supabase/payroll_installments.sql` (2) push `shared/hr-api.js`,`shared/supabase.js`,`payroll-review/index.html`

#### 💳 ผ่อนในเงินเบิกด้วย ✅
- **แปลงใบเบิก → แผนผ่อน:** ในแท็บเบิกเงินล่วงหน้า ใบที่ "จ่ายแล้ว + ยังไม่หัก" มีปุ่ม **"💳 ผ่อน"** → ใส่ยอดหัก/งวด → สร้างแผนผ่อน (ยอด=ยอดเบิก, label="เบิกเงิน RC-xxx") + ทำเครื่องหมายใบเบิก `deducted=true` + `installment_id` (ไม่หักก้อนเดียว) → ระบบผ่อนหักทีละงวดต่อ
- **ไม่กระทบ finalize/reopen เดิม:** ใบที่แปลงแล้ว deducted=true จึงข้ามการหักก้อน · `payroll_ref=null` จึงไม่โดน reopen คืน · ใบแสดง "💳 ผ่อนอยู่"
- **สิทธิ์:** HR + ผจก. (สาขาตน) · action `hr_advance_installment`
- **ไฟล์:** `supabase/payroll_installments.sql` (คอลัมน์ `advance_requests.installment_id`) · `shared/hr-api.js` (hrAdvanceInstallment) · `hr/index.html` (ปุ่ม+handler) · node --check ผ่าน
- **⚠️ Deploy:** รัน `supabase/payroll_installments.sql` ซ้ำ (มี alter add installment_id) + push `shared/hr-api.js`,`hr/index.html`

#### 🏷️ ส่วนลด (ลดหนี้ค้าง) แผนผ่อน/เบิก ✅
- **คงเหลือ = total − discount − หักแล้ว** · เพิ่มคอลัมน์ `payroll_installments.discount`
- **ใช้ที่หน้าตรวจ:** โมดัล 💳 ผ่อน → ปุ่ม **"ส่วนลด"** ต่อแผน active → ใส่ยอด+เหตุผล → ลดคงเหลือทันที (ลดเกินคงเหลือไม่ได้ · ลดจนเหลือ 0 = ปิดแผน) · รวมเบิกที่แปลงเป็นผ่อนด้วย
- **ผูกทุกจุด:** hrPayrollRun (หักงวด=min(per_round, คงเหลือหลังลด)) · finalize/reopen (done เมื่อ paid ≥ total−discount) · installmentList/reviewLoad remaining หัก discount
- **ไฟล์:** `supabase/payroll_installments.sql` (discount) · `shared/hr-api.js` · `shared/supabase.js` (installmentDiscount) · `payroll-review/index.html` (ปุ่ม+แสดง)
- **ตรวจ:** node --check ผ่าน · logic ส่วนลด/cap/ครบ 5/5 เคส
- **⚠️ Deploy:** รัน `supabase/payroll_installments.sql` ซ้ำ (alter add discount) + push `shared/hr-api.js`,`shared/supabase.js`,`payroll-review/index.html`

#### 🐞 แก้บั๊ก: ใบเบิกที่แปลงผ่อนแสดงผลสับสน ✅
- **ป้ายเดิม "หักคืนจากเงินเดือนแล้ว":** ใบเบิกที่แปลงผ่อนตั้ง `deducted=true` เป็น marker → รายละเอียดใบเบิกเลยขึ้น "หักแล้ว" (สับสน) · แก้: ถ้า `installment_id` → ขึ้น **"แปลงเป็นผ่อน — หักทีละงวด (ดูแผนในหน้าตรวจ)"** · timeline `installment`→"แปลงเป็นผ่อน"
- **ยกเลิกแผนผ่อนที่มาจากใบเบิก:** เดิมใบเบิกค้าง deducted=true (หนี้หาย) · แก้ `installmentCancel`: ถ้ายังไม่หักงวดไหน → **คืนใบเบิกเป็นปกติ** (deducted=false, หักคืนก้อนรอบที่จะถึง) · ถ้าหักไปบางงวด → ตัดสายผูก + ยอดที่เหลือยกให้ · toast บอกผล
- **badge ผ่อนไม่ขึ้นในหน้าตรวจ:** สาเหตุหลัก = ยังไม่ deploy `shared/supabase.js` เวอร์ชันที่มี badge · ผ่อนแสดงเป็น **ป้ายใต้ชื่อ** (💳 ผ่อน N · หักรอบนี้ X · เหลือ Y) ไม่ใช่คอลัมน์ "เงินเบิก" (คอลัมน์นั้น=เบิกที่ยังไม่แปลงผ่อนเท่านั้น) · ทำ select เป็น `*` กันพังถ้ายังไม่รัน SQL discount
- **⚠️ Deploy:** push `shared/hr-api.js`,`shared/supabase.js`,`hr/index.html`,`payroll-review/index.html` + รัน `supabase/payroll_installments.sql` ให้ครบ (installment_id + discount)

#### 🧾 ปรับการจัดกลุ่มยอดในสลิป ✅
- **เงินได้:** OT + เบี้ยเลี้ยง/ตำแหน่ง + เบี้ยขยัน + โบนัส + รายการเพิ่ม (เบี้ยพิเศษ/ค่ากะดึก) → **รวมทั้งหมดใน "เงินได้อื่นๆ"** (แถว OT/นายหน้า/เบี้ยเลี้ยง/โบนัส = 0) · เหลือ 2 บรรทัด: เงินเดือน/ค่าจ้าง + เงินได้อื่นๆ
- **รายการหัก:** รายการหัก (สินค้าเสื่อม/หักอื่นๆ/ผ่อน) + เงินเบิกล่วงหน้า → รวมใน **"รายการหักอื่นๆ"** (เดิมทำอยู่แล้ว) · เหลือ ปกส. + รายการหักอื่นๆ
- **Remark:** เหลือแค่ "หักเบิกล่วงหน้า X บาท" (ตัด note อื่นออก)
- ยอดรวม/สุทธิเท่าเดิม (แค่ย้ายที่แสดง · base+earnOther=gross · sso+dedOther=total_deduct) · แก้ที่ `plSlipInner` ใช้ร่วมทั้งพิมพ์/อีเมล/PDF
- **+ ช่องพิมพ์ Remark:** หน้าต่างแก้รายคน (ดินสอ) เพิ่มช่อง "📝 หมายเหตุ (Remark บนสลิป)" → บันทึกลง `payroll_items.note` → แสดงในสลิปต่อจากบรรทัดเงินเบิก (คงทนเมื่อกดคำนวณซ้ำ)
- **⚠️ Deploy:** push `hr/index.html`

#### 💵 HR คีย์เบิกให้พนักงานได้ ✅
- แท็บเบิกเงินล่วงหน้า → ปุ่ม **"＋ คีย์เบิกให้พนักงาน"** → เลือกพนักงาน + จำนวนเงิน + เหตุผล → สร้างคำขอ (status submitted) เข้าคิว "รออนุมัติ" → HR กดอนุมัติ+โอนตามปกติ (พนักงานไม่ต้องคีย์เอง)
- ไม่ติดเงื่อนไขวันคีย์/วงเงิน (ดุลยพินิจ HR) · ผูกบัญชี+สาขาจากข้อมูลพนักงานอัตโนมัติ · event 'submit' note "คีย์โดยสำนักงาน"
- **สิทธิ์:** HR (ทุกสาขา) + ผจก. (สาขาตน) · action `hr_advance_create` + `_nextAdvanceNo` (AD-พ.ศ.-####)
- **ไฟล์:** `shared/hr-api.js` (hrAdvanceCreate) · `hr/index.html` (ปุ่ม+ฟอร์ม openAdvKey) · node --check ผ่าน
- **จำกัด HR เท่านั้น** (`me.role!=='hr'` + ซ่อนปุ่มโหมด ผจก. `body.mgr-mode #adv_keybtn`)
- **+ ติ๊ก "อนุมัติทันที":** คีย์ + อนุมัติในคลิกเดียว → สร้าง status='approved' + approved_amount + payout_due_date + event approve + แจ้งเตือนพนักงาน → พร้อมกดโอนเลย
- **⚠️ Deploy:** push `shared/hr-api.js` + `hr/index.html` (ไม่มี SQL)

### 🧭 กำลังวางแผน (ยังไม่เริ่มโค้ด): ระบบตำแหน่งพนักงาน + สิทธิ์ตามตำแหน่ง (RBAC)
> เอกสารแผนเต็ม: **`แผน-ระบบตำแหน่งและสิทธิ์.md`** — อ่านก่อนเริ่มพัฒนา
- **การตัดสินใจล็อกแล้ว (24 ก.ค.):** ตำแหน่งเริ่มต้น **พนักงาน/ผู้ช่วย/ผู้จัดการ/ไรเดอร์** · โครงสิทธิ์ = **ติ๊ก capability ต่อตำแหน่ง (ยืดหยุ่น)** · ครอบทั้ง สิทธิ์ทำรายการ + เมนู/หน้าจอ + ป้ายกำกับ/รายงาน + เบี้ยตามตำแหน่ง
- **โครง:** ขยาย `positions` (caps jsonb/code/rank/allowance) + `employees.position_id` · ขยาย RPC `mgr_login` คืน caps · `_termActor` คืน caps + `can()` · เมนู ผจก.สร้างจาก caps · **บังคับสิทธิ์ที่ API จริงทุก action** · backward compat: `is_manager=true` เดิม → ตำแหน่งผู้จัดการ (caps เต็ม)
- **เฟส 1:** SQL+API+หน้า "ตำแหน่ง & สิทธิ์" (ติ๊ก cap) + dropdown ตำแหน่งในฟอร์มพนักงาน · **เฟส 2:** Payroll เบี้ย/อัตราตามตำแหน่ง + ป้าย/กรองทุกหน้า + เมนูไรเดอร์
- **สถานะ:** รอผู้ใช้เคาะเริ่มเฟส 1
- **ผู้ใช้เลือกเริ่มจาก "ไรเดอร์" ก่อน** เพราะต้องมีฟังก์ชันเบิกซ่อมบำรุงรถ → ดูแผนถัดไป

### 🏍️ ระบบเบิกซ่อมบำรุงรถ — ตำแหน่ง Rider · เฟส 1 ✅ (สร้างแล้ว รอ deploy)
> เอกสารแผนเต็ม: **`แผน-ระบบเบิกซ่อมรถไรเดอร์.md`** (แปลงนโยบาย 15 หมวดของผู้ใช้เป็นระบบ)
- **หัวใจ:** ทุกรายการเบิก **เปิด/ปิดได้ + ตั้งเงื่อนไขเอง** (แคตตาล็อก `rider_maint_items` seed 18 รายการจากตารางหมวด 11) — HR ปิดรายการที่ไม่ให้เบิกได้เอง
- **การตัดสินใจที่ล็อก:** ไรเดอร์แบบเบา (`employees.is_rider` flag, ยังไม่ผูก RBAC เต็ม) · อนุมัติ **3 ชั้นตามวงเงิน** (≤500 หัวหน้างาน · ≤2,000 ผจก. · เกิน = ฝ่ายบริหาร/HR) — ปรับวงเงินได้ในตั้งค่า
- **สร้างอะไรบ้าง (เฟส 1):**
  - `supabase/rider.sql` — 6 ตาราง (`rider_vehicles`/`rider_maint_items`+seed18/`rider_odometer`/`rider_claims`/`rider_claim_config`/`rider_claim_events`) + `employees.is_rider` + RLS
  - `shared/hr-api.js` — actions `rider_*` (vehicles list/save/delete · items list/save[toggle+เงื่อนไข] · cfg get/save · claim eligibility/create/list/review/pay) + สูตร `_riderEligibility` (ถึงระยะ/เพดานปี/รอบเดือน/ชนิดรถ) + อนุมัติ 3 ชั้น (ผจก.อนุมัติชั้น lead/manager ในสาขาตน, exec=HR)
  - `shared/supabase.js` — `window.HR.rider*` (isRider/myVehicles/items/eligibility/submitClaim/myClaims) สำหรับหน้าพนักงาน
  - `hr/index.html` — แท็บ **🏍️ เบิกซ่อมรถ (ไรเดอร์)** (กลุ่มเมนู "ไรเดอร์") 4 ซับ: คำขอ(อนุมัติ/จ่าย) · ทะเบียนรถ · รายการเบิก(สวิตช์เปิด/ปิด+แก้เงื่อนไข, HR) · ตั้งค่า(HR) · ผจก.เห็นแท็บนี้ด้วย
  - `employee/index.html` — การ์ด **🏍️ เบิกซ่อมรถ** โผล่เฉพาะ `is_rider` (หลังกรอกรหัส) → โมดัลขอเบิก (เลือกรถ/รายการ→เห็นสิทธิ์สด→แนบรูปตาม require_*→ส่ง) + auto-save ร่าง (sessionStorage) + "คำขอของฉัน"
- **ตรวจแล้ว:** node --check ผ่านทุกไฟล์ · logic เช็คสิทธิ์ 10/10 เคส · SQL seed 18แถว×13คอลัมน์ตรง
- **สวิตช์ `is_rider`:** เพิ่มในฟอร์มแก้ไขพนักงานแล้ว (หน้า "พนักงาน" → ส่วน "ระบบ" → toggle "เป็นไรเดอร์ 🏍️") · `hrSave` บันทึก `is_rider` · `hrList` ส่งค่ากลับ (spread `*`)
- **⚠️ ต้อง deploy:** (1) รัน `supabase/rider.sql` ใน Supabase (2) push `shared/hr-api.js`, `shared/supabase.js`, `hr/index.html`, `employee/index.html` (3) เปิดสวิตช์ "เป็นไรเดอร์" ในฟอร์มพนักงาน + ลงทะเบียนรถในแท็บ "ทะเบียนรถ"
### ⛽ Rider: เบิกค่าน้ำมัน (เงินหมุนเวียน หักคืนเงินเดือน) ✅
> เบิกเงินไปเติมน้ำมัน (ไม่ใช่สวัสดิการ) → ยอดอนุมัติหักคืนจากเงินเดือนรอบที่ปิด · HR อนุมัติ
- **เพดาน (ตั้งค่าได้):** ครั้งละ ≤500 · รวม/รอบ/คน ≤1500 · ไม่จำกัดจำนวนครั้ง · บังคับกรอกไมล์ (option)
- **หลักฐาน:** แนบรูปบิล (ปั๊มเวลา/พิกัด) + HR ติ๊ก "ได้บิลตัวจริง" · เก็บเลขไมล์ไว้เช็คความสอดคล้อง
- **ตาราง:** `rider_fuel_claims` + `rider_fuel_config` (`supabase/rider_fuel.sql`)
- **หักเงินเดือน:** `hrPayrollRun` ดึง approved+undeducted → รายการหัก "ค่าน้ำมัน (เบิก)" src='fuel' · finalize mark deducted+payroll_ref · reopen คืน (ทำงานเหมือน advance)
- **API:** supabase `riderFuelConfig/Quota/Submit/MyList` · hr-api `rider_fuel_list/review/cfg_get/cfg_save` (HR อนุมัติเท่านั้น)
- **UI:** employee ปุ่ม "⛽ เบิกค่าน้ำมัน" (โควตา+รูปบิล) · HR แท็บไรเดอร์ → ซับ "⛽ ค่าน้ำมัน" (อนุมัติ/ปฏิเสธ/ติ๊กบิลจริง/สถิติ + ตั้งค่าเพดาน)
- **ตรวจ:** node --check ผ่านทุกไฟล์ · logic เพดานครั้ง/รวม/ไม่จำกัดครั้ง 7/7 เคส
- **+ HR คีย์ย้อนหลังได้:** ปุ่ม "＋ คีย์ย้อนหลัง" ในซับแท็บค่าน้ำมัน → เลือกไรเดอร์ + ยอด + **ระบุรอบ (เดือนหัก)** + ไมล์/บิล(ถ้ามี) + ติ๊กบิลจริง + อนุมัติทันที · `hrFuelCreate` (ไม่ติดเพดาน = ดุลยพินิจ HR สำหรับแก้ข้อมูลรอบก่อนใช้ระบบ)
- **⚠️ Deploy:** (1) รัน `supabase/rider_fuel.sql` (2) push `shared/hr-api.js`,`shared/supabase.js`,`hr/index.html`,`employee/index.html` (3) เปิด is_rider + ลงทะเบียนรถให้ไรเดอร์

### 🏍️ Rider เฟส 2 ✅ (สร้างแล้ว รอ deploy) — เลขไมล์ + GPS + by_distance + กันทุจริต
- **บันทึกเลขไมล์ประจำวัน:** ตาราง `rider_odometer` (มีอยู่แล้ว) · ก่อน/หลังงาน · ถ่ายหน้าปัด + GPS อัตโนมัติ · unique(vehicle,date,phase) กันซ้ำ
- **กันทุจริต:** ห้ามไมล์ถอยหลัง (เทียบ `odo_last` — น้อยกว่าครั้งก่อน = บล็อก) · GPS เทียบรัศมีสาขา (`branches.lat/lng/radius_m`+200ม.) → `gps_ok` · ฝั่ง HR ไฮไลต์แถวไมล์ถอยหลัง/GPS นอกรัศมี
- **ระยะทางสะสม → สิทธิ์ by_distance:** ระยะปีนี้ = ไมล์สูงสุด−ต่ำสุด · น้ำมันเครื่อง เพดาน = ระยะ÷4,000 (เช่น วิ่ง 24,000 = เบิกได้ 6 ครั้ง) — คิดอัตโนมัติทั้ง `_riderEligibility` (API) และ `riderEligibility` (พนักงาน)
- **สร้าง/แก้:**
  - `shared/hr-api.js` — actions `rider_odo_log`/`rider_odo_list`/`rider_odo_today` + `_riderDistanceYear` + `_haversine` + อัปเดต `_riderEligibility` by_distance
  - `shared/supabase.js` — `HR.riderLogOdometer`/`riderTodayOdometer`/`riderDistanceYear` + อัปเดต eligibility by_distance
  - `employee/index.html` — ปุ่ม "📍 บันทึกเลขไมล์" ในการ์ดไรเดอร์ + โมดัล (เลือกรถ→เห็นสถานะวันนี้→เลือกก่อน/หลัง→ถ่ายหน้าปัด→GPS อัตโนมัติ→บันทึก) · ใช้ `gps` global + กันไมล์ถอยหลัง
  - `hr/index.html` — การ์ดรถ ปุ่ม "📍 ไมล์" → โมดัลประวัติ + ระยะทางปีนี้ + ไฮไลต์ผิดปกติ
- **ปั๊มไทมสแตมป์บนรูป (หมวด 3.2):** รูปไมล์ + รูปหลักฐานซ่อมทุกช่อง ปั๊ม **ชื่อพนักงาน + ในเขต/นอกเขต + พิกัด GPS + สถานที่ + เวลา + วันที่** ลงบนรูปผ่าน `drawStamp()` (ตัวเดียวกับรูปลงเวลา) — helper `riderStampFile()` ใน employee/index.html
- **ตรวจแล้ว:** node --check ผ่านทุกไฟล์ · logic ระยะทาง/by_distance/ไมล์ถอยหลัง 7/7 เคส
- **⚠️ Deploy:** ตาราง `rider_odometer` มากับ `rider.sql` (เฟส1) แล้ว → push 4 ไฟล์ static เท่านั้น · ต้องมี `branches.lat/lng/radius_m` (มีอยู่จาก geofence ลงเวลา) จึงจะเช็ค GPS ได้
- **ถัดไป เฟส 3:** ค่าน้ำมัน (ใบเสร็จ/ต่อกม.) + ค่าโทร/ทางด่วน/อุปกรณ์ความปลอดภัย + รายงานฝ่ายบริหาร (หมวด13) + แจ้งเตือนใกล้ถึงรอบ + อุบัติเหตุ (หมวด9)

---

## 🗓 รอบ 24 ก.ค. 2026 (ต่อ) — เฟส 1.5: ส่งสลิปเงินเดือนทางอีเมล (Resend) ✅

> ต่อยอดจาก Payroll · ปุ่ม "📧 ส่งอีเมล" ใช้งานได้จริงแล้ว (เดิมเป็น stub) · ตรวจ logic (สลิป/นับผลส่ง) ผ่านในแซนด์บ็อกซ์

**ทำอะไรไปบ้าง:**
- **SQL ใหม่ `supabase/payroll_mail.sql`** — เพิ่มคอลัมน์ `payroll_items.mail_status / mail_error / mail_msg_id` (มี `mailed_at` อยู่แล้ว) + config key `mail` = `{from, from_name}` (ค่าเริ่มต้น `onboarding@resend.dev`) · รันซ้ำได้
- **Edge Function ใหม่ `supabase/functions/payroll-mail/index.ts`** — ส่งทีละคนผ่าน Resend API · ยืนยันรหัส HR (`hr_check_password`) · body = สลิป HTML + แนบ PDF (base64) · เขียนสถานะกลับ `payroll_items` (sent/failed/no_email + mailed_at + msg_id)
- **`shared/hr-api.js`** — `_payrollConfig` + `hrPayrollConfigSave` รองรับ key `mail` (ผู้ส่ง)
- **`hr/index.html`** —
  - แยกสลิปเป็นฟังก์ชันใช้ร่วม `plSlipInner` + `PL_SLIP_CSS_CORE` + `plSlipDocHtml` (พิมพ์/อีเมล/PDF ใช้ตัวเดียวกัน)
  - PDF สร้างฝั่ง client จากสลิป HTML เดิม (html2canvas + jsPDF โหลดจาก CDN ตอนกดส่ง) → ฟอนต์ไทยตรง → base64 แนบไปกับเมล
  - ต่อปุ่ม `pl_mail` → modal `openPayrollMail`: ปุ่ม "ส่งทุกคนที่มีอีเมล" + ส่งรายคน/ส่งซ้ำ + คอลัมน์สถานะต่อคน (ส่งแล้ว/ล้มเหลว+เหตุผล/ไม่มีอีเมล) + progress
  - ช่องตั้ง "อีเมลผู้ส่ง (From) + ชื่อผู้ส่ง" ในหน้า ⚙️ ตั้งค่า · ยังบังคับ "ปิดรอบก่อน" จึงส่งได้
- **🔓 ปุ่ม "เปิดรอบใหม่ (คืนค่า)"** (เฉพาะ HR · โผล่เมื่อรอบ finalized) — เปิดรอบที่ปิดแล้วกลับเป็น "ร่าง" เพื่อแก้/ทดสอบ · **คืนการหักเงินเบิกที่ผูกกับรอบนี้อัตโนมัติ** (ยกเลิก deducted โดย `payroll_ref = run.id` — ย้อนตรงเป๊ะ + ลง event 'reopen') · ⚠️ `defer_rounds` ที่ถูกลดตอนปิดรอบไม่คืนอัตโนมัติ (เตือนใน confirm) · API `hrPayrollReopen` (action `hr_payroll_reopen`)
  - เหตุผลที่ทำเป็นปุ่มถาวร: การปิดรอบหักเงินเบิกแบบ global (ทุกคน) → sample data แยกทดสอบไม่ได้ · และงานจริงมีโอกาสเผลอปิดรอบผิดต้องเปิดแก้อยู่แล้ว
- **🧾 ออกแบบสลิปใหม่ 2 ภาษา 3 คอลัมน์** (เงินได้ / รายการหัก / ยอดสะสมทั้งปี) ตามฟอร์มมาตรฐาน + ช่องลายเซ็นผู้จ่าย + หมายเหตุ + ข้อความรักษาความลับ ไทย/อังกฤษ · **ใช้ร่วมกันทั้งพิมพ์ (A4 1 คน/แผ่น) / อีเมล / PDF** (ฟังก์ชัน `plSlipInner` + `PL_SLIP_STYLE` + `plSlipMeta`)
  - **YTD คำนวณจริง** ใน `hrPayrollRun` — สะสม gross→เงินได้สะสม, sso→เงินประกันสังคมสะสม จาก payroll_items ของรอบ finalized ในปีปฏิทินเดียวกัน (รอบที่ยังไม่ปิด บวกยอดรอบปัจจุบันให้) · คืน `ytd_earnings/ytd_sso/ytd_tax`
  - ช่องที่ระบบยังไม่เก็บ (ภาษีหัก ณ ที่จ่าย / กยศ. / เงินประกัน / ขาด-ลา-สาย) แสดง **0** ไว้ก่อน (โครงพร้อมเพิ่มทีหลัง)
  - แมปข้อมูลเดิม: เบี้ยตำแหน่ง+เบี้ยขยัน→"ค่าเบี้ยเลี้ยง/ค่าครองชีพ" · additions→"เงินได้อื่นๆ" · เบิกล่วงหน้า+หักอิสระ→"รายการหักอื่นๆ" (ยอดรวม reconcile กับ gross/total_deduct/net เป๊ะ — ตรวจแล้ว)
  - ปีแสดงเป็น พ.ศ. (ให้ตรงกับทั้งระบบ) · ไฟล์: `hr/index.html` + `shared/hr-api.js` (static · ไม่มี SQL/Edge ใหม่)
- **📧 เพิ่มช่องอีเมลในหน้าพนักงานกรอกเอง (`staff/`)** — เดิมมีเฉพาะฟอร์ม HR · หน้า staff ไม่มีเลย พนักงานกรอกอีเมลเองไม่ได้ · แก้ครบ pipeline: `staff/index.html` (ช่องกรอก + โหลดค่าเดิม + ส่ง) · `shared/supabase.js` (`PROFILE_FIELDS` + `submitProfile` เพิ่ม email) · `shared/hr-api.js` (`hrSubmissionApprove` copy email → employees) · SQL ใหม่ `supabase/profile_email.sql` (คอลัมน์ email ใน profile_submissions)
  - **ดีพลอย:** รัน `supabase/profile_email.sql` · อัป `staff/index.html`+`shared/supabase.js`+`shared/hr-api.js` ขึ้น GitHub
- **🖨️ พิมพ์สลิปกลับเป็น 2 คน/แผ่น A4** — ตอนออกแบบสลิปใหม่เผลอเปลี่ยนเป็น A4 1 คน/แผ่น · แก้ `payrollPrintSlips` ให้ซ้อน 2 สลิปต่อแผ่น (บน-ล่าง มีเส้นประคั่น + บีบระยะห่างให้พอดี) ตามสเปกเดิม · ไฟล์: `hr/index.html`
- **✅ ตั้งค่า Resend + โดเมนจริงเสร็จแล้ว (พร้อมส่งจริง)** — จดโดเมน `mpbpconnect.com` (Cloudflare) · เพิ่ม subdomain **`send.mpbpconnect.com`** ใน Resend → verify ผ่าน (Auto configure DNS ผ่าน Cloudflare) → สถานะ **Verified**
  - **ผู้ส่ง (From) ที่ใช้จริง = `payroll@send.mpbpconnect.com`** (ตั้งในหน้าเงินเดือน → ⚙️ ตั้งค่า → การ์ดอีเมลผู้ส่ง) · เลิกใช้โหมดทดสอบ onboarding@resend.dev แล้ว → ส่งหาพนักงานทุกคนได้
  - เหลือ (ผู้ใช้ทำเอง): สร้าง Resend API key → ใส่ `RESEND_API_KEY` ใน Supabase secrets · รัน `payroll_mail.sql`+`profile_email.sql` · `supabase functions deploy payroll-mail --no-verify-jwt` · อัปไฟล์ static ขึ้น GitHub

### ⚠️ สิ่งที่ต้อง deploy รอบนี้ (เฟส 1.5)
1. **ผู้ใช้ตั้งค่า Resend เอง:** สมัคร resend.com → (ใช้จริง) verify โดเมน → สร้าง API key → ใส่ `RESEND_API_KEY` ใน Supabase secrets → ตั้ง From ในหน้าตั้งค่า · โหมดทดสอบใช้ `onboarding@resend.dev` ได้ (ส่งได้เฉพาะเข้าอีเมลเจ้าของบัญชี Resend เท่านั้น)
2. **รัน SQL:** `supabase/payroll_mail.sql`
3. **Edge Function → deploy:** `supabase functions deploy payroll-mail --no-verify-jwt`
4. **ไฟล์ static → GitHub:** `shared/hr-api.js` · `hr/index.html` → Ctrl+Shift+R
5. กรอกอีเมลพนักงานในฟอร์มพนักงานให้ครบ (คนไม่มีอีเมล = ขึ้น "ไม่มีอีเมล" ข้ามให้)

**ค้างต่อ:** ให้พนักงานกดขอเลื่อนหักเบิกเองในแอป · Payroll เฟส 2 (Excel/ไฟล์ธนาคาร, สปส.1-10, YTD)

---

## 🗓 รอบ 24 ก.ค. 2026 — แก้บั๊กจากการใช้งานจริง + UX (หลาย fix)

> ทุกจุดตรวจ syntax/logic ผ่านในแซนด์บ็อกซ์ · เอกสารชี้แจง 2 ไฟล์ (สำหรับที่ประชุม + NotebookLM) ก็ทำในรอบนี้

### 1) 📸 กันรูปหายเวลาถ่ายส่งงาน (auto-save WIP) — `handover/index.html`
- **ปัญหา:** ถ่ายรูปส่งงานแล้วสลับแอป/รีโหลด/คนอื่นหยิบมือถือ → รูปหาย ต้องถ่ายใหม่
- **แก้:** เก็บร่างอัตโนมัติลง **IndexedDB** ทุกครั้งที่ถ่าย/ลบรูป/พิมพ์หมายเหตุ · กลับมาทำต่อได้ · ส่งสำเร็จ=ลบร่าง · ร่างเก่า >3 วันล้างเอง
- **ครอบคลุมครบ 6 จุด:** งานในกะ (`t:`) · งานพิเศษ (`sp:`) · เชลฟ์ (`shv:`) · งานแก้ตีกลับ (`fx:`) · งาน ผจก.มอบหมาย (`mgt:`) · รับสินค้า (`gd:new`)
- ตัวช่วยกลาง `wipSave/wipGet/wipDel/wipCleanup/wipPut/wipRestore` + `taskDraftSave`
- **ฝั่ง ผจก. (`hr/index.html`) ด้วย:** งานที่สำนักงานมอบหมาย (MT — ส่งรูป/ข้อความความคืบหน้า) auto-save key `mt:<taskid>` · helpers `hrwipOpen/Save/Get/Del/Cleanup` + `mtDraftSave/mtRestoreDraft` · hook: แนบรูป/ลบรูป/วาด/ดึงรูปจากไทม์ไลน์/พิมพ์ข้อความ → เก็บร่าง · เปิดงาน=กู้ร่าง · ส่งสำเร็จ=ลบร่าง · ใช้ IndexedDB `hr_wip` ร่วม origin เดียวกับฝั่งพนักงาน

### 2) 🧾 หน้า QA สินค้า → ตาราง + หน้ารวมทุกโฟลเดอร์ — `hr/index.html` · `shared/hr-api.js`
- รายการสินค้าในโฟลเดอร์: เปลี่ยนจากการ์ด → **ตาราง** (รูป/ชื่อ/หมดอายุ+เหลือกี่วันสี/สถานะ/ผู้บันทึก) เรียงวันหมดอายุใกล้สุด
- เพิ่ม **หน้ารวมสินค้าทุกโฟลเดอร์** บนสุดของแท็บ QA: API ใหม่ `hrQaAllItems` (action `hr_qa_all_items`) + ตัวกรองสถานะ/สาขา/ค้นหา + ไฮไลต์ใกล้หมด ≤7 วัน + ปุ่มเปิดโฟลเดอร์/เปลี่ยนสถานะ/ลบ

### 3) 💵 อัตราค่าจ้าง/OT (Payroll) — `shared/hr-api.js` · `hr/index.html` · `supabase/payroll.sql`
- **อัตราค่าแรงกลาง** (config `wage`) ตั้งที่เดียวใช้ทุกคน + override รายคน (ช่องฐานโชว์ placeholder "กลาง X")
- **ค่าเริ่มต้น = รายวันทุกคน**
- **OT เรทคงที่ต่อชั่วโมง** (config `ot.mode=flat/multiplier` + `flat_rate`) — ค่าเริ่มต้น flat · ตั้งอัตรา OT รายคนทับได้
- หน้าคำนวณ: ตาราง 1 คน/บรรทัด + ตัวกรองสาขา + **เต็มจอเดสก์ท็อป** (`.pl-fullbleed`) + คอลัมน์ครบ (OT/ปกส./หักเบิก/หักอื่น แยกชัด)

### 4) 💸 หักเงินเบิก "ตามรอบจริง ไม่เว้นเดือน" + ผ่อนผัน — `shared/hr-api.js` · `shared/supabase.js` · `supabase/advance_defer.sql`
- payroll หักทุกใบที่ "จ่ายแล้ว+ยังไม่หัก" ในรอบทันที (ไม่ผูก deduct_month) · `deduct_month=รอบปัจจุบัน`
- **ผ่อนผัน/เลื่อนการหัก** (`advance_requests.defer_rounds`) — HR กดเลื่อน N รอบ · payroll ข้าม · ปิดรอบลดตัวนับ · API `hrAdvanceDefer` (action `hr_advance_defer`) + ปุ่มในโมดัลใบเบิก

### 5) 🤖 แก้บั๊กนิดา (Edge Functions — ต้อง redeploy)
- **OT ไม่คำนวณใหม่ตอนแก้เวลาออก** (`hr-assistant` → `edit_attendance`): เดิมคิด OT ใหม่เฉพาะกรณีออกก่อน · กรณีอยู่เกินเวลาเลิกกะ OT ค้างค่าเดิม → **แก้ให้คำนวณ OT ใหม่ทั้ง 2 กรณี** (เคารพ `no_ot`)
- **แก้เวลาแล้วบอร์ดขึ้น "ยังไม่มา"** (`hr-assistant` → `edit_attendance`): เดิมใช้ `update` เปล่า ๆ ซึ่งไม่ทำอะไรถ้าไม่มีแถว (พนักงานสแกนหน้าไม่ได้ ยังไม่เคยลงเวลา) → **แก้ให้สร้างแถวใหม่ให้ครบ** (ดึงกะ/สาขาจากตารางเวร + คำนวณสายด้วย `calc_late_min`)
- **นิดาเงียบหลัง ผจก.ตอบรับทราบ** (`chat-reply`): ตัวนับกันลูป `nidaRecent>=5` นับข้อความเตือนอัตโนมัติรวมด้วย → **แก้:** ถ้า ผจก.เพิ่งพูดแบบมีความหมาย (ปิดเรื่อง/ตอบ/ถาม) และนิดายังไม่ปิดบทสนทนา → ตอบได้ 1 ครั้งเสมอ ไม่ติดเบรก

### 6) 💬 ปุ่มแชทนิดาลากเลื่อนได้ — `hr/index.html`
- ปุ่มลอย `chatFab` ลากย้ายได้ (เมาส์/ทัช) กันทับปุ่มส่งข้อความ · แตะสั้น=เปิดแชท · จำตำแหน่ง (localStorage) · `makeChatFabDraggable`

### 📄 เอกสารชี้แจง (ในโฟลเดอร์)
- `สรุป-ประโยชน์ระบบ-พนักงานและผู้จัดการ.md/.docx` — ชี้แจงที่ประชุม
- `NotebookLM-แหล่งข้อมูล-ระบบบริหารงานร้าน.md/.pdf` — ป้อนเข้า NotebookLM ทำสไลด์ (PDF ฝังฟอนต์ Sarabun/Noto Thai)

### ⚠️ สิ่งที่ต้อง deploy รอบนี้
1. **ไฟล์ static → GitHub:** `hr/index.html` · `shared/hr-api.js` · `shared/supabase.js` · `handover/index.html` → Ctrl+Shift+R
2. **SQL:** `supabase/advance_defer.sql` (+ `payroll.sql` ถ้ายังไม่รันเวอร์ชันล่าสุด)
3. **Edge Functions → redeploy:** `supabase functions deploy hr-assistant --no-verify-jwt` · `supabase functions deploy chat-reply --no-verify-jwt`
4. **แก้ข้อมูลเฉพาะเคส (รันครั้งเดียว):** OT อมรรัตน์ 0874790 (set ot_hours=2) · เวลาเข้าณัชชา 0657377 (insert แถวลงเวลา 08:20) — ดู SQL ในแชท

---

## 🗓 รอบ 23 ก.ค. 2026 — ปรับปรุง Payroll ให้ใช้จริง + แก้/เพิ่มระบบหักเงินเบิก

> ต่อจากเฟส 1 Payroll (ดูรอบ 22 ก.ค. ด้านล่าง) · ทั้งหมดตรวจสูตร+syntax ผ่านในแซนด์บ็อกซ์ (รวม 60+ เคส)
> **ไฟล์ที่แตะรอบนี้:** `shared/hr-api.js` · `shared/supabase.js` · `hr/index.html` · `supabase/payroll.sql` · `supabase/advance_defer.sql` (ใหม่)

### 1) 💵 อัตราค่าแรงกลาง (ตั้งที่เดียวใช้ทุกคน)
- เพิ่ม config key `wage` = `{daily_rate, monthly_rate}` ในหน้า ⚙️ ตั้งค่า (การ์ดเขียว "อัตราค่าแรงกลาง") — ตามค่าแรงขั้นต่ำ
- `_payrollCompute`: อัตราค่าแรง = **รายคน (ถ้าตั้ง >0) → ไม่งั้นใช้อัตรากลาง**
- หน้าตั้งค่าจ้าง: ช่อง "ฐาน" โชว์ placeholder `กลาง Xxx` · เว้นว่าง = ใช้กลาง · `hrPayrollProfileList` คืน `defaults`
- **ค่าเริ่มต้น = รายวันทุกคน** (SQL default 'daily' + hrPayrollRun/ProfileList/Save default daily + dropdown daily มาก่อน)

### 2) ⏱ OT คิดแบบเรทคงที่ต่อชั่วโมง (เข้าใจง่าย)
- config `ot` เพิ่ม `mode` (`flat`|`multiplier`) + `flat_rate` · ค่าเริ่มต้น **flat 60 บาท/ชม.** (แก้เป็น 65 ฯลฯ)
- `flat`: OT = ชม. × flat_rate · `multiplier`: ชม. × (ค่าแรงรายชั่วโมง × ตัวคูณ) — สลับได้ใน dropdown
- อัตรา OT รายคน (`payroll_profiles.ot_rate`) ทับค่ากลางได้เสมอ

### 3) 📊 หน้าคำนวณ = ตาราง 1 คน/บรรทัด + เต็มจอ
- เดิมเป็นการ์ดกินพื้นที่ → เปลี่ยนเป็นตารางแถวเดียว/คน · คอลัมน์ครบ: วัน·อัตรา·ฐาน·**OT**·เบี้ย/โบนัส·เพิ่มอื่น┃รวมรับ┃**ปกส.**·**หักเบิก**·หักอื่น┃รวมหัก┃สุทธิ·✏️ (tooltip ดูรายละเอียดย่อย)
- **ตัวกรองสาขา** + ยอดรวมด้านบน (รวมรับ/ปกส.นำส่ง/หักเบิก/สุทธิ) คิดตามสาขาที่กรอง
- **เต็มจอบนเดสก์ท็อป** (CSS `.pl-fullbleed` @media ≥1281px = 98vw) · มือถือเลื่อนแนวนอนได้

### 4) 💸 แก้สำคัญ: หักเงินเบิก "ตามรอบจริง ไม่เว้นเดือน"
- **ปัญหาเดิม:** advance ตั้ง `deduct_month = เดือนถัดไป` และ payroll ดึงเฉพาะ deduct_month == เดือนจ่าย → เงินเบิกไม่ถูกหัก (เสี่ยงเบิกเกินค่าแรงแล้วหาย)
- **แก้:** payroll หัก **ทุกใบที่ "จ่ายแล้ว (paid) + ยังไม่หัก (deducted=false)"** ในรอบที่คำนวณทันที — ไม่ผูก deduct_month · ปิดรอบ = mark หักแล้ว (ผูก payroll_ref) · ใช้ `approved_amount` ถ้ามี
- ฝั่งเบิก (`supabase.js`): `deduct_month = q.month` (รอบปัจจุบัน) + ข้อความเปลี่ยนเป็น "หักคืนรอบที่จะถึงนี้ทันที (ไม่เว้นเดือน)" · popup ไม่โชว์เลขเดือนที่ค้างแล้ว
- ⚠ **แนะนำ:** ปิดรอบทีละรอบตามลำดับ (อย่าค้างร่างหลายรอบพร้อมกัน) กันยอดหักซ้อนข้ามรอบ

### 5) ⏳ ผ่อนผัน/เลื่อนการหักเงินเบิก (ใหม่)
- **SQL ใหม่ `supabase/advance_defer.sql`** — เพิ่ม `advance_requests.defer_rounds` (ตัวนับรอบ) + defer_note/by/at
- HR เปิดใบเบิก (paid+ยังไม่หัก) → ปุ่ม **"⏳ เลื่อนการหัก 1 รอบ"** (ใส่เหตุผล) · "+ เลื่อนอีก 1 รอบ" · "ยกเลิกการเลื่อน"
- payroll run **ข้าม** ใบที่ `defer_rounds > 0` · ปิดรอบ = ลด defer_rounds ลง 1 (พอ 0 รอบถัดไปหักจริง อัตโนมัติ)
- API `hrAdvanceDefer` (action `hr_advance_defer`) · เฉพาะ HR · หักไปแล้วเลื่อนไม่ได้ · ลง event ในไทม์ไลน์
- ยังเป็น **HR กดเลื่อนให้** (พนักงานแจ้งผ่านแชท) — ต่อยอด: ให้พนักงานกดขอเองในแอป แล้ว HR อนุมัติ

### ⚠️ สิ่งที่ต้อง deploy รอบนี้
1. **รัน SQL (เรียงได้):** `supabase/payroll.sql` (รันซ้ำ — ได้ key `wage`/`ot` ใหม่ + default daily) · `supabase/advance_defer.sql` (คอลัมน์ defer_rounds)
2. **ไฟล์ static → GitHub:** `shared/hr-api.js` · `shared/supabase.js` · `hr/index.html` → **Ctrl+Shift+R**
3. ตั้งรหัสเงินเดือนครั้งแรก (ถ้ายังไม่ตั้ง): `select public.payroll_set_password('<รหัส HR>','<รหัสเงินเดือน>');`
4. Edge Function: **ไม่ต้อง**

### ▶️ งานต่อไป (ค้างไว้)
- **เฟส 1.5 อีเมลสลิป (Resend — เลือกแล้ว):** สมัคร resend.com → ยืนยันโดเมนผู้ส่ง → `supabase secrets set RESEND_API_KEY=...` → ทำ Edge Function `payroll-mail` (วนส่งสลิป HTML/PDF ต่อคน) → ต่อปุ่ม "📧 ส่งอีเมล" (ปัจจุบันเป็น stub เตือนว่ายังไม่พร้อม) ให้ส่งทุกคน/รายคน + สรุปผล · กรอกอีเมลพนักงานที่ฟอร์มพนักงาน (มีช่องแล้ว)
- ให้พนักงานกด "ขอเลื่อนหักเบิก" เองในแอป → HR อนุมัติ
- Payroll เฟส 2: ส่งออก Excel/ไฟล์ธนาคาร · รายงาน สปส.1-10 · YTD · (ถ้าต้องการ) prorate ขาดงานรายเดือน / ภาษีหัก ณ ที่จ่าย

---

## 🗓 รอบ 22 ก.ค. 2026 (ต่อ) — ระบบเงินเดือน (Payroll) เฟส 1

> ทำตามแผน `แผน-ระบบเงินเดือน-Payroll.md` · ตรวจสูตรคำนวณ 16 เคส + syntax ผ่านในแซนด์บ็อกซ์

**ขอบเขต:** คำนวณ + ออกสลิปเท่านั้น **ไม่โอนเงินจริง** · ค่าจ้างผสมรายเดือน/รายวัน · เห็นเฉพาะ HR + **ล็อกรหัสอีกชั้น**

**สูตร:** รายรับ = ฐาน (รายเดือนเต็ม / รายวัน = วันทำงานถ่วง day_value × อัตรา) + OT (ชม.×อัตรา) + เบี้ยตำแหน่ง + เบี้ยขยัน (จ่ายเมื่อไม่ขาด+ไม่สายเกินกำหนด) + โบนัสคะแนนวินัย + รายการเพิ่มอิสระ · รายหัก = ประกันสังคม 5% (เพดาน 750, ข้ามเมื่อฐาน=0) + หักเบิกล่วงหน้า + รายการหักอิสระ · สุทธิ = รับ − หัก (ปัดเศษ)

**ทำอะไรไปบ้าง:**
- **SQL `supabase/payroll.sql`** — `payroll_config` / `payroll_profiles` / `payroll_runs` / `payroll_items` + `employees.email` + RPC `payroll_check_password` / `payroll_set_password` (SECURITY DEFINER · รหัสเก็บใน `app_config` ที่ล็อก anon เหมือนรหัส HR) · RLS · รันซ้ำได้
- **`shared/hr-api.js`** — `hrPayroll*`: unlock / pass_set / config / profiles / profile_save / **run (คำนวณร่างจาก attendance+score+advance)** / item_save (เพิ่ม-หักอิสระ) / **finalize (ปิดรอบ + เชื่อม `hrAdvanceDeduct` กันหักซ้ำ ผูก payroll_ref)** / summary · ผูก dispatch `hr_payroll_*` · กันหารศูนย์/ฐาน=0
- **`hr/index.html`** — แท็บ **💰 เงินเดือน** (กลุ่มเบิกเงิน · ไม่อยู่ใน MGR_TABS = ผจก.ไม่เห็น): ประตูรหัสชั้นสอง → 4 หน้าย่อย (รันรอบ · ตั้งค่าจ้าง inline · สรุปยอดจ่าย/สาขา · ตั้งค่ากลาง+เปลี่ยนรหัส) · การ์ดรายคน (รับ/หัก/สุทธิเด่น) · **พิมพ์สลิป A5 2 คน/แผ่น A4** (iframe @page A4 + page-break) · ปุ่มอีเมล (แจ้งว่ารอเฟส 1.5) · เพิ่มช่องอีเมลในฟอร์มพนักงาน

### ⚠️ สิ่งที่ต้อง deploy รอบนี้
1. **รัน SQL:** `supabase/payroll.sql` → แล้วตั้งรหัสเงินเดือนครั้งแรก: `select public.payroll_set_password('<รหัส HR>', '<รหัสเงินเดือนใหม่>');` (หรือตั้งในหน้าตั้งค่า)
2. **ไฟล์ static:** `shared/hr-api.js` · `hr/index.html` → GitHub → Ctrl+Shift+R
3. Edge Function: **ไม่ต้อง** (เฟส 1 ยังไม่ใช้อีเมล)

> **เฟส 1.5 (ต่อไป):** ส่งสลิปทางอีเมลผ่าน **Resend** (เลือกแล้ว) — ต้องสมัคร resend.com + ยืนยันโดเมน + ใส่ `RESEND_API_KEY` ใน secrets → ทำ Edge Function `payroll-mail` + ปุ่มส่งทุกคน/รายคน · **เฟส 2:** ส่งออก Excel/ไฟล์ธนาคาร + รายงาน สปส. + YTD

---

## 🗓 รอบ 22 ก.ค. 2026 — ระบบประเมินผลงานผู้จัดการสาขา (เฟส 1)

> ทำตามแผน `แผน-ประเมินผลงานผู้จัดการสาขา.md` (การตัดสินใจล็อกแล้ว) · ตรวจ syntax + logic 21 เคสผ่านในแซนด์บ็อกซ์

**แนวคิด:** คะแนนการปฏิบัติงาน (Operational Scorecard) จากข้อมูลที่ระบบมีอยู่แล้ว — **แยก 2 คะแนน** `A ผจก.ทำเอง` (รับผิดชอบงาน 40 / ตรวจคุณภาพ 40 / ตอบสนอง 20) กับ `B ผลทีม` (วินัยทีม 55 / หน้าร้าน 45) · **บล็อก C = KPI ธุรกิจคีย์มือ** (ยอดขาย/ตัดจ่าย/QSSI) แยกต่างหาก · **เห็นเฉพาะ HR** ซ่อนในโหมด ผจก.

**หลักสำคัญ:** ตัวชี้วัดไหนไม่มีข้อมูลในรอบ → ตัดออกแล้วเกลี่ยน้ำหนักให้ตัวที่เหลือ (ไม่ให้ 0 ทำร้ายคะแนนเกินจริง) · normalize คะแนน B ด้วย % ตามขนาดสาขา · กันหารศูนย์ทุกจุด

**ทำอะไรไปบ้าง:**
- **SQL ใหม่ `supabase/mgr_eval.sql`** — 3 ตาราง: `mgr_eval_config` (น้ำหนัก/เกณฑ์/threshold ปรับได้), `mgr_eval_business` (KPI คีย์มือต่อสาขา/รอบ), `mgr_eval_snapshots` (ตรึงผลสิ้นรอบ) + RLS + ค่าตั้งต้น · รันซ้ำได้
- **`shared/hr-api.js`** — เพิ่ม `hrMgrEval` (คำนวณ 2 คะแนน + normalize + reweight + บล็อก C ไฟจราจร), `hrMgrEvalConfigGet/Save`, `hrMgrEvalBusinessGet/Save`, `hrMgrEvalSnapshot`, `hrMgrEvalTrend` · ผูก action `hr_mgr_eval*` ในdispatch · ดึงข้อมูลจากตารางที่มีอยู่จริง (mgr_tasks/mgr_daily_logs/task_assignments/mgr_chat/attendance/handovers/goods_receipts/disc_actions/score)
- **`hr/index.html`** — แท็บใหม่ **"🏅 ประเมิน ผจก."** (กลุ่มพนักงาน): การ์ดเทียบทุกสาขา (Own/Team + ไฟจราจร KPI), โมดัลเจาะลึกที่มาคะแนน, ฟอร์มคีย์ KPI ธุรกิจ, ตั้งค่าน้ำหนัก, ปุ่มตรึงผล, **พิมพ์ใบประเมิน A4** (iframe+DOC_PRINT_CSS), **ปุ่มนิดาวิเคราะห์** · เพิ่ม `mgreval` ใน `ALL_TABS`+`NAV_GROUPS` · **ไม่อยู่ใน `MGR_TABS`** → ผจก. ไม่เห็น

**เฟส 2 (ทำต่อในรอบเดียวกัน):**
- **กราฟแนวโน้มย้อนหลัง** ในโมดัลเจาะลึก — วาด SVG เส้น Own/Team ข้ามรอบจาก `mgr_eval_snapshots` (ผ่าน `hr_mgr_eval_trend`) · รองรับค่าขาด (null) · ถ้ายังไม่มี snapshot แสดงคำแนะนำให้กดตรึงผล
- **นิดาอ่านผลประเมินได้เอง** — เพิ่ม tool `mgr_eval` ใน `hr-assistant` (อ่านจาก snapshots ที่ตรึงไว้ · แยก Own/Team + แนวโน้มเมื่อระบุ emp_id) + กฎใน SYS: รายงานแยก 2 คะแนนเสมอ ห้ามแต่งคะแนนเอง ถ้ายังไม่ตรึงผลให้บอกตรง ๆ · ไม่มี PIN/รหัสหลุด

### ⚠️ สิ่งที่ต้อง deploy รอบนี้
1. **รัน SQL:** `supabase/mgr_eval.sql` (ใน SQL editor ของ Supabase — รันซ้ำได้ปลอดภัย)
2. **ไฟล์ static → อัปโหลดทับขึ้น GitHub:** `shared/hr-api.js` · `hr/index.html` — แล้วรีเฟรช **Ctrl+Shift+R**
3. **Edge Function → redeploy:** `supabase functions deploy hr-assistant` (เพราะเพิ่ม tool `mgr_eval` ให้นิดา) · ตัวอื่นไม่ต้อง

> นิดาอ่านผลได้เฉพาะรอบที่ HR กด "💾 ตรึงผลรอบนี้" แล้ว (คะแนนสดดูในแท็บได้ตลอด) — ออกแบบให้ใช้ผลที่ตรึงเป็นทางการ ไม่ใช่ตัวเลขสดที่ยังเปลี่ยนได้

---

## 🗓 รอบ 21 ก.ค. 2026 — แก้บั๊กจัดกะ, แก้แชทนิดา, แก้ปุ่มวิเคราะห์เสี่ยง, เพิ่มตัวกรอง+บัญชีหน้าเบิกเงิน

> รอบนี้เน้น "แก้บั๊กที่เจอจากการใช้งานจริง" + เพิ่ม 2 ฟีเจอร์เล็กในหน้าเบิกเงิน
> ทุกจุดตรวจ logic ในแซนด์บ็อกซ์แล้ว · **ดู "สิ่งที่ต้อง deploy" ท้ายหัวข้อนี้ก่อนไปทำต่อเครื่องอื่น**

### 1) 🐛 บั๊ก "จัดกะไปแล้ว อยู่ดี ๆ พนักงานมีกะเพิ่มเอง (ควบกะ)"
**สาเหตุ:** ตาราง `schedules` ใช้ unique key `(emp_id, work_date, shift_id)` (รองรับควบกะ) → การ `upsert` กะที่ `shift_id` ต่างจากเดิม = "เพิ่มแถวใหม่" ไม่ใช่ "ทับ" · ไม่มี trigger/cron แอบเพิ่ม — มาจาก write paths ที่ไม่ล้างของเก่าก่อน
**แก้แล้ว:**
- `shared/hr-api.js → hrSchedCopy` (ปุ่ม "คัดลอกสัปดาห์ก่อนมาวาง"): เดิม upsert เฉย ๆ ทำให้กะเดิมในสัปดาห์ปลายทางค้างซ้อนกับที่คัดลอกมา → **แก้ให้ "ล้างกะสัปดาห์ปลายทางก่อน" แล้วค่อยคัดลอกทับจริง** (ตามคำว่า "วางทับ") · ปุ่มนี้เป็น **HR เท่านั้น** (ผจก. ถูกซ่อนที่บรรทัด ~5992) จึงล้างทั้งสัปดาห์ได้ปลอดภัย
- เพิ่ม **activity_log ทุกจุดที่เขียนกะ** เพื่อตามรอยครั้งหน้า: `hrSchedSave` ("บันทึกกะ"), `hrSchedFillWeek` ("จัดกะทั้งสัปดาห์"), `hrSchedCopy` ("คัดลอกตารางเวร (วางทับ)")
- `shared/supabase.js → addShiftMember` (หัวหน้าผลัดกด "เพิ่มคนเข้ากะ" ในแอป): เพิ่ม log พร้อม **ชื่อคนที่กดเพิ่ม** (รับ `byEmpId`/`byName` จาก `handover/index.html`)
**วิธีตรวจย้อนหลังในข้อมูลจริง:** ดู `note` ของแถวกะซ้ำ — `เพิ่มเข้ากะเฉพาะกิจ`=หัวหน้าผลัด · มีคำว่า `นิดา`=AI · ว่าง=หน้า HR · หรือ query `activity_log`
**ไฟล์:** `shared/hr-api.js` · `shared/supabase.js` · `handover/index.html`

### 2) 💬 แชทนิดา — ผจก. ตอบ "รับทราบ" แล้วนิดาเงียบ
**สาเหตุ:** ตั้งใจออกแบบ (กันคุยวน) — ใน `chat-reply/index.ts` ถ้าข้อความล่าสุดเป็นคำปิดท้าย (รับทราบ/โอเค/เรียบร้อย) + ไม่มีงานค้าง → เดิมเงียบทันที
**แก้แล้ว:** ปรับให้ **นิดาตอบปิดท้ายสั้น ๆ ได้ 1 ครั้งก่อนจบ** (เช็ก `nidaAlreadyClosed` — ถ้าข้อความก่อนหน้าของนิดายังไม่ใช่คำรับทราบ → ตอบปิดได้ 1 ที · ถ้าปิดไปแล้ว = เงียบ) · เบรกกันลูปตัวอื่นยังอยู่ครบ (นิดาตอบติด ≥5 ครั้งก็หยุด)
**ไฟล์:** `supabase/functions/chat-reply/index.ts` ⚠️ (Edge Function — ต้อง redeploy)

### 3) 🤖 ปุ่ม "ให้นิดาวิเคราะห์ความเสี่ยง" (หน้าเคสพิจารณาเลิกจ้าง) ขึ้น "นิดาตอบไม่ได้ในตอนนี้"
**สาเหตุ:** `termAskNida()` ส่ง payload ผิดรูป — ส่ง `{message, history}` และ **ไม่ส่ง `password`** → `hr-assistant` ตีกลับ 401 ไม่มี `reply`
**แก้แล้ว:** เปลี่ยนเป็น `{ password:pwd, messages:[{role:'user',text:prompt}] }` (ตรงกับปุ่มนิดาตัวอื่นที่ใช้ได้) · เช็กแล้วมีปุ่มนี้ปุ่มเดียวที่ส่งผิด
**ไฟล์:** `hr/index.html` (ฟังก์ชัน `termAskNida` ~บรรทัด 3900) — static

### 4) 💵 หน้าเบิกเงิน — เพิ่มตัวกรองสาขา + ปุ่มดูเลขบัญชี
- **ตัวกรองสาขา** (`<select id="adv_branch">`): แสดงเฉพาะสาขาที่มีคำขอในรอบ พร้อมจำนวนต่อสาขา · กรองทันทีไม่โหลดใหม่ · **KPI ด้านบนคำนวณตามสาขาที่กรองด้วย** (แยก `renderAdvKpi()` ออกมา, คงค่าที่เลือกด้วย `advFillBranchFilter()`)
- **ปุ่ม "🏦 บัญชี" ทุกแถว** → popup แสดง ชื่อบัญชี/ธนาคาร/เลขบัญชี (ตัวใหญ่) + ปุ่มคัดลอก · ข้อมูลมาจากสแนปช็อต `bank_name`/`bank_account` ในตาราง `advance_requests` (มีอยู่แล้ว ไม่ต้อง query เพิ่ม) · **แสดงเฉพาะ HR** (`ADV_CAN.pay`) — ผจก. ไม่เห็น (PDPA)
- **ไฟล์:** `hr/index.html` เท่านั้น (ฟังก์ชัน `advBank`, `advFillBranchFilter`, `renderAdvKpi`, `renderAdvRows`, ตัวแปร `advBranch`) — static

### ⚠️ สิ่งที่ต้อง deploy รอบนี้ (สำคัญตอนไปทำต่อเครื่องอื่น)
1. **ไฟล์ static → อัปโหลดทับขึ้น GitHub (Pages อัปเดตเอง 1–2 นาที):**
   `hr/index.html` · `shared/hr-api.js` · `shared/supabase.js` · `handover/index.html` — แล้วรีเฟรช **Ctrl+Shift+R**
2. **Edge Function → ต้องรัน CLI (อัป GitHub อย่างเดียวไม่พอ):**
   ```
   supabase functions deploy chat-reply --no-verify-jwt
   ```
   (ข้อ 2 หน้า "แชทนิดา" จะยังไม่มีผลจนกว่าจะ deploy)

### 📌 งานที่เสนอไว้แต่ยังไม่ได้ทำ (ทำต่อได้)
- เพิ่ม activity_log ให้ฝั่งนิดา (`hr-assistant` add_shift/change_shift) เพื่อตามรอยการจัดกะของ AI ให้ครบ
- `git init` โฟลเดอร์นี้ + push ขึ้น GitHub เพื่อ sync ข้ามเครื่องแทนการก๊อปทั้งโฟลเดอร์ (ตอนนี้ยังไม่ใช่ git repo)
- ปุ่มคัดลอกบัญชีแบบรวม "ธนาคาร + เลขบัญชี" ครั้งเดียว · ตัวกรองสาขาแบบเดียวกันในหน้าอื่น (รายงาน ฯลฯ) · log ตอน HR เปิดดูเลขบัญชี (PDPA)

---

## 🗓 รอบ 14 ก.ค. 2026 — ระบบเบิกเงินล่วงหน้า, งานเชลฟ์, ใบเตือนเข้าแชท, แก้การพิมพ์เอกสาร

### 1) 💵 ระบบเบิกเงินล่วงหน้า (Salary Advance) — ระบบใหม่ทั้งก้อน
**กติกา:** คีย์วันที่ 10–12 · HR อนุมัติวันที่ 15 · โอนเงินวันที่ 20–22 · หักคืนครั้งเดียวเต็มจำนวนในรอบเงินเดือนถัดไป · พนักงานยกเลิกเองได้ก่อนอนุมัติ

- **วงเงินคำนวณให้อัตโนมัติ ไม่ต้องรออนุมัติ**: `อัตราต่อวัน × วันทำงานจริงในรอบ × ตัวคูณวินัย` (ไม่เกินเพดาน 3,000)
  - ตัวคูณวินัยผูกกับ `score_bands` (คอลัมน์ใหม่ `advance_factor`): ปกติ ×1.0 · วาจา ×0.75 · ลายลักษณ์อักษร ×0.5 · ใบเตือน ×0.25 — HR แก้เองได้จากหน้าตั้งค่า
  - **โหมดช่วงเปลี่ยนผ่าน**: ปิดการคิดตามวันทำงาน (`use_days`) หรือกำหนด "วันทำงานขั้นต่ำที่ใช้คำนวณ" (`assume_days`) — แก้ปัญหาข้อมูลลงเวลายังไม่ครบตอนเริ่มใช้ระบบ
- **หน้าพนักงาน (หน้าแรก)**: ปุ่มโผล่เฉพาะ 10–12 · แสดงวงเงินทันทีพร้อมที่มาครบทุกบรรทัด · ช่องเงินล็อกไม่ให้เกินวงเงิน + ปุ่ม "เต็มวงเงิน" · ดูสถานะ/ยกเลิกคำขอเองได้
- **เบิกฉุกเฉิน**: ผจก. เปิดสิทธิ์ให้ (24 ชม.) → พนักงานเห็นปุ่ม **ในหน้าลงเวลาหลังใส่รหัส** พร้อมนับถอยหลัง · บังคับเหตุผล + แนบหลักฐาน + **สแกนใบหน้ายืนยันตัวตน** · HR อนุมัติเท่านั้น (ผจก. ไม่มีอำนาจอนุมัติเงิน = กันสมรู้ร่วมคิด) · จำกัดครั้ง/ปี · จ่ายภายใน 2 วันทำการ (ระบบคำนวณวันครบกำหนดให้ ไม่นับ ส-อา/วันหยุด)
- **หน้า HR (แท็บใหม่)**: KPI รออนุมัติ/รอโอน/โอนแล้ว · อนุมัติทีละใบ (ปรับลดยอดได้) หรืออนุมัติทั้งหมดในคลิกเดียว · บันทึกการโอน (เลขอ้างอิง + สลิป) · **สรุปหักเงินเดือน + Export CSV** · ตั้งค่าครบ
- **เก็บสแนปช็อตทุกคำขอ** (วันทำงาน คะแนน ตัวคูณ อัตรา วงเงิน เลขบัญชี ณ วันคีย์ + `deduct_month`) → ระบบเงินเดือนในอนาคตดึงไปหักคืนได้ทันที
- ไฟล์: `supabase/advance.sql`(ใหม่) · `shared/hr-api.js` · `shared/supabase.js` · `hr/index.html` · `index.html` · `employee/index.html` · แผนเต็ม: `แผน-ระบบเบิกเงินล่วงหน้า.md`

### 2) งานพิเศษ & เชลฟ์ (ฝั่งพนักงาน)
- **งานพิเศษที่ตรวจผ่านแล้วหายจากหน้างาน** (เดิมค้างเป็นการ์ดใหญ่ตลอดไป) เหลือแถบเขียวสั้น ๆ "ตรวจผ่านแล้ววันนี้" ที่หายเองวันถัดไป
- **เชลฟ์ที่รับผิดชอบเข้ามาอยู่ในหน้างานของฉัน** — ติ๊กเช็กลิสต์ ถ่ายรูป ส่งตรวจ ได้จากหน้าเดียว ไม่ต้องเข้าหน้าเชลฟ์แยก

### 3) หน้าตรวจเชลฟ์ HR/ผจก. — ตรวจง่ายบนมือถือ
- **กล่อง "📥 รอตรวจ" ขึ้นบนสุด** รวมทุกเชลฟ์ทุกสาขา (เดิมต้องเลื่อนผ่านทะเบียน/ฟอร์มมอบหมายยาว ๆ แล้วเปิดโมดัลทีละเชลฟ์)
- การ์ดเดียวเห็นครบ: เช็กลิสต์ผ่านกี่ข้อ + ข้อที่ยังไม่ติ๊ก · รูปใหญ่กดขยาย · ปุ่ม **ผ่าน/ตีกลับ เต็มความกว้าง** · ของค้างข้ามวันขึ้นขอบแดง
- **ปุ่มพับดูย้อนหลัง**: "✅ ตรวจผ่านแล้ว (14 วัน)" และ "⤴ ตีกลับ รอแก้"
- ส่วนทะเบียน/มอบหมายยุบเป็น "⚙️ ตั้งค่าเชลฟ์ & มอบหมาย" · ผจก. เห็นเฉพาะสาขาตัวเอง (บังคับที่ API ด้วย PIN จริง)
- API ใหม่: `hr_shelf_pending`

### 4) แดชบอร์ด — ไทม์ไลน์วินัยรายคน
- กลุ่มเสี่ยงวินัยเพิ่ม **ป้ายสถานะการดำเนินการ**: 🔴 ยังไม่ได้ออกใบเตือน · 🟡 ออกแล้วรอพนักงานรับทราบ · 🟢 รับทราบแล้ว · ⚖️ พิจารณาอยู่/มีมติแล้ว
- เพิ่ม **ไทม์ไลน์ย่อ "รอบนี้"** ใต้ชื่อแต่ละคน: `🗣️ วาจา 9 ก.ค. ✓ → 📝 ลายลักษณ์อักษร 12 ก.ค. ✓ → ➡️ ขั้นต่อไป: ออกใบเตือน` (ชี้เมาส์เห็นเหตุผล/เลขที่ใบ/ผู้ดำเนินการ) + ปุ่มเปิดไทม์ไลน์เต็ม
- เพิ่มการ์ด **เคสพิจารณาเลิกจ้าง** + KPI ในแดชบอร์ด · หน้าคะแนนเพิ่มคอลัมน์ "เคสพิจารณา"

### 5) 💬 ส่งใบเตือนเข้าแชท ผจก.
- ปุ่ม "ส่งเข้าแชท ผจก." (ในพรีวิวใบเตือน + ตารางใบเตือน) → แปลงเอกสารเป็นรูป (A4) แล้วส่งเข้าห้องแชทของสาขานั้น พร้อมข้อความสรุปและคำสั่งให้ ผจก. พิมพ์ให้พนักงานลงนาม + ยิง push
- ใบเตือนที่ถูกยกเลิกแล้วจะไม่มีปุ่มนี้

### 6) 🖨️ แก้การพิมพ์เอกสารทางการ (ใบเตือน / บันทึกเสนอพิจารณา)
ไล่แก้ 4 รอบกว่าจะจบ — บันทึกไว้เป็นบทเรียน:
- กระดาษเปล่า → เพราะ `.overlay` เป็น `position:fixed` + `.modal-b` มี `overflow:auto` (เบราว์เซอร์ตัดเนื้อหาทิ้ง)
- แถบเลื่อนติดไปในกระดาษ → `overflow:auto` ของกล่องต่าง ๆ
- หน้าเปล่า 6 หน้า → ซ่อนด้วย `visibility:hidden` ซึ่ง **ยังกินพื้นที่**
- **วิธีที่ใช้จริง (จบปัญหา): พิมพ์ผ่าน iframe แยกที่มี CSS ของตัวเอง** — เขียนเอกสาร HTML ใหม่ทั้งหน้าใน iframe (A4 210mm · padding 12/12/14mm · ตารางไม่ล้น · ห้ามตัดครึ่งแถว/ช่องเซ็นชื่อ · รอรูปโหลดก่อนพิมพ์) → กระดาษมีแต่เอกสาร ไม่ปนอะไรจากหน้าเว็บ

### 7) LINE แจ้งรับสินค้า
- **เอาการส่งรูปแยกตามหลังการ์ดออก** (ที่ใส่ไว้ตอนดีบัก) — เหลือ **การ์ด Flex ใบเดียว** ที่มีรูปอยู่ในการ์ดอยู่แล้ว
- คงไว้: ตรวจรูปก่อนส่งจริง (รูปที่ LINE ดึงไม่ได้จะไม่ใส่ในการ์ด) + การ์ดบอกชัดถ้ามีรูปแต่แสดงไม่ได้
- **แก้บั๊กรูปหายเงียบ ๆ เฉพาะบางเครื่อง**: `resizeFile()` ไม่มี `onerror`/timeout → เครื่องที่ถอดรหัส HEIC (ไอโฟน) ไม่ได้จะค้าง รูปหายโดยไม่มี error · แก้: มี fallback + timeout + **บังคับ output เป็น JPEG** + ฟ้องทันทีถ้ามีรูปใช้ไม่ได้ + ยืนยันก่อนบันทึกถ้าไม่มีรูป

### 8) 🤖 แก้พฤติกรรมนิดา 2 จุด
- **นิดา↔ผจก. (chat-reply) ตอบไม่ตรงคำถาม**: ผจก. ถามวิธีแก้/รายงานปัญหา แต่นิดาตอบ "รับทราบ + ทวงงาน" แทนที่จะช่วยแก้ · ต้นเหตุ: persona เป็น "ผู้ติดตามงาน" + คำสั่งรอบบอกแค่ "มีงานค้าง" ไม่แยกโหมด → เพิ่มตัวตรวจว่าผู้ใช้กำลัง "ถาม/รายงานปัญหา" ไหม (จับคำ ยังไง/วิธี/แก้/ไม่ได้/ทำไม/ช่วย/เมนูไหน/`?`) ถ้าใช่ → บังคับ "โหมดช่วยเหลือ": ตอบตรงคำถาม **ห้ามแปะการทวงงานท้ายข้อความ** + ยกกฎนี้เป็นข้อสำคัญสุดใน SYS
- **นิดาฝั่ง HR หาสาขาไม่เจอ**: ถามถึงสาขา (เช่น 06573 ตลาดหล่มสัก) แต่นิดาตอบ "ไม่พบสาขา" ทั้งที่มีจริง — เพราะตอบจากความจำ ไม่ได้ค้น DB · เพิ่มเครื่องมือ **`find_branch`** (ทนศูนย์นำหน้า 06573/6573 + ชื่อบางส่วน + เจอหลายสาขาให้ถามกลับ) และสั่งใน SYS ว่าต้องเรียกก่อนเสมอ ห้ามตอบ "ไม่พบ" โดยไม่เรียกเครื่องมือ
- ไฟล์: `supabase/functions/chat-reply/index.ts` · `supabase/functions/hr-assistant/index.ts` → **redeploy ทั้งสองตัว**

### 9) 📱 SMS (deeSMSX) — พร้อมใช้งาน
- ผู้ใช้ได้รับอนุมัติ Sender Name แล้ว · ตรวจโค้ด `sms-send` เทียบกับเอกสาร API จริง (readme.io) — **ตรงทุกจุด** ปรับแค่รูปแบบเบอร์เป็น `66XXXXXXXXX` ตามเอกสาร
- เพิ่ม **ปุ่มทดสอบส่ง SMS แยก** ในหน้าตั้งค่ากะ (การ์ด "📱 ทดสอบส่ง SMS") — ส่งเข้าเบอร์ตัวเองได้โดยไม่ต้องมีผู้สมัคร · แสดงผล ✅/❌ + เครดิตคงเหลือ + ข้อความจริงจาก deeSMSX
- ปุ่มส่งจริง (นัดสัมภาษณ์) อยู่ที่ หน้า HR → รับสมัครงาน → เปิดใบสมัคร → นัดสัมภาษณ์
- **ต้องทำก่อนใช้:** `supabase secrets set DEESMSX_API_KEY/SECRET_KEY/SENDER=...` → `supabase functions deploy sms-send --no-verify-jwt` (⚠️ key ที่เคยพิมพ์ในแชทควร revoke แล้วออกใหม่)

### 📦 ดีพลอยรอบนี้
1. **SQL**: `advance.sql`
2. **Edge Function**: `line-goods-notify` · `sms-send` · **`chat-reply`** · **`hr-assistant`** (`--no-verify-jwt`)
3. **ไฟล์**: `shared/hr-api.js` · `shared/supabase.js` · `hr/index.html` · `index.html` · `employee/index.html` · `handover/index.html`
4. เข้าแท็บเบิกเงิน → ⚙️ ตั้งค่า → ตรวจ **อัตราต่อวัน** (ค่าเริ่มต้น 150) และ **ตัวคูณวินัย** ให้ตรงกับนโยบายจริง

---

## 📌 แผนพัฒนาต่อ (ยังไม่เริ่มทำ)

### ประเมินผลงานผู้จัดการสาขา (Branch Manager Scorecard)
> เอกสารแผนเต็ม: **`แผน-ประเมินผลงานผู้จัดการสาขา.md`** — อ่านไฟล์นี้ก่อนเริ่มพัฒนา

- **สำรวจข้อมูลแล้ว**: ระบบมี "ร่องรอยการกระทำ" ของ ผจก. ใน 8 ตาราง — `mgr_tasks` (งาน HR มอบ) · `mgr_daily_logs` (งานประจำวัน) · `task_assignments.checked_by_emp/mgr_checked_at/mgr_result` (ตรวจงานในกะ) · `shelf_checks`/`special_task_assignees` reviewer (ตรวจเชลฟ์/งานพิเศษ) · `mgr_chat` (ตอบข้อความ) · `disc_actions.performed_role='mgr'` + `termination_cases.opened_role='mgr'` (จัดการวินัย) · `advance_windows.opened_emp` (เปิดสิทธิ์เบิก)
- **ข้อจำกัดที่ต้องยอมรับ**: ระบบ**ไม่มี**ยอดขาย/กำไร/waste/turnover → ประเมินได้แค่ "วินัยการบริหารงานภายใน + คุณภาพการดูแลทีม" เรียกว่า "คะแนนการปฏิบัติงาน" ไม่ใช่ KPI ยอดขาย
- **โครงคะแนน 100 (5 ด้าน)**: ความรับผิดชอบงานที่มอบ 25 · ตรวจงานคุมคุณภาพทีม 25 · ตอบสนอง/สื่อสาร 15 · ผลวินัยทีม 20 · ดำเนินงานหน้าร้าน 15
- **การตัดสินใจที่เคาะแล้ว (14 ก.ค.)**:
  - จุดประสงค์: ครอบทั้ง 3 (พัฒนา + เทียบสาขา + ประกอบโบนัส/เลื่อนตำแหน่ง)
  - ผลทีม: **นับรวมในคะแนน แต่แสดงแยก 2 คะแนน** (ผจก.ทำเอง / ผลทีม)
  - ความโปร่งใส: **เฉพาะ HR/สำนักงานเห็น** — ผจก. ไม่เห็นคะแนนตัวเอง (แท็บไม่โผล่ในเมนู ผจก. + API บล็อก)
- **หลักความเป็นธรรม**: normalize ตามขนาดสาขา (ใช้ % ไม่ใช่ตัวนับดิบ) · กำกับปัจจัยควบคุมไม่ได้ (พนักงานใหม่เยอะ) · แยกคะแนน ผจก.เอง vs ผลทีม
- **แผนทำงาน**: เฟส 1 = API `hr_mgr_eval` + ตารางเทียบสาขา + การ์ดเจาะลึก + นิดาวิเคราะห์ + พิมพ์เอกสาร (ทำได้ทันที ไม่ต้องเก็บข้อมูลเพิ่ม) · เฟส 2 = snapshot รายเดือน + กราฟแนวโน้ม + ช่องคีย์ตัวเลขธุรกิจด้วยมือ
- **ตารางที่จะเพิ่ม (เฟส 2)**: `mgr_eval_config` (น้ำหนัก/เกณฑ์) · `mgr_eval_snapshots` (ตรึงผลรายรอบ)

---

## 🗓 รอบ 12–13 ก.ค. 2026 — พิจารณาเลิกจ้าง, สิทธิ์ ผจก., แก้ข้อมูลเพี้ยนหลายจุด

### 1) ระบบ "พิจารณาเลิกจ้าง" (เสนอพิจารณาเท่านั้น — ระบบไม่เลิกจ้างเอง)
- แท็บใหม่ **⛔ พิจารณาเลิกจ้าง** (กลุ่มเมนู "วินัย") · ระบบ**ไม่**ปิดใช้งานพนักงาน ไม่หยุดจ่ายค่าจ้าง ปลายทางคือ "มติเสนอ + เอกสารหลักฐาน"
- **เกณฑ์เข้าข่าย ตั้งค่าเองได้** (`termination_rules`): คะแนนตกโซนต่ำสุด (≤25) · ใบเตือนครบ 2 ใบใน 12 เดือน · ขาดติดต่อกัน ≥3 วัน · ความผิดร้ายแรง (เปิดเคสเอง) · รองรับเพิ่ม ขาดรวม/สายรวมในรอบ
- **ขั้นตอน**: เสนอเรื่อง → HR ตรวจสอบ (ส่งต่อ/ขอหลักฐานเพิ่ม/ยุติ) → ผู้มีอำนาจให้ความเห็น (จบเรื่อง) · ทุกขั้นบังคับกรอกเหตุผล+ชื่อ เก็บเป็นไทม์ไลน์ · เคสที่ปิดแล้ว **แก้ไขมติ/เปิดใหม่ได้** (ไทม์ไลน์เดิมยังอยู่)
- **นิดาวิเคราะห์ความเสี่ยง** ทางกฎหมาย (เสี่ยงถูกฟ้องเลิกจ้างไม่เป็นธรรม) + ชี้จุดอ่อนของเคส
- **เอกสารทางการ** "บันทึกเสนอพิจารณาทางวินัย" (ใช้ส่วนหัวที่ตั้งค่าไว้ + รูปพนักงาน + ช่องลงนาม 3 ฝ่าย)
- พนักงานได้รับแจ้งทันทีที่ถูกเปิดเคส พร้อมสิทธิ์ชี้แจง
- ไฟล์: `supabase/termination.sql`(ใหม่), `shared/hr-api.js`, `hr/index.html`

### 2) สิทธิ์ผู้จัดการสาขา + แยกเมนู ผจก. ออกจาก HR
- ผจก. **ทำได้**: เห็นรายชื่อเข้าข่าย (เฉพาะสาขาตัวเอง) · **เสนอเปิดเคส** · บันทึกความเห็นเพิ่ม · พิมพ์เอกสาร
- ผจก. **ทำไม่ได้**: ตรวจสอบ/ชี้ขาด/ยกเลิก/เปิดใหม่/แก้เกณฑ์/เรียกนิดาวิเคราะห์ — บังคับสิทธิ์ที่ **ฝั่ง API** ด้วยการตรวจ PIN จริง (`mgr_login` RPC) ทุกคำสั่ง ไม่ใช่แค่ซ่อนปุ่ม · ชื่อผู้เสนอยึดจากผลตรวจ PIN ปลอมไม่ได้
- แก้บั๊กเมนู: ปุ่ม**กลุ่มเมนูของ HR ไม่เคยถูกซ่อน**ในโหมด ผจก. (ซ่อนแค่แท็บย่อย) · ผจก. ไม่ได้ใช้เมนูแบบกลุ่ม (ไม่เรียก `buildGroupedNav`) · หัวเรื่องยังขึ้น "สำหรับฝ่ายบุคคล"
- เพิ่มทางเข้า **`/mgr/`** สำหรับ ผจก. (เด้งเข้าฟอร์ม ผจก. ตรง ๆ ไม่เห็นหน้า HR) · ปุ่มหน้าแรกชี้ไปที่นี่
- ไฟล์: `shared/hr-api.js`, `hr/index.html`, `mgr/index.html`(ใหม่), `index.html`, `sw.js`(cache v3)

### 3) แก้ "นิดาฝั่ง HR ให้ข้อมูลเพี้ยน" — 3 บั๊กจริง
- `coreStats` ตัดสินระดับวินัยจาก `discipline_rules` ที่ถูกปิดใช้งานไปแล้ว → นิดารายงานว่าทุกคนปกติ ขัดกับหน้า HR · **แก้: ใช้คะแนน + `score_bands` ชุดเดียวกับหน้าเว็บ**
- `score_status` หักคะแนนขาดงานแบบไม่ถ่วงครึ่งวัน (หน้า HR ถ่วง 0.5) → คะแนนไม่ตรงกัน
- `app_data` โมดูล `rule_acks` เรียงด้วยคอลัมน์ `acked_at` ที่ไม่มีจริง (ชื่อจริง `accepted_at`) → query error
- แก้ SYS prompt + คู่มือของนิดาที่ยังท่องเกณฑ์เก่า ("สาย 3 ครั้ง = ตักเตือนวาจา") ให้เป็นเกณฑ์คะแนนจริง
- ไฟล์: `supabase/functions/hr-assistant/index.ts` → **redeploy**

### 4) นิดายกเลิก/ลบใบเตือนได้ (เดิมทำไม่ได้เลย)
- ต้นเหตุ: ตาราง `warnings` ไม่ได้อยู่ใน `WRITE_TABLES` ของนิดา
- เพิ่มเครื่องมือเฉพาะ **`warning_void`** (บังคับ warning_id + เหตุผล + ต้องกดยืนยัน) · ค่าเริ่มต้น = **ยกเลิก** (เก็บใบไว้เป็นหลักฐาน ไม่มีผลบังคับ) · **ลบถาวร** เฉพาะเมื่อสั่งชัดเจน
- หน้า HR: เพิ่มปุ่ม "🚫 ยกเลิก" + คอลัมน์สถานะ (รอรับทราบ/รับทราบแล้ว/ยกเลิกแล้ว)
- **แก้บั๊กข้อมูลค้าง**: ลบใบเตือนแล้วแถวใน `disc_actions` ไม่ถูกลบ → ประวัติวินัย/สรุปรายบุคคลยังโชว์ใบที่ลบไปแล้ว (แก้ทั้งโค้ด + SQL ล้างแถวค้าง)
- ไฟล์: `supabase/warning_void.sql`(ใหม่), `supabase/functions/hr-assistant/index.ts`, `shared/hr-api.js`, `shared/supabase.js`, `hr/index.html`

### 5) หน้าวิเคราะห์ดึงข้อมูลผิด — 4 บั๊ก
- **กรองสาขาผิดชั้น**: กรอง `attendance`/`schedules` ด้วย `branch_id` ของแถว → วันที่ไป "ทำแทนสาขาอื่น" ถูกตัดทิ้ง วันทำงานหาย · แก้เป็นกรองที่สาขาประจำของพนักงาน
- ไม่ถ่วงกะครึ่งวัน (นับ 1 วันเต็ม ต่างจากหน้ารายงาน/คะแนน) · ระดับวินัยยังอ่านจาก `discipline_rules` ที่ปิดไปแล้ว · งานถูกตีกลับนับซ้ำ (บวก `sent_back_count` แล้วบวกสถานะ sent_back อีก)
- ไฟล์: `shared/hr-api.js`

### 6) รายงาน: มุมมองใหม่ "สรุปแยกกะ"
- ตารางไขว้ พนักงาน × กะ (ทำกะไหนกี่วัน) + คอลัมน์รวม + คอลัมน์ไปแทน + ยอดรวมทุกคนต่อกะ · Export CSV ได้
- **แก้บั๊กตัวกรองกะ**: กรอง "ดึก" แล้วยังโชว์คนที่ไม่เคยเข้ากะดึก เพราะลูปตารางเวร/ใบลาไม่ได้กรองกะ (ดึงทุกคนกลับเข้ามาพร้อมเลขขาด/ลาของกะอื่น)
- ไฟล์: `shared/hr-api.js`, `hr/index.html`

### 7) รับสมัครงาน: วันที่นัดเริ่มงาน
- เดิม API เขียน `start_date = วันนี้` ตายตัว (วันที่กดปุ่ม) → อายุงาน/รายงานเพี้ยน · เพิ่มช่อง **วันที่นัดเริ่มงาน** (บังคับ) + เลือกกะประจำ ตอนกดรับเข้าทำงาน
- ไฟล์: `supabase/recruit_startdate.sql`(ใหม่), `shared/hr-api.js`, `hr/index.html`

### 8) ประกาศ: ปุ่มแก้ไข
- แก้ประกาศเดิมได้ทั้งข้อความ/รูป/ระดับ/สาขา/คำถาม (รูปเดิมไม่ต้องอัปใหม่) · มีตัวเลือก "ให้พนักงานรับทราบใหม่ทั้งหมด" (ล้างประวัติรับทราบ) · แก้เฉย ๆ ไม่ยิง push ซ้ำ
- ไฟล์: `shared/hr-api.js`, `hr/index.html`

### 9) หน้ากรอกข้อมูลพนักงาน: ดึงข้อมูลเดิมมาแสดง
- เดิมเติมให้แค่ชื่อ/ชื่อเล่น · เพิ่ม `getMyProfile()` ดึงครบทุกช่อง + **เอกสารเดิมแสดงเป็นรูปและไม่ต้องถ่ายใหม่** (ถ้าไม่ถ่ายทับ ระบบแนบไฟล์เดิมไปให้) + แถบสถานะ (รอตรวจ/อนุมัติ/ไม่ผ่าน)
- ไฟล์: `shared/supabase.js`, `staff/index.html`

### 10) เกณฑ์บวกคะแนนผลงานดี (สรุป)
- บวกได้เฉพาะจำนวนบวก · **ต้องกรอกเหตุผล** · เพดาน 20 คะแนน/ครั้ง (`score_bonus_max`) · แจ้งพนักงานทันที · นิดาแนะนำได้ว่าควรบวกกี่คะแนน (ดีมาก 10–20 / ดี 5–10)

### ⚠️ เหตุการณ์สำคัญ: ไฟล์ `hr/index.html` เคยพังทั้งไฟล์ (กู้คืนแล้ว)
- สคริปต์ค้นหา-แทนที่ผ่าน shell **อ่านไฟล์ไม่ครบแต่เขียนทับทั้งไฟล์** → ท้ายไฟล์หาย ~1,800 บรรทัด เว็บล่ม
- กู้คืนโดยต่อ "ส่วนหัวจากไฟล์ปัจจุบัน + ส่วนท้ายจากไฟล์บน GitHub" · ตรวจ syntax ผ่านแล้ว งานทุกชิ้นยังอยู่ครบ
- 📌 อ่าน **`บทเรียน-การแก้ไฟล์.md`** ก่อนแก้ไฟล์ใหญ่ทุกครั้ง

### 📦 ดีพลอยรอบนี้
1. **SQL** (Supabase SQL Editor): `termination.sql` · `warning_void.sql` · `recruit_startdate.sql`
2. **Edge Function**: `supabase functions deploy hr-assistant`
3. **อัปโหลดไฟล์**: `shared/hr-api.js` · `shared/supabase.js` · `hr/index.html` · `staff/index.html` · `index.html` · `mgr/index.html`(ใหม่) · `sw.js`
4. หลังอัปโหลด: เปิดเว็บแล้ว **Ctrl+Shift+R** หนึ่งครั้ง (service worker ขึ้นเวอร์ชันใหม่)

---

## 🗓 รอบ 9 ก.ค. 2026 — งาน ผจก.↔พนักงาน, ความปลอดภัยลงเวลา, แก้รูปติดแคช, งานส่งผลัด

### 1) มอบหมายงาน ผจก. → พนักงาน + รูปส่งกลับไทม์ไลน์ (สองทาง)
- ผจก. เลือกช่อง "มอบหมายให้" พนักงาน → งานไปโผล่ในหน้า "งานรับส่งผลัด" ของพนักงาน (การ์ด "📋 งานที่ผู้จัดการมอบหมาย") + แจ้งเตือนในกล่อง `emp_notifications`
- พนักงานถ่ายรูป/ใส่หมายเหตุ → ส่งเข้า **ไทม์ไลน์งาน ผจก.** (`mgr_task_feed` role='emp' บับเบิลเหลือง "พนักงาน 👷") เห็นแบบเรียลไทม์ทั้ง ผจก./HR · สถานะเด้งเป็น review อัตโนมัติ · ส่งความคืบหน้าได้หลายครั้งจนกว่าจะปิดงาน
- ไฟล์: `supabase/mgr_task_emp.sql`(ใหม่ — เพิ่ม assignee_emp/assignee_name/emp_photos/emp_note/emp_submitted_at), `shared/supabase.js`(getMyMgrTasks/submitMgrTaskByEmp), `shared/hr-api.js`(แจ้งพนักงานตอนมอบหมาย), `handover/index.html`(แสดง+ส่งรูป), `hr/index.html`(ไทม์ไลน์ role emp)
- **ดีพลอย:** รัน `supabase/mgr_task_emp.sql` · อัปโหลด `shared/supabase.js`,`shared/hr-api.js`,`handover/index.html`,`hr/index.html`

### 2) สวิตช์ปิด push งานผู้จัดการ (ตั้งค่า HR)
- แท็บตั้งค่า HR หัวข้อ "แจ้งเตือนงาน/ลงเวลา" เพิ่มสวิตช์ **"ปิดแจ้งเตือน push งานผู้จัดการ"** (setting `push_mgr_task_off`) · ปิดเฉพาะ push "งานใหม่จาก HR→ผจก." งานยังโผล่ในบอร์ด/ไทม์ไลน์ปกติ
- ไฟล์: `hr/index.html`, `supabase/functions/hr-notify/index.ts` · **ดีพลอย:** อัปโหลด `hr/index.html` + redeploy `hr-notify` (ไม่มี SQL)

### 3) วันสิ้นสุดการทำงาน (หน้าข้อมูลพนักงาน)
- เพิ่มช่องเลือกวันที่ "วันสิ้นสุดการทำงาน" ในฟอร์มแก้ไข/รายละเอียดพนักงาน + ป้าย "สิ้นสุดแล้ว/จะสิ้นสุด" · บันทึกเป็นข้อมูล (ไม่ปิดใช้งานอัตโนมัติ)
- ไฟล์: `supabase/emp_end_date.sql`(ใหม่ — คอลัมน์ `end_date`), `shared/hr-api.js`, `hr/index.html` · **ดีพลอย:** รัน SQL + อัปโหลด 2 ไฟล์

### 4) เหตุผลการลาเป็นช่องบังคับ (หน้าขอลา)
- หน้าขอลา (`index.html`): ถ้าไม่กรอกเหตุผล กดส่งไม่ได้ + ขึ้นข้อความเตือนสีแดง + โฟกัสช่องเหตุผล · **ดีพลอย:** อัปโหลด `index.html`

### 5) ปิดฟังก์ชันจำรหัสก่อนหน้า (กันลงเวลาผิดคน)
- ต้นเหตุ: `shared/session.js` จำ `hr_emp_id` แล้วเติมช่องรหัสอัตโนมัติทุกหน้า → บนแท็บเล็ตสาขาที่ใช้ร่วมกัน คนถัดไปเผลอลงเวลาเป็นคนก่อน · แก้: ปิดการจำ/เติมทั้งหมด (คงชื่อฟังก์ชัน no-op) + ล้างค่าเก่าตอนโหลด · **ดีพลอย:** อัปโหลด `shared/session.js`

### 6) บังคับเทียบใบหน้าตอนเช็คอิน
- เดิม `faceMatch` เป็นแค่ธง (soft flag) — ถ่ายเพดาน/คนอื่นก็ผ่าน · แก้: ถ้าลงทะเบียนใบหน้าไว้แล้วแต่หน้าไม่ตรง → **บล็อก** + ยกเกณฑ์ตรวจใบหน้าตอนกดส่ง (scoreThreshold 0.5→0.6) · คนยังไม่ลงทะเบียนยังเข้าได้ (แนะนำ HR ลงทะเบียนให้ครบ) · **ดีพลอย:** อัปโหลด `employee/index.html`

### 7) แก้รูปเช็คอิน "เป็นรูปเดิม" (ติดแคช)
- ต้นเหตุ: รูปเช็คอินเซฟที่ path ตายตัว `{รหัส}/{วันที่}.jpg` → ลบข้อมูลแล้วเช็คอินใหม่วันเดิมได้ URL เดิม เบราว์เซอร์/CDN แคชรูปเก่า
- แก้ 2 ชั้น: (ก) `shared/supabase.js` ใส่ timestamp ในชื่อไฟล์เช็คอิน (กันซ้ำอนาคต) · (ข) `hr/index.html` เติมตัวกันแคช `?v=เวลาเข้า/ออก` ตอนแสดงรูปเช็คอิน/เช็คเอาต์ (แก้เคสเก่าทันทีโดยไม่ต้องให้พนักงานลงเวลาใหม่) · **ดีพลอย:** อัปโหลด `shared/supabase.js`,`hr/index.html`

### 8) แก้ไขงานส่งผลัด (งานในกะ) หลังกดส่งได้
- เดิมกดส่งงานในกะแล้วแก้ไม่ได้ · แก้: สถานะ submitted ยังแก้/ส่งใหม่ได้จนกว่า HR จะตรวจ (ปุ่ม "อัปเดต/ส่งใหม่" + prefill หมายเหตุ + แสดงรูปที่ส่งไว้) · **ดีพลอย:** อัปโหลด `handover/index.html` (ไม่มี SQL — `submitTaskMulti` รองรับอยู่แล้ว)

> 🗓 5 ก.ค. 2026 — **แจ้งเตือนการเปลี่ยนคะแนนวินัย (เฟส 1)** + นิดาหัก/บวกคะแนนได้
> นิดา HR สั่ง "หัก/บวกคะแนน" ได้ (adjust_score, ค่าลบ=หัก บวก=เพิ่ม, ยืนยัน 2 จังหวะ) · เพิ่มกล่องแจ้งเตือนพนักงาน `emp_notifications` — เมื่อถูกหัก/บวกคะแนน (นิดา หรือหน้า HR) พนักงานจะได้ **ป๊อปอัพหลังสแกนหน้าเข้างาน (เด้งทีละอัน + ปุ่มรับทราบ)** + เห็นประวัติในหน้า "สถานะของฉัน" พร้อมเหตุผล
> ไฟล์: `supabase/emp_notifications.sql`(ใหม่), `shared/hr-api.js`, `shared/supabase.js`, `employee/index.html`, `hr-assistant/index.ts`
> **ดีพลอย:** รัน `supabase/emp_notifications.sql` · อัปโหลด `employee/index.html`+`shared/hr-api.js`+`shared/supabase.js` · redeploy `hr-assistant`
> เฟส 2 (ถัดไป): แจ้งคืนคะแนนตอนอนุมัติลาย้อนหลัง + guardrail กันหักซ้ำ auto · เฟส 3: push cron + ใส่คะแนนล่าสุดในข้อความ


> 🗓 5 ก.ค. 2026 — **เปลี่ยนการแสดงวันที่เป็น พ.ศ. ทั้งระบบ** (รูปแบบ "10 ก.ค. 2569")
> เพิ่มตัวช่วยกลาง `window.beDate()` / `window.beDateTime()` ใน `shared/config.js` (โหลดทุกหน้า) แล้วแปลงจุดที่แสดง ค.ศ. ดิบทั้งหมด (ใบลา/รายงาน/งานในกะ/รอบประเมิน/ใบเตือน/วิเคราะห์/หน้าพนักงาน) · formatter ที่ใช้ locale ไทย/​+543 อยู่แล้วเป็น พ.ศ. ตั้งแต่แรก · ช่องเลือกวันที่ (`input type=date`) ยังเป็น ค.ศ. ตามเบราว์เซอร์ · ดีพลอย: อัปโหลด `shared/config.js`, `hr/index.html`, `index.html`, `employee/index.html`

ย้ายออกจาก Google Apps Script มาเป็น Supabase ครบ ทดสอบผ่านจริง (เช็กอิน → ข้อมูลขึ้น HR dashboard)

---

## 🗓 รอบ 5 ก.ค. 2026 — อนุมัติลาแบบมีเงื่อนไข (เฟส 1)

ให้ HR ส่ง "ข้อเสนอแนะเพิ่มเติม" แทนการอนุมัติ/ปฏิเสธตรง ๆ (เช่น สลับวันหยุด + อนุมัติบางวัน) แล้วพนักงานกดยอมรับ/ปฏิเสธ · **HR แก้กะในตารางเอง** (ระบบไม่สลับกะอัตโนมัติ กันพลาด) แล้วกดอนุมัติเดิม
- **วงจร:** pending → HR ส่งข้อเสนอแนะเพิ่มเติม(ข้อความ) → `proposed` → พนักงานตอบ(ยอมรับ/ปฏิเสธ + เหตุผล) → HR แก้กะ+กดอนุมัติ → approved
- **แจ้งเตือนพนักงาน 3 จุด:** ป๊อปอัพหลังสแกนหน้าเข้างาน (เด้งทีละใบ) · แถบเตือนในหน้าขอลาเมื่อใส่รหัส · ประวัติในหน้า "สถานะของฉัน"
- **สื่อสารสองทาง:** ข้อความ HR→พนักงาน (`proposal_msg`) + เหตุผลพนักงาน→HR (`response_msg`) · **บังคับเหตุผลเฉพาะตอนปฏิเสธ**
- **ล็อก:** ระหว่าง proposed ยื่นใบลาใหม่ไม่ได้ (มีข้อความแจ้ง) จนกว่าจะตอบ
- HR เห็นผลตอบในแท็บ "ลา & วันหยุด" (ป้าย ✓ยอมรับ/✗ปฏิเสธ + ข้อความ) และปุ่ม "ข้อเสนอแนะเพิ่มเติม"/"เสนอแนะอีกครั้ง"

**ไฟล์:** `supabase/leave_conditional.sql` (ใหม่), `shared/hr-api.js` (`hr_leave_propose`), `shared/supabase.js` (`getLeaveProposals`/`respondProposal`+ล็อก), `employee/index.html` (ป๊อปอัพ), `hr/index.html` (ปุ่มเสนอ), `index.html` (แถบเตือน)

**⚠️ ต้องทำก่อนใช้:**
1. **รัน SQL** `supabase/leave_conditional.sql` (เพิ่ม 6 คอลัมน์ใน `leaves` + สถานะ `proposed`)
2. อัปโหลด GitHub: `hr/index.html`, `index.html`, `employee/index.html`, `shared/hr-api.js`, `shared/supabase.js`
- ไม่ต้องแตะ edge function · เฟส 2: push แจ้งเตือน 2 ทาง + "ให้นิดาร่างข้อความ"

### เฟส 2 — push 2 ทาง + นิดาร่างข้อความ + นิดาพนักงานเตือน
- **Push 2 ทาง (`hr-notify`):** HR เสนอ → push หาพนักงาน ("📩 ข้อเสนอแนะเพิ่มเติมจาก HR") · พนักงานตอบ → push หา HR (ยอมรับ/ปฏิเสธ + ข้อความ) · กันส่งซ้ำด้วย `notify_sent` (key `leaveprop:`/`leaveresp:`)
- **ให้นิดาร่างข้อความ (`hr/index.html`):** เปลี่ยนปุ่มเสนอจาก prompt → **โมดัล** มีช่องพิมพ์ + ปุ่ม **"✨ ให้นิดาร่าง"** (เรียก hr-assistant สร้างร่างจากข้อมูลใบลา แล้ว HR แก้ก่อนส่ง)
- **นิดาพนักงานเตือน (`emp-assistant`):** เพิ่ม tool `my_leave_proposals` — เช็กข้อเสนอที่ค้าง เตือนพนักงานให้ไปตอบ + ปุ่มพาไปหน้าขอลา
- **ดีพลอย:** redeploy `hr-notify` + `emp-assistant` (`--no-verify-jwt`) · อัปโหลด `hr/index.html` · ไม่มี SQL (ใช้คอลัมน์เฟส 1)

---

## 🗓 รอบ 5 ก.ค. 2026 — นิดาพนักงาน = "เจ้าหน้าที่ส่วนกลาง" (โหมดรายบุคคล เฟส A)

ต่อยอด `emp-assistant` ให้เป็นด่านหน้าที่คุยกับพนักงานได้ทุกมิติ **แบบอ่านอย่างเดียว + นำทาง** (ไม่ยื่น/ไม่เขียนข้อมูล) — กติกาที่ตกลง: พูดวินัยกว้าง ๆ · ทวงงานตามความเหมาะสม · แค่พาไป · ทุกมิติพร้อมกัน · ยืนยันตัวตนด้วยรหัสพนักงาน

**สิ่งที่เพิ่ม (`supabase/functions/emp-assistant/index.ts`):** โหมดรายบุคคล (รับ `emp_id`) + เครื่องมืออ่านของตัวเอง
- `my_leave` — โควตาเหลือแต่ละประเภทเดือนนี้ + กฎ + ประวัติลา → ปุ่มไปหน้าขอลา
- `my_attendance` — สรุป มา/สาย/ขาด/OT/ออกก่อน รอบนี้ (ข้อเท็จจริง)
- `my_standing` — สถานะวินัย **กว้าง ๆ** (ดี/ควรระวัง/ควรรีบปรับ) ไม่บอกคะแนน/ระดับใบเตือนเป็นตัวเลข → ชี้ไป "สถานะของฉัน"
- `my_schedule` — ตารางเวรวันนี้–7 วัน · `my_tasks` — งานในกะ/เชลฟ์ที่ค้างของฉัน → ปุ่มพาไปทำ
- `rules_status` — รับทราบระเบียบฉบับล่าสุดหรือยัง → ปุ่มไปหน้ารับทราบ
- คงเครื่องมือระดับสาขาเดิมไว้ครบ · SYS ใหม่: บุคลิก "เข้มงวดกับกฎ อบอุ่นกับคน" + ขอบเขต (เห็นเฉพาะข้อมูลตัวเอง, ไม่แตะ PDPA, ไม่ตัดสิน/อนุมัติเอง, เรื่องอ่อนไหวส่งต่อคนจริง)

**Client (`index.html`):** ส่ง `emp_id` (จาก `HRSession`) + `rules_version` ไปด้วย · เพิ่มชิปส่วนตัว (โควตาลา/กะของฉัน/งานของฉัน/สถานะ/วิธีขอลา) + ข้อความต้อนรับใหม่

**การดีพลอย:** ไม่มี SQL (ใช้ตารางเดิมทั้งหมด) · **redeploy** `supabase functions deploy emp-assistant --no-verify-jwt` + อัปโหลด `index.html`
- พนักงานต้องเคยกรอกรหัส (เช่น เปิดหน้าขอลา/สถานะของฉัน) เพื่อให้จำรหัสไว้ ฟีเจอร์รายบุคคลจึงทำงาน — ถ้ายังไม่มีรหัส นิดาจะขอให้กรอกก่อน
- นี่คือ **เฟส A** (ตอบ/แนะนำ/นำทาง) · เฟส B (คัดกรอง+ยื่น+ธงให้ HR) และ C (เตือนเชิงรุก+PIN) ทำต่อได้ — ดูเอกสารออกแบบ 2 ฉบับที่แนบไว้

---

## 🗓 รอบ 5 ก.ค. 2026 (ใหม่) — แท็บ "วิเคราะห์" (Analytics) + Export Excel/PDF

เพิ่มแท็บ **📈 วิเคราะห์** ในหน้า HR — รวม "วินัย + ความรับผิดชอบงาน" ไว้ที่เดียว วิเคราะห์เชิงลึกทุกคน/ทุกสาขา
- **ตัวกรองช่วงเวลา**: รอบปัจจุบัน (21–20) · รอบก่อนหน้า · 30 วันล่าสุด · เลือกช่วงเอง · + กรองสาขา
- **KPI รวม**: จำนวนพนักงาน, วันทำงานรวม, มาสาย(ครั้ง/นาที), ขาด, OT รวม, งานในกะผ่าน/ทั้งหมด, งานผ่านเฉลี่ย %, จำนวนคนเสี่ยงใบเตือน
- **กราฟแนวโน้มรายวัน**: มาตรงเวลา (เขียว) / มาสาย (แดง) ต่อวัน + tooltip (มา/สาย/ขาด/OT)
- **เทียบสาขา**: บาร์เรียงตามสายรวม + สถิติ (สาย/ขาด/OT/ผ่าน%)
- **สกอร์บอร์ดรายบุคคล (รวมวินัย+งาน)**: รหัส·ชื่อ·สาขา·วันทำงาน·สาย·นาทีสาย·ขาด·OT·**ระดับวินัย** · งานในกะ·**ผ่าน%**·ตีกลับ·**QA·เชลฟ์·ส่งผลัด** — คลิกหัวคอลัมน์เพื่อจัดเรียง · คลิกแถวเพื่อไปหน้า "สรุปรายบุคคล" ของคนนั้น
- **Export**: ⬇ Excel (.xlsx — 3 ชีต: รายบุคคล/เทียบสาขา/รายวัน ผ่าน SheetJS) · 🖨️ PDF (สั่งพิมพ์เฉพาะรายงาน)

**ไฟล์ที่แก้ (พัฒนาต่อ):**
- `shared/hr-api.js` — เพิ่ม action `hr_analytics` + ฟังก์ชัน `hrAnalytics()` (รวมข้อมูล att/sched/leave/task/qa/shelf/handover ต่อคน + ซีรีส์รายวัน + เทียบสาขา)
- `hr/index.html` — เพิ่มแท็บ+วิว+CSS+JS (`loadAnalytics/renderAnalytics/anRenderScoreboard/exportAnalyticsXlsx`) + โหลด SheetJS ผ่าน CDN

**การดีพลอย (ไม่ต้องแตะฐานข้อมูล):** ใช้ตารางเดิมทั้งหมด **ไม่มี SQL ใหม่** · แค่อัปโหลดขึ้น GitHub: `hr/index.html`, `shared/hr-api.js` (ต้องต่อเน็ตให้โหลด SheetJS จาก cdnjs สำหรับปุ่ม Excel)

### แก้เพิ่ม — HR ดูงานตรวจเชลฟ์รายวันได้แล้ว
- เดิม API `hr_shelf_checks` มีอยู่แต่ **หน้า HR ไม่เคยเรียกใช้** → HR เห็นแค่ตัวเลข "ตรวจแล้ว X วัน" เปิดดูรายละเอียดไม่ได้
- เพิ่มปุ่ม **📋 ดูการตรวจ** ที่หัวข้อเชลฟ์แต่ละอันในแท็บ "เชลฟ์ประจำเดือน" (รายการมอบหมาย) → เปิดป็อปอัพแสดงการตรวจรายวันที่พนักงานส่งมา: วันที่ · คนตรวจ · เช็กลิสต์ที่ติ๊ก (✓/○) · หมายเหตุ · **รูปถ่าย** (คลิกดูเต็ม)
- แก้ไฟล์เดียว `hr/index.html` (ไม่มี SQL/ไม่แตะ API เดิม)

### ระบบ "ผ่าน/ตีกลับ" การตรวจเชลฟ์ (แบบเดียวกับงานในกะ)
ให้ HR ตรวจงานดูแลเชลฟ์รายวัน → **ผ่าน** หรือ **ตีกลับ** พร้อมวาดชี้จุดบนรูปได้ · พนักงานเห็นเหตุผล + รูปที่วาด แล้วแก้ส่งใหม่
- **ฝั่ง HR** (ในป็อปอัพ 📋 ดูการตรวจ): แต่ละครั้งมีป้ายสถานะ (⏳รอตรวจ/✓ผ่าน/✗ตีกลับ) + ปุ่ม **✓ ผ่าน** และ **⤴ ตีกลับให้แก้** · กดตีกลับ = เปิด **เครื่องมือวาดวงกลม/กรอบ/ปากกา** ตัวเดียวกับงานในกะ (reuse `openMarkupEditor` ผ่าน `reviewFn`) + พิมพ์เหตุผล
- **ฝั่งพนักงาน** (หน้าเชลฟ์): การ์ดเชลฟ์แสดงสถานะ (ถูกตีกลับ/ผ่านแล้ว/รอตรวจ) · เปิดเข้าไปเห็น **แถบแดง "ถูกตีกลับ"** พร้อมเหตุผล + รูปที่ผู้ตรวจวาด → แก้เช็กลิสต์+ถ่ายรูปใหม่ → ส่งใหม่ (กลับเข้าคิวรอตรวจ)
- นับ `sent_back_count` (ถูกตีกลับกี่ครั้ง) · ส่งใหม่จะล้างผลรีวิวเดิมอัตโนมัติ

**ไฟล์ที่แก้:** `supabase/shelf_review.sql` (ใหม่), `shared/hr-api.js` (action `hr_shelf_check_review`), `hr/index.html` (ปุ่ม+โมดัล+reuse เครื่องมือวาด), `shared/supabase.js` + `shelf/index.html` (ฝั่งพนักงาน)

**⚠️ ต้องทำก่อนใช้:**
1. **รัน SQL**: `supabase/shelf_review.sql` บน Supabase (เพิ่มคอลัมน์ status/reviewer/review_note/reviewed_at/review_markup/sent_back_count ในตาราง `shelf_checks`) — แถวที่ตรวจไว้ก่อนหน้าจะกลายเป็น "รอตรวจ"
2. **อัปโหลด GitHub**: `hr/index.html`, `shelf/index.html`, `shared/hr-api.js`, `shared/supabase.js`
- หมายเหตุ: ลูปตรวจทำงานแบบ "รายวันเดียวกัน" (HR ตรวจงานของวัน พนักงานแก้ในวันเดียวกัน) — เหมาะกับงานเชลฟ์ที่เป็นงานประจำวัน

### แก้บั๊ก — แจ้งเตือน "เลยเวลาออกงาน" ผิด เมื่อ HR เปลี่ยนกะหลังพนักงานเช็กอินแล้ว
**อาการ:** พนักงานเช็กอินกะเช้า (จบ 17:00) → เกิดเหตุต้องย้ายไปกะ Delivery (จบดึกกว่า) → HR แก้กะในตารางหลังเช็กอิน → ระบบยังเด้งเตือน "ลืมกดออก/เลยเวลาเลิกกะ" ตอน 17:15 ทั้งที่กะใหม่ยังไม่เลิก

**สาเหตุ:** `attendance.shift_id` ถูก "ถ่ายรูปนิ่ง" ไว้ตอนเช็กอิน · `hrSchedSave` แก้แค่ตาราง `schedules` ไม่แตะ `attendance` · แต่ตัวจับ "เลยเวลาเลิกกะ" อ่านเวลาเลิกจาก `attendance.shift_id` (กะเก่า) ไม่ได้ดูตารางเวรปัจจุบัน — พลาด 3 จุด (auto-close ที่เขียนทีหลังทำถูกอยู่แล้ว)

**แก้:** ทำ 3 จุดให้ยึด "กะสุดท้ายจากตารางเวรปัจจุบัน" (fallback เป็นกะที่เช็กอินถ้าไม่มีเวร) + เคารพ `extend_until` (ควบกะต่อ) เหมือน auto-close:
- `supabase/functions/hr-notify/index.ts` — เหตุการณ์ push `nocheckout`
- `shared/hr-api.js` — `hrDashboard` (การ์ด "ลืมกดออก") + `hrNotifications` (พาเนลแจ้งเตือน HR)

**การดีพลอย:** ไม่มี SQL ใหม่ (ใช้คอลัมน์ `extend_until` เดิม) · อัปโหลด `shared/hr-api.js` ขึ้น GitHub + **redeploy edge function** `supabase functions deploy hr-notify --no-verify-jwt`
- นอกขอบเขตที่แก้รอบนี้ (ทางเลือกภายหลัง): ถ้าอยากให้ "รายงาน/สาย/OT" อิงกะใหม่ด้วย ต้องอัปเดต `attendance.shift_id` ตอน HR เปลี่ยนกะ — ตอนนี้ late_min ยังยึดค่าตอนเช็กอิน (แต่ OT ตอนกดออกคำนวณอิงตารางเวรถูกแล้ว)

### แก้ต้นตอถาวร — เปลี่ยนกะในตารางงานแล้ว "แถวลงเวลา" ตามให้อัตโนมัติ
**ปัญหาต่อเนื่อง:** หน้ารายงาน (สรุปต่อคน) กรองด้วย "กะ" แต่นับจาก `attendance.shift_id` (กะที่ค้างตอนเช็กอิน) · พอ HR ย้ายพนักงานไปอีกกะหลังเช็กอิน (เช่น อานนท์ → Delivery) รายงานกรองกะ Delivery จึงนับวันทำงาน = 0 ทั้งที่บอร์ด (อ่านจากตารางเวร) แสดงว่ามาแล้ว · และหน้า HR ไม่มี UI ให้แก้ `attendance.shift_id`

**แก้:** `hrSchedSave` (shared/hr-api.js) — หลังบันทึกตารางเวร ถ้ามีแถวลงเวลาที่ "เช็กอินแล้ว" ในวันนั้น และกะที่ค้างในแถว **ไม่ตรงกับตารางเวรปัจจุบัน** → อัปเดต `attendance.shift_id` ให้ตรงกะที่จัดใหม่อัตโนมัติ (กันแตะเคสควบกะที่กะเดิมยังอยู่ในตาราง) · บันทึก activity_log ไว้ตรวจสอบ
- ผลลัพธ์: HR เปลี่ยนกะที่หน้า **"ตารางงาน"** ที่เดียว → แถวลงเวลา + รายงาน(กรองกะ) + แจ้งเตือน "เลยเวลาเลิกกะ" ตรงกันหมด (ไม่ต้องแตะ Supabase/นิดา)
- โฟลว์ "เปลี่ยนกะ" ในหน้าตารางงาน = ลบกะเก่า → บันทึกกะใหม่ (scEditShift) → sync ทำงานตอนบันทึกกะใหม่พอดี

**การดีพลอย:** ไม่มี SQL · อัปโหลด `shared/hr-api.js` ขึ้น GitHub
- **การนับวันทำงาน = เรียลไทม์** (นับทันทีที่เช็กอิน ไม่ต้องรอจบวัน/เที่ยงคืน — `hrReport` บวก `days` เมื่อมี `check_in`)
- เคสที่ค้างอยู่ก่อนดีพลอยฟิกซ์นี้ (เช่น อานนท์วันนี้) แก้ครั้งเดียวด้วยนิดา ("แก้กะของ [รหัส] วันที่ ... เป็น Delivery") หรือจัดกะซ้ำอีกครั้งหลังดีพลอยเพื่อให้ sync ทำงาน

### แก้นิดา — รับ "ชื่อกะ" ได้ (ไม่ใช่แค่รหัส) + ซิงค์แถวลงเวลา
**ปัญหา:** สั่งนิดา "เปลี่ยนกะเป็น Delivery" → error FK `schedules_shift_id_fkey (Key (shift_id)=(Delivery) not present)` เพราะนิดายัดชื่อกะ "Delivery" เป็น `shift_id` ตรง ๆ (จริง ๆ รหัสคือ D)

**แก้ (`supabase/functions/hr-assistant/index.ts`):**
- เพิ่ม `resolveShiftId()` แปลง รหัส/โค้ด/ชื่อกะ (เช่น "Delivery" → "Delivery (วิ่งส่ง)") → `shift_id` จริง · ใช้ใน `add_shift`, `change_shift`, `edit_attendance` · หาไม่เจอตอบชัดเจนแทน error งง ๆ
- เพิ่ม `syncAttShift()` — เมื่อนิดา `add_shift`/`change_shift` แล้ว ถ้ากะในแถวลงเวลาไม่ตรงตารางเวรก็ปรับ `attendance.shift_id` ให้ (เหมือน `hrSchedSave` ฝั่งเว็บ) → ไม่ว่าจะเปลี่ยนกะทางเว็บหรือนิดา รายงาน/แจ้งเตือนตรงกันหมด
- อัปเดตคำอธิบาย tool ให้บอกว่า shift รับได้ทั้งรหัส/โค้ด/ชื่อ

**การดีพลอย:** ไม่มี SQL/ไม่แตะเว็บ · **redeploy edge function** `supabase functions deploy hr-assistant --no-verify-jwt`
- หลังดีพลอย: อานนท์แก้ได้ด้วย "แก้กะในแถวลงเวลาของ 0874771 วันที่ 5/7/2026 เป็น Delivery" (นิดาใช้ edit_attendance + แปลงชื่อ→รหัสให้เอง)

---

## 🗓 รอบ 4–5 ก.ค. 2026 — นิดา AI, รูปออกงาน, เครื่องมือวาดตรวจงาน, ปุ่มลอย, แก้บั๊กกะ

### A) นิดา AI (ผู้ช่วย HR + ผู้ช่วยพนักงาน)
- ✅ แก้ปัญหานิดา HR ตอบ "ไม่มีข้อมูลตอบกลับค่ะ" บ่อย → เกิดจากโหมด thinking ของ gemini-2.5-flash กิน token คำตอบหมด · แก้โดย **ปิด thinking (`thinkingBudget:0`) + เพิ่ม maxOutputTokens + retry อัตโนมัติ 1 ครั้ง** (ทั้ง hr-assistant + emp-assistant)
- ✅ แก้บั๊ก **สั่งลบเวลาออก แต่ลบทั้งเข้า+ออก** → `edit_attendance` เดิมกรอง null ทิ้ง (ล้างช่องไม่ได้) นิดาเลยไปใช้ `delete_attendance` (ลบทั้งแถว) · แก้: เพิ่มพารามิเตอร์ `clear:["check_out"]` ล้างเฉพาะช่อง + รีเซ็ตสถานะเป็น OPEN + สอนนิดาให้แยก "ลบเวลาออก" กับ "ลบทั้งวัน"
- ✅ แก้ error เพี้ยน "[object Object]" ตอนแก้ลงเวลา → เพิ่ม `errText()` แปลง error อ่านออก + `normTs()` แปลงเวลา "20:00" → timestamp เต็มอัตโนมัติ + **คำนวณ early_out ใหม่อัตโนมัติ** เมื่อแก้เวลาออก

### B) รูปถ่ายตอน "ออกงาน" (บังคับ)
- ✅ เดิมถ่ายรูปเฉพาะตอนเข้างาน → เพิ่มถ่ายเซลฟีตอนกดออกด้วย (บังคับ) เก็บใน `attendance.checkout_photo_url`
- ✅ HR ดูได้ทั้งรูปเข้า+ออก ที่ **บอร์ดวันนี้** (รูปย่อ เข้า=ขอบเทา/ออก=ขอบเขียว) และ **รายงาน (มุมมองรายละเอียด)** (📷 เข้า / 📷 ออก)

### C) ตัวกรอง + งานในกะ
- ✅ **หน้าพนักงาน**: เพิ่มตัวกรองสาขา · **บอร์ดวันนี้**: เพิ่มตัวกรองสาขา + กะ
- ✅ **งานในกะ (HR)**: ชิปสถานะแสดงครบ (รอตรวจ/ยังไม่ทำ/ตีกลับ/ผ่าน) + **กดชิปเพื่อกรอง** + เพิ่มตัวกรองกะ + จำนวนอิงตัวกรองสาขา/กะ

### D) เครื่องมือวาดชี้จุดตอนตีงานกลับ (สำคัญ)
- ✅ ตอนตีกลับ ผู้ตรวจ **วาดวงกลม/กรอบแดง/ปากกา** บนรูปที่พนักงานส่ง เพื่อชี้จุดที่ต้องแก้ (canvas editor) เก็บใน `task_assignments.review_markup`
- ✅ พนักงานเห็น "📍 จุดที่ต้องแก้" (รูปที่วาด) พร้อมข้อความ · รายงานเก็บทั้ง **รูปที่สั่งแก้** + **รูปที่แก้แล้ว**
- ✅ ใช้ได้ทั้งฝั่ง **HR** (งานในกะ) และ **หัวหน้าผลัดในแอป** (handover เมนู 3)

### E) กะ / ตารางเวร
- ✅ กันงานหลุดระบบตรวจ: ถ้าพนักงาน **ไม่มีกะ** จะบันทึกงานไม่ได้ (เตือนให้จัดกะ/เพิ่มเข้ากะก่อน)
- ✅ **หัวหน้าผลัดเพิ่มคนเข้ากะ** (รวม **ข้ามสาขา** — คนสาขาอื่นมาช่วย) → บันทึกเวรให้อัตโนมัติ นับเป็นวันทำงานปกติ ทำงาน/รับมอบหมายได้เหมือนสมาชิกกะ
- ✅ แก้บั๊ก **กะดึกข้ามคืนตรวจงานบ่ายเมื่อวานไม่ได้** → `_prevMainGroup` เดิมอิงวันปฏิทินจริง แก้ให้อิง work_date ของกะที่ทำ (รองรับข้ามคืน)

### F) ปุ่มลอยนิดา (เว็บ + APK Android)
- ✅ ปุ่มลอยใช้รูปมาสคอตนิดา + **เด้งขึ้นลง + วงแหวนกระเพื่อม** (ทั้งในเว็บและ native overlay ของ APK Capacitor)
- ✅ แตะปุ่มลอย = เปิด **หน้าแชทเต็มจอ** (ไม่เห็นหน้าหลัง) · ลากย้ายได้
- ✅ แก้บั๊ก **มือถือแตะปุ่มแล้วแชตไม่เปิด** → touch ยิงซ้อน mouse จำลอง (เปิดแล้วปิดทันที) แก้ด้วย preventDefault + ตัวกันเวลา 800ms

### ⚠️ ต้องทำหลังอัปเดตรอบนี้
1. **รัน SQL ใน Supabase** (ถ้ายังไม่ได้): `checkout_photo.sql`, `task_review_markup.sql`, `backfill_early_out.sql` (backfill ออกก่อนเวลาย้อนหลัง — ครั้งเดียว), `fix_breeze_afternoon_tasks.sql` (แก้เคสเฉพาะ — ครั้งเดียว)
2. **อัปโหลดขึ้น GitHub**: `index.html`, `hr/index.html`, `handover/index.html`, `employee/index.html`, `shared/supabase.js`, `shared/hr-api.js`
3. **Redeploy Edge Functions** (`--no-verify-jwt`): `hr-assistant`, `emp-assistant`
4. **APK**: ถ้าแก้ปุ่มลอย native ต้อง build ใหม่ (คู่มือใน `nida-android/`) · ส่วนแชทเต็มจอ/tap แก้ที่เว็บ อัป GitHub พอ ไม่ต้อง build

---

## ทำเสร็จแล้ว
- **Phase 0** — ฐานข้อมูล Supabase: 8 ตาราง + ฟังก์ชัน + RLS policies + Storage 2 buckets
- **Phase 1** — หน้าลงเวลาพนักงาน (`employee/index.html`): กล้อง + จดจำใบหน้า + GPS + เช็กอิน/เอาท์
- **Phase 2** — หน้า HR (`hr/index.html`): ครบ 5 แท็บ (Dashboard, พนักงาน, รายงาน, วินัย&ใบเตือน, ลา&วันหยุด)
- **ฟีเจอร์เสริม** — ปุ่ม "สถานะของฉัน" หน้าพนักงาน (ดูสถิติวินัยตนเองตามรหัส, ไม่แสดง OT)
- ปรับปุ่มสถานะ/ลงทะเบียนใบหน้าให้เป็นปุ่มชัดเจน (มีไอคอน ขอบ)

## โครงไฟล์
```
HR-7-eleven-main/
├── employee/index.html    # หน้าลงเวลาพนักงาน
├── hr/index.html          # หน้า HR (รหัสผ่าน: admin1234)
├── shared/
│   ├── config.js          # URL + anon key ของ Supabase (ตั้งค่าแล้ว)
│   ├── supabase.js        # ชั้นเชื่อมต่อ + selfStatus
│   └── hr-api.js          # backend หน้า HR (ทุก action)
├── supabase/              # ไฟล์ SQL (รันบน Supabase หมดแล้ว)
│   ├── schema.sql · seed.sql · functions.sql
│   ├── policies.sql · storage_policies.sql
│   └── SETUP.md
├── README.md              # วิธีรัน
└── แผนพัฒนาระบบบริหารบุคลากร-7Eleven.md   # แผนหลัก
```

## วิธีรันต่อ (ครั้งหน้า)
1. เปิด Command Prompt ที่โฟลเดอร์ `HR-7-eleven-main` (พิมพ์ `cmd` ที่ address bar ของ File Explorer)
2. พิมพ์ `python -m http.server 8000`
3. เปิดเบราว์เซอร์:
   - พนักงาน → http://localhost:8000/employee/
   - HR → http://localhost:8000/hr/  (รหัส `admin1234`)
   > ถ้า cmd เปิดผิดโฟลเดอร์ ให้เติม path: `localhost:8000/Downloads/HR-7-eleven-main/employee/`

## รอบล่าสุด (27 มิ.ย. 2026 — ต่อจากที่ค้าง)
- ✅ ยืนยันเกณฑ์วินัยตรงกัน 2 ไฟล์ (`hr-api.js` / `supabase.js`) — คงเกณฑ์เดิมตามที่ตัดสินใจ
- ✅ เตรียม SQL แก้รัศมี B002 → 20 ม. ที่ `supabase/fix_branch_radius.sql` (ต้องรันบน Supabase)
- ✅ เตรียม deploy GitHub Pages: `index.html` (หน้าเลือกเมนู), `.nojekyll`, `.gitignore`, `DEPLOY-GITHUB.md`
  (path เป็น relative อยู่แล้ว ใช้กับ Pages ได้เลย) — deploy แล้วที่ factzaa.github.io/HR-7-eleven/
- ✅ ทำเป็น PWA ติดตั้งบนมือถือได้: `manifest.webmanifest`, `sw.js`, โฟลเดอร์ `icons/`
  ผูก manifest + meta + register service worker เข้าทั้ง 3 หน้า (index/employee/hr)
  ⚠️ ไฟล์ใหม่เหล่านี้ต้องอัปขึ้น GitHub ด้วย: `manifest.webmanifest`, `sw.js`, `icons/` (ทั้งโฟลเดอร์)
     และอัป `index.html`, `employee/index.html`, `hr/index.html` ทับของเดิม
- ✅ สแตมป์เวลา/สถานที่บนรูปเช็กอิน (หน้า employee): เบิร์น overlay ลงรูปตอนกดเข้างาน
  เนื้อหา: เวลา (ตัวใหญ่) + วันที่ พ.ศ.ย่อ + วันในสัปดาห์ + ชื่อสาขา + พิกัด/ความแม่นยำ
  + ชื่อพนักงาน + สถานะ ในเขต/นอกเขต + ลายน้ำโลโก้ 7-Eleven (แก้ใน capturePhoto/drawStamp)
  เพิ่มความละเอียดรูปจาก 320px → 720px เพื่อให้ตัวอักษรคมชัด
  ⚠️ ต้องอัป employee/index.html ทับบน GitHub

## รอบ 28 มิ.ย. 2026 — จัดการสาขา + ตารางงาน + ไปทำแทนสาขา
- ✅ ตารางใหม่ `schedules` (ตารางเวร) — ไฟล์ `supabase/schedules.sql` (ยังไม่ได้รันบน Supabase)
- ✅ แท็บ "สาขา" 🏪 — CRUD สาขา (ชื่อ/lat/lng/รัศมี) + ปุ่มดึงพิกัด GPS + ลิงก์แผนที่
  API: `hr_branch_list/save/delete` (ลบไม่ได้ถ้ายังมีพนักงาน/ประวัติลงเวลา)
- ✅ แท็บ "ตารางงาน" 🗓️ — กริดรายสัปดาห์ (พนักงาน×7วัน) ตั้งกะ+สาขา, นำทางสัปดาห์, คัดลอกสัปดาห์
  API: `hr_sched_week/save/delete/copy` — ช่องสีส้ม = ไปแทนสาขาอื่น (auto เมื่อสาขา≠สาขาประจำ)
- ✅ ไปทำแทนสาขา: เช็กอินที่สาขาไหนก็ได้ในเขต geofence, ระบบ flag "ไปแทน" อัตโนมัติ (attendance.branch_id ≠ สาขาประจำ)
- ✅ วินัย/ขาดงาน อิงตารางเวร: ถ้ารอบมีตารางเวร → วันควรมา = วันที่จัดเวร, ไม่งั้น fallback weekly_off
- ✅ รายงานเพิ่ม: คอลัมน์ "ไปแทน" + สรุปวันประจำ/วันไปแทนต่อคน + มุมมอง "การไปทำแทนสาขา" รายวัน + export
- ไฟล์ที่แก้: `shared/hr-api.js`, `hr/index.html` · ไฟล์ใหม่: `supabase/schedules.sql`
  ⚠️ ต้องทำ 2 อย่าง: (1) รัน `supabase/schedules.sql` บน Supabase (2) อัป `shared/hr-api.js` + `hr/index.html` ขึ้น GitHub

## รอบ 28 มิ.ย. 2026 (2) — คีย์โค้ดจัดเวร + ตั้งค่ากะ + ลาออนไลน์ + แจ้งเตือน
- ✅ จัดตารางแบบ "คีย์โค้ด": พิมพ์โค้ดย่อในช่อง (1=เช้า 2=บ่าย 3=ดึก D=Delivery) Enter เพื่อบันทึก, ช่องว่าง=วันหยุด, คลิก "ไปแทน" ใต้ช่องเพื่อจัด cover
- ✅ แท็บใหม่ "ตั้งค่ากะ" ⚙️ — เพิ่ม/แก้/ลบกะ: โค้ดคีย์, ชื่อ, เวลาเข้า-ออก, ผ่อนผัน · API hr_shift_list/save/delete
- ✅ หน้าพนักงาน (index): ปุ่ม "ขอลา" + ฟอร์มส่งคำขอ → สถานะ "รออนุมัติ" + ดูสถานะคำขอของตนเอง (HR.requestLeave/myLeaves)
- ✅ HR อนุมัติ/ปฏิเสธใบลา ในแท็บ "ลา & วันหยุด" (ใบลารออนุมัติเด้งขึ้นก่อน) · API hr_leave_status
- ✅ แผงแจ้งเตือนแอดมินบน Dashboard: ใบลารออนุมัติ / คนยังไม่กดออก / มาสายวันนี้ / ขาด-ยังไม่มาตามตาราง · API hr_notifications
- ไฟล์แก้: shared/hr-api.js, hr/index.html, employee/index.html, shared/supabase.js · ไฟล์ใหม่: supabase/shift_codes.sql
  ⚠️ ต้องรัน `supabase/shift_codes.sql` บน Supabase (เพิ่มคอลัมน์ code + กะ D + เปิดสิทธิ์แก้กะ) แล้วอัปไฟล์ที่แก้ขึ้น GitHub

## รอบ 28 มิ.ย. 2026 (3) — โฮม + พนักงานกรอกข้อมูลเอง + ย้ายขอลา
- ✅ ปุ่ม 🏠 หน้าแรก ในหน้า employee, hr, staff (กลับไป landing index)
- ✅ ย้าย "ขอลา" จากหน้า employee ไปไว้ที่ "หน้าแรก" (index.html) แล้ว — มี modal + ดูสถานะคำขอ
- ✅ หน้าใหม่ `staff/index.html` — ลิงก์ส่งให้พนักงานกรอกข้อมูลเอง: กรอกรหัส → กรอกข้อมูลส่วนตัว/บัญชี + อัปเอกสาร 5 ชนิด (รูปถ่าย, สำเนาบัตร, สมุดบัญชี, ทะเบียนบ้าน, วุฒิ) → ส่งเข้าสถานะ "รอตรวจ"
- ✅ แท็บ HR ใหม่ "ข้อมูลรอตรวจ" 📥 — ดูข้อมูล+เปิดเอกสาร, กดอนุมัติ (เขียนเข้า employees) / ปฏิเสธ + เด้งในแผงแจ้งเตือน
- ไฟล์แก้: index.html, employee/index.html, hr/index.html, shared/hr-api.js, shared/supabase.js · ไฟล์ใหม่: staff/index.html, supabase/profile.sql
  ⚠️ ต้องรัน `supabase/profile.sql` (คอลัมน์เอกสาร + ตาราง profile_submissions + bucket employee-docs) แล้วอัปไฟล์ขึ้น GitHub

## รอบ 28 มิ.ย. 2026 (4) — เงื่อนไขการลา
- ✅ พนักงานตรวจสถานะคำขอลาได้ที่หน้าแรก (กรอกรหัส → เห็น รออนุมัติ/อนุมัติ/ปฏิเสธ + เหตุผลถ้าถูกปฏิเสธ)
- ✅ HR ปฏิเสธใบลาต้องระบุเหตุผล (prompt) → เก็บใน leaves.hr_note และพนักงานเห็น
- ✅ แท็บ "ตั้งค่ากะ" เพิ่มตาราง "เงื่อนไขการลา" (ตาราง leave_types): ตั้งลาล่วงหน้าขั้นต่ำ/โควตาต่อปี/ลาย้อนหลัง/ต้องแนบเอกสาร · API hr_leavetype_list/save
- ✅ ตรวจเงื่อนไขตอนพนักงานส่ง: ลาล่วงหน้าไม่พอ/เกินโควตา/ทับวันไปแทนสาขา → บล็อก พร้อมข้อความ
- ✅ ลาป่วยต้องแนบใบรับรองแพทย์ (require_doc) — ฟอร์มขอลาโชว์ช่องแนบรูป, บล็อกถ้าไม่แนบ, HR เปิดดูได้ (📎) เก็บใน leaves.doc_url
- ไฟล์แก้: index.html, hr/index.html, shared/hr-api.js, shared/supabase.js · ไฟล์ใหม่: supabase/leave_rules.sql
  ⚠️ ต้องรัน `supabase/leave_rules.sql` แล้วอัปไฟล์ขึ้น GitHub

## รอบ 28 มิ.ย. 2026 (5) — ตารางงาน: กรองสาขา + ส่งสรุป Line
- ✅ เลือกดูตารางตามสาขา (dropdown กรองพนักงานตามสาขาประจำ)
- ✅ ปุ่ม "คัดลอกข้อความ" — สรุปตารางทั้งสัปดาห์เป็นข้อความ (โค้ดกะต่อวัน + → ไปแทนสาขา) คัดลอกไปวางในกลุ่ม Line
- ✅ ปุ่ม "บันทึก/แชร์รูป (Line)" — render ตารางเป็นรูป PNG แล้วแชร์ผ่าน Web Share (มือถือเด้งเลือก Line ได้) หรือดาวน์โหลด
- หมายเหตุ: LINE Notify ปิดบริการแล้ว + เว็บเป็น static → ยังไม่ทำ auto-push (ถ้าต้องการต้องใช้ LINE Messaging API + backend)
- ไฟล์แก้: hr/index.html เท่านั้น (ไม่มี SQL ใหม่)

## รอบ 28 มิ.ย. 2026 (6) — หน้าระเบียบการทำงาน + รับทราบ
- ✅ หน้าใหม่ `rules/index.html` — แสดงระเบียบข้อบังคับ (โทนเข้ม เน้นวินัย/บทลงโทษ) + ช่องกรอกรหัส + ติ๊กยอมรับ → บันทึกการรับทราบ
- ✅ เพิ่มปุ่ม "📋 ระเบียบการทำงาน" ที่หน้าแรก
- ✅ บันทึกการยอมรับลงตาราง rule_acks (emp_id, version) · แสดงถ้ารหัสนั้นยอมรับแล้ว
- ร่างเอกสารต้นฉบับ: `ระเบียบการทำงาน-ร่าง.md`
- ✅ บังคับยอมรับก่อนใช้งาน: หน้า employee เด้ง popup ระเบียบ + บล็อกเช็กอินจนกว่าจะยอมรับ · หน้า staff ต้องติ๊กยอมรับก่อนส่ง
- ✅ เวอร์ชันระเบียบเป็นค่ากลางที่ `shared/config.js` (`window.RULES_VERSION`) — เปลี่ยนเลขนี้เมื่อมีฉบับใหม่ → พนักงานทุกคนต้องยอมรับใหม่อัตโนมัติ
- ไฟล์ใหม่: rules/index.html, supabase/rules_ack.sql · แก้: index.html, employee/index.html, staff/index.html, shared/supabase.js, shared/config.js
  ⚠️ ต้องรัน `supabase/rules_ack.sql`

## รอบ 29 มิ.ย. 2026 — เกณฑ์ใบเตือนปรับเองได้ + แก้การนับขาด
- ✅ แก้การนับ "ขาดงาน" ในวินัย: นับเฉพาะวันที่จัดเวรไว้และ **ผ่านไปแล้วจริง (ก่อนวันนี้)** — วันนี้ที่กะยังไม่จบจะไม่นับเป็นขาด (กันใบเตือนขึ้นทั้งบริษัท)
- ✅ หน้าตั้งค่า "เกณฑ์ใบเตือน / วินัย" ในแท็บตั้งค่ากะ — HR ปรับได้เองว่ามาสายกี่ครั้ง/ขาดกี่วันจึงได้ใบเตือนแต่ละระดับ + เปิด/ปิดแต่ละระดับ
- ✅ ลอจิกวินัยอ่านเกณฑ์จากตาราง `discipline_rules` แทนค่าตายตัว (ถ้ายังไม่มีตาราง ใช้ค่าเริ่มต้นเดิม) → ไม่ต้องแก้โค้ด 2 ที่อีกต่อไป
- ไฟล์แก้: `shared/hr-api.js`, `hr/index.html` · ไฟล์ใหม่: `supabase/discipline_rules.sql`
  ⚠️ ต้องรัน `supabase/discipline_rules.sql` แล้วอัป `shared/hr-api.js` + `hr/index.html` ขึ้น GitHub

## รอบ 29 มิ.ย. 2026 (2) — ครอป/เลื่อนตำแหน่งรูปพนักงาน
- ✅ เพิ่มเครื่องมือครอป (ลากเลื่อน+ซูม → จัตุรัส) ในฟอร์มแก้ไขพนักงาน · ปุ่ม "ปรับตำแหน่ง / ครอป"
- ✅ ใช้ได้ทั้งรูปใหม่และรูปเดิมที่อัปไปแล้ว (โหลดรูปเดิมจาก Storage ผ่าน CORS) · บันทึกเป็นรูปจัตุรัสแสดงผลถูกต้องทุกที่
- ไฟล์แก้: `hr/index.html` เท่านั้น (ไม่มี SQL ใหม่)

## รอบ 29 มิ.ย. 2026 (3) — ระบบคะแนนวินัยรายเดือน
- ✅ ทุกคนเริ่ม 100 คะแนน/เดือน (รอบ 21–20) · ตั้งค่าได้
- ✅ ตัดคะแนน **อัตโนมัติ** จากข้อมูลลงเวลา/ตารางเวร: สาย 1–10 (-3), 11–30 (-5), เกิน 30 (-10), ขาดไม่แจ้ง (-30)
- ✅ ตัดคะแนน **HR กดเพิ่มเอง**: แจ้งกะทันหัน<2ชม.(-15), ไม่ส่งมอบผลัด(-10), เกี่ยงงาน(-10..-20 เลือกได้), ทะเลาะ(-15) — เพิ่มเหตุ/ปรับคะแนน/ลบได้
- ✅ แท็บใหม่ "คะแนนวินัย" — คะแนนปลายเดือนทุกคน + แถบผล + โบนัส + รายละเอียดการตัด (เรียงจากน้อยไปมาก)
- ✅ แถบผล (ตั้งค่าได้): 90–100 โบนัส 1500 / 80–89 โบนัส 500 / 70–79 คุยปรับ / 60–69 ใบเตือน 1 / <60 ใบเตือน 2
- ✅ ปุ่ม "ออกใบเตือนตามคะแนน" — ออกใบเตือนอัตโนมัติตามแถบ (กันซ้ำต่อรอบ) เชื่อมกับระบบใบเตือนเดิม
- ✅ ตั้งค่าทั้งหมดในแท็บตั้งค่ากะ: คะแนนเริ่มต้น, กฎตัดคะแนน, แถบผล/โบนัส/ระดับใบเตือน
- ไฟล์แก้: `shared/hr-api.js`, `hr/index.html` · ไฟล์ใหม่: `supabase/score_system.sql`
  ⚠️ ต้องรัน `supabase/score_system.sql` แล้วอัป `shared/hr-api.js` + `hr/index.html` ขึ้น GitHub
  หมายเหตุ: โบนัสเป็นการ "แสดงผล/คำนวณสิทธิ" ระบบไม่จ่ายเงินจริง

## รอบ 29 มิ.ย. 2026 (4) — หน้า HR รองรับมือถือ + แก้ครอปรูป
- ✅ แก้ครอปรูป: หน้าต่างครอป z-index สูงกว่า modal แก้ไข (ไม่อยู่ข้างหลังแล้ว) · โหลดรูปเดิมจาก Supabase ผ่าน client เป็น object URL เลี่ยง CORS taint → ครอปรูปเก่าแล้วบันทึกได้จริง
- ✅ ปรับ responsive หน้า HR (≤640px): หัว/แท็บกระชับ, ตารางบีบช่อง+เลื่อนแนวนอนลื่น, KPI เล็กลง, Modal แบบเต็มจอพร้อมแถบปุ่มติดล่าง (กดง่าย), toolbar ปุ่ม/ดรอปดาวน์เต็มความกว้าง · จอ ≤380px ปุ่มใน modal เรียงเต็มแถว
- ไฟล์แก้: `hr/index.html` เท่านั้น (ไม่มี SQL ใหม่)

## รอบ 29 มิ.ย. 2026 (5) — Web Push แจ้งเตือนฝั่ง HR (เด้งแม้ปิดแอป)
- ✅ ตาราง `push_subscriptions` (อุปกรณ์ที่สมัคร) + `notify_sent` (กันส่งซ้ำ) — `supabase/push_notifications.sql`
- ✅ service worker (`sw.js`) รับ `push` + `notificationclick` (คลิกเปิดหน้า HR) · bump cache v2
- ✅ หน้า HR: ปุ่ม 🔔 เปิด/ปิดแจ้งเตือนต่ออุปกรณ์ (subscribe ด้วย VAPID → บันทึก Supabase) · VAPID public อยู่ใน `config.js`
- ✅ Edge Function `supabase/functions/hr-notify/index.ts` — คำนวณ ขาด/สาย/ลืมเช็กเอาต์/ใบลาใหม่/ข้อมูลรอตรวจ แล้วส่ง web push (รวมเป็นแจ้งเตือนเดียว กันซ้ำ ลบ subscription ที่หมดอายุ)
- ✅ cron `supabase/push_cron.sql` (ทุก 15 นาที ผ่าน pg_cron + pg_net) · คู่มือ `supabase/PUSH-SETUP.md`
- ไฟล์แก้: `sw.js`, `hr/index.html`, `shared/config.js` · ไฟล์ใหม่: push_notifications.sql, push_cron.sql, PUSH-SETUP.md, functions/hr-notify/index.ts
  ⚠️ ทำตาม `supabase/PUSH-SETUP.md`: รัน SQL → ตั้ง secret VAPID_PRIVATE → deploy function → เปิด pg_cron/pg_net → ตั้ง cron → กดปุ่ม 🔔 ที่อุปกรณ์
  หมายเหตุ: เฟสนี้ทำเฉพาะฝั่ง HR · ฝั่งแอปพนักงานวางแผนเพิ่มภายหลัง

## รอบ 29 มิ.ย. 2026 (6) — ส่ง/รับผลัด (Shift Handover)
- ✅ ตาราง `handovers` — `supabase/handover.sql`
- ✅ หน้าพนักงานใหม่ `handover/index.html` — เช็กลิสต์สภาพร้าน 3 หมวด (ความสะอาด/เติมสินค้า/ความเรียบร้อย) + งานค้าง + ปัญหา + แนบรูป · แบบ 2 ขั้น (ส่ง→รับยืนยัน) · ถ้าไม่มีผลัดรอรับ กดแจ้ง "ไม่มีการส่งผลัด"
- ✅ ลิงก์ "🔄 ส่ง/รับผลัด" บนหน้าแรก · ฟังก์ชันใน `shared/supabase.js` (submitHandover/getPendingHandover/receiveHandover/reportNoHandover) · ขยาย lookupEmployee ให้คืน branch_id/default_shift
- ✅ HR แท็บใหม่ "ส่ง/รับผลัด" — ประวัติทุกสาขา สถานะ เช็กลิสต์ งานค้าง รูป + ป้ายนับ "ไม่ส่งผลัด/ไม่เรียบร้อย/รอรับ" (`hr_handover_list`)
- ✅ แจ้งเตือน HR (push) เมื่อมี "ไม่มีการส่งผลัด" หรือ "รับผลัดไม่เรียบร้อย" — เพิ่มใน Edge Function `hr-notify`
- ไฟล์ใหม่: handover.sql, handover/index.html · แก้: index.html, shared/supabase.js, shared/hr-api.js, hr/index.html, functions/hr-notify/index.ts
  ⚠️ ต้องทำ: (1) รัน `supabase/handover.sql` (2) อัปไฟล์เว็บขึ้น GitHub (3) อัปเดตโค้ด Edge Function hr-notify ในหน้าเว็บ Supabase แล้วกด Deploy updates (เพราะเพิ่มแจ้งเตือนผลัด)

## รอบ 29 มิ.ย. 2026 (7) — ระบบงานในกะ (Shift Tasks)
- ✅ รายการงานตั้งค่าได้ (`task_defs`) — HR เพิ่ม/ลบ/แก้ในแท็บ "ตั้งค่ากะ" · ตั้งได้รายงานว่า "ต้องแนบรูป" ไหม
- ✅ มอบหมายงานรายบุคคล (`task_assignments`) — เลือกพนักงาน+วันที่+งาน ในแท็บ "ส่ง/รับผลัด"
- ✅ พนักงานส่งงาน (หน้า handover → "งานของฉันวันนี้") · ถ่ายรูปส่งงาน (ถ้างานนั้นต้องมีรูป) · งานที่ถูกตีกลับแสดงเหตุผลให้แก้แล้วส่งใหม่
- ✅ ตีงานกลับได้ทั้ง 2 ทาง: คนรับผลัด/หัวหน้า (หน้า handover → "ตรวจงาน") และ HR (แท็บ ส่ง/รับผลัด → งานในกะ) · อนุมัติ/ตีกลับพร้อมเหตุผล
- สถานะงาน: ยังไม่ทำ → รอตรวจ → ผ่าน / ตีกลับ (วนแก้ได้)
- ไฟล์ใหม่: task_system.sql · แก้: shared/supabase.js, shared/hr-api.js, hr/index.html, handover/index.html
  ⚠️ ต้องรัน `supabase/task_system.sql` แล้วอัปไฟล์เว็บขึ้น GitHub

## SQL ที่ต้องรันบน Supabase (รวมทุกรอบที่ยังไม่ได้รัน)
1. `schedules.sql` — ตารางเวร
2. `branches_rls.sql` — ให้เพิ่ม/แก้/ลบสาขาได้
3. `shift_codes.sql` — โค้ดกะ + กะ Delivery + ให้แก้กะได้
4. `profile.sql` — พนักงานกรอกข้อมูล/อัปเอกสารเอง + bucket เอกสาร
5. `leave_rules.sql` — เงื่อนไขการลา + เหตุผลปฏิเสธ + แนบใบรับรองแพทย์
6. `rules_ack.sql` — บันทึกการรับทราบระเบียบ
7. `activity_log.sql` — ตารางบันทึกกิจกรรม (audit log)
8. `discipline_rules.sql` — เกณฑ์ใบเตือน/วินัย (ปรับจากหน้า HR ได้)
9. `score_system.sql` — ระบบคะแนนวินัยรายเดือน (config/rules/bands/events)
10. `push_notifications.sql` — ตาราง Web Push (subscription + กันส่งซ้ำ)
11. `push_cron.sql` — ตั้ง cron เรียก Edge Function (หลัง deploy function + เปิด pg_cron/pg_net)
12. `handover.sql` — ส่ง/รับผลัด (เช็กลิสต์สภาพร้าน)
13. `task_system.sql` — ระบบงานในกะ (รายการงานตั้งค่าได้ + มอบหมาย + ส่งงาน/ตรวจ)
14. `shift_leads.sql` — ⚠️ ตารางหัวหน้าผลัด (เพิ่งพบว่าขาด ดูรอบตรวจระบบ 30 มิ.ย.)

## รอบ 30 มิ.ย. 2026 — ตรวจระบบหาบั๊ก (code review)
ตรวจ syntax ทุกไฟล์ JS, cross-check ตาราง/action/rpc/RLS ที่เรียก vs ที่มีจริง
- ✅ Syntax ผ่านหมด (shared/*.js + inline script ทั้ง 7 หน้า)
- ✅ ทุก action ที่หน้า HR เรียก มี handler ครบใน hr-api.js
- ✅ rpc (calc_late_min, hr_check_password) มีฟังก์ชันครบ
- ✅ ตารางใหม่ทุกตัวมี RLS policy ครบ
- 🔴 **พบบั๊ก: ตาราง `shift_leads` ขาดหายทั้งหมด** (ไม่มี CREATE TABLE / ไม่มี policy / ไม่มีไฟล์ SQL)
  แต่โค้ดเรียกใช้ 7 จุด (หัวหน้าผลัด, ส่ง/รับผลัด, ตรวจงานกะก่อน)
  → ไม่กระทบเช็กอินปกติ แต่ฟีเจอร์หัวหน้าผลัด/ส่งผลัดจะพัง
  → **แก้แล้ว: สร้าง `supabase/shift_leads.sql`** (ต้องรันบน Supabase)
- ⚠️ ข้อควรตรวจ: SQL 14 ไฟล์ในรายการด้านบน ต้องยืนยันว่ารันบน Supabase ครบแล้ว
  (ฟีเจอร์ที่อิงตารางซึ่งยังไม่ได้รัน จะ "อ่านได้ว่าง" แบบเงียบ ๆ ไม่ขึ้น error)

## รอบ 30 มิ.ย. 2026 (2) — แก้คำนวณสาย + เลิกใช้กะประจำ + บอร์ดวันนี้
- 🔴 แก้บั๊กคำนวณสาย: เดิม checkIn ใช้ default_shift (ว่าง) → ไม่บันทึกกะ → ไม่คิดสาย
  ✅ checkIn/checkOut ดึง "กะวันนี้" จาก `schedules` ก่อน (fallback กะประจำ) · ไฟล์ `shared/supabase.js`, `employee/index.html`
  ✅ SQL ย้อนหลัง `supabase/fix_attendance_shift_late.sql` — เติมกะ+คำนวณสายใหม่ให้แถวที่ลงไปแล้ว (ต้องรัน)
- ✅ เลิกใช้ "กะประจำ" (default_shift) ตามที่ใช้ตารางเวรวนทุกสัปดาห์: เอาช่องออกจากฟอร์มพนักงาน,
  ไม่เขียนทับคอลัมน์ตอน save, Dashboard "กะวันนี้" นับจากตารางเวรแทน (คอลัมน์ DB คงไว้ ไม่ลบ)
  ไฟล์ `hr/index.html`, `shared/hr-api.js` (ไม่มี SQL)
- ✅ แท็บใหม่ "🗒️ บอร์ดวันนี้" — เลือกวันได้, จัดกลุ่ม สาขา × กะ × คน + สถานะ (มาแล้ว/สาย/ยังไม่มา/ลา/ไปแทนสาขา/นอกตาราง) + สรุปนับด้านบน
  API `hr_board(date)` · ไฟล์ `shared/hr-api.js`, `hr/index.html` (ไม่มี SQL ใหม่)
  ⚠️ ต้องอัป `shared/supabase.js`, `shared/hr-api.js`, `hr/index.html`, `employee/index.html` ขึ้น GitHub + รัน `shift_leads.sql` และ `fix_attendance_shift_late.sql`

## รอบ 30 มิ.ย. 2026 (3) — ป๊อปอัพรับทราบสถานะหลังเช็กอิน (push)
- แนวคิด: เปลี่ยนจากให้พนักงานกดดูสถานะเอง (pull) → เด้งป๊อปอัพบังคับเห็น + กดรับทราบ (push)
- ✅ backend `HR.myStatus(empId)` (supabase.js): รวมสาย/ขาดรอบนี้ + ระดับวินัย (อ่านจากตาราง `discipline_rules` จริง) + ระยะถึงใบเตือนถัดไป + คะแนนเดือน (สูตรเดียวกับ hrScoreGet) + ระยะถึงโซนใบเตือน · resilient: ถ้าตารางวินัย/คะแนนยังไม่มี จะ fallback ไม่พัง
- ✅ `HR.acknowledgeStatus()` เก็บ log ลง `activity_log` (action 'รับทราบสถานะ') = หลักฐานว่าพนักงานเห็น/รับทราบทุกวัน
- ✅ หน้า employee: หลังเช็กอิน/ออกเด้ง modal `#ackOverlay` แสดงผลวันนี้ (ตรงเวลา/สาย) + สะสมรอบ + ระดับวินัย + คะแนน + "อีก N ครั้ง/คะแนน ถึงบทลงโทษ" · ปุ่ม "รับทราบ" หน่วง 2 วิ · หัวป๊อปอัพเปลี่ยนสีตามความรุนแรง
- ไฟล์แก้: `shared/supabase.js`, `employee/index.html` (ไม่มี SQL ใหม่ ใช้ discipline_rules/score_*/activity_log เดิม)
  ⚠️ อัป 2 ไฟล์นี้ขึ้น GitHub · ตาราง discipline_rules + score_system + activity_log ต้องรันไปแล้วถึงจะโชว์ครบ
  หมายเหตุ: ปุ่ม "สถานะของฉัน" เดิม (selfStatus) ยังใช้เกณฑ์วินัย hardcode — ถ้าอยากให้ตรงกับป๊อปอัพ (อ่าน discipline_rules) บอกได้ เดี๋ยวปรับให้

## รอบ 30 มิ.ย. 2026 (4) — แก้แจ้งเตือน "ยังไม่กดออก" เด้งเร็วเกินไป
- 🔴 บั๊ก: แจ้งเตือน "ยังไม่กดออก/ยังไม่เช็กเอาต์" เด้งทันทีหลังพนักงานเช็กอิน (ไม่ดูเวลาเลิกกะ) → สับสน
- ✅ แก้ให้เตือนเฉพาะ "เลยเวลาเลิกกะแล้วยังไม่เช็กเอาต์" (ใช้ pattern เดียวกับ absent ที่เช็กเวลาเข้ากะ) + กันกะข้ามคืนเตือนผิดวัน
- ✅ แก้ 3 ที่: `hr_notifications` (แผงแจ้งเตือน Dashboard), KPI "ลืมกดออก" บน Dashboard, Edge Function push `hr-notify`
- ✅ เปลี่ยนคำเป็น "ลืมกดออก (เลยเวลาเลิกกะแล้ว)" + แสดงเวลาเลิกกะ
- การเตือน "มาสาย" และป๊อปอัพหลังเช็กอินถูกต้องอยู่แล้ว (แจ้งเข้างาน+สายเท่านั้น)
- ไฟล์แก้: `shared/hr-api.js`, `hr/index.html`, `supabase/functions/hr-notify/index.ts`
  ⚠️ ต้องทำ: (1) อัป hr-api.js + hr/index.html ขึ้น GitHub (2) **Redeploy Edge Function `hr-notify`** บน Supabase (เพราะแก้ index.ts)
  ⚠️ เงื่อนไข: กะทุกตัวต้องตั้ง "เวลาออก (end_time)" ที่แท็บตั้งค่ากะ ไม่งั้นจะไม่เตือนลืมกดออกของกะนั้น (ปลอดภัย ไม่เตือนมั่ว)

## รอบ 30 มิ.ย. 2026 (5) — ประวัติแจ้งเตือน + ประกาศถึงพนักงาน + ล้างแถวค้าง
- ✅ แท็บใหม่ "🔔 ประวัติแจ้งเตือน" — รวม push ที่ส่งแล้ว (notify_sent แปลงเป็นไทย) + ประกาศที่ HR เขียน · API `hr_notify_history`
- ✅ ปุ่มเขียนประกาศถึงพนักงาน (ระดับ ทั่วไป/สำคัญ/ด่วน + วันหมดอายุ) · ตาราง `announcements` · API `hr_announce_save`/`hr_announce_delete`
- ✅ พนักงานเห็นประกาศ: แบนเนอร์บนหน้าลงเวลา + เด้งในป๊อปอัพหลังเช็กอิน (`HR.getAnnouncements`)
- ✅ ส่ง Web Push เมื่อกดประกาศ: เพิ่ม event `announce:<id>` ใน Edge Function hr-notify + หน้า HR ยิง invoke ทันทีหลังบันทึก (cron ตามส่งถ้าพลาด)
- ✅ SQL `fix_stuck_open.sql` — ปิดงานให้คน "ลืมกดออก" แบบปลอดภัย (ตั้ง check_out=เวลาเลิกกะ เฉพาะคนเลยเวลาเลิกแล้ว) ไม่ลบข้อมูลจริง · ห้ามใช้ DELETE
- ไฟล์: ใหม่ `supabase/announcements.sql`, `supabase/fix_stuck_open.sql` · แก้ `shared/hr-api.js`, `shared/supabase.js`, `hr/index.html`, `employee/index.html`, `supabase/functions/hr-notify/index.ts`
  ⚠️ ต้องทำ: (1) รัน `announcements.sql` + `fix_stuck_open.sql` (2) อัปไฟล์เว็บขึ้น GitHub (3) **Redeploy Edge Function hr-notify**
  ⚠️ ข้อจำกัด: Web Push ปัจจุบันส่งถึง "อุปกรณ์ที่สมัครไว้" = เครื่อง HR เท่านั้น (ปุ่ม 🔔 อยู่หน้า HR) · พนักงานเห็นประกาศผ่านแบนเนอร์+ป๊อปอัพได้เลย แต่ถ้าอยากให้ push เด้งบนมือถือพนักงานด้วย ต้องเพิ่มปุ่มสมัครแจ้งเตือนในแอปพนักงาน (ยังไม่ได้ทำ)

## รอบ 1 ก.ค. 2026 — เตรียมเปิดหลายสาขา (รอบ 1: กรองสาขา + เช็กอินผิดสาขา)
อ้างอิงเอกสาร `ตรวจสอบก่อนเปิดหลายสาขา.md`
- ✅ **(ข้อ 1) รายงาน "งานในกะ" กรอง/จัดกลุ่มสาขา** — เพิ่ม dropdown สาขา · "ทุกสาขา" จัดกลุ่ม 2 ชั้น (สาขา → กะ) มีหัวข้อสาขา
- ✅ **(ข้อ 2) หน้ามอบหมายงาน เพิ่ม dropdown สาขา** — กรองพนักงานตาม สาขา + กะ (กันมอบผิดสาขา/ผิดคน)
- ✅ **(ข้อ 3) เช็กอินกันผิดสาขา** — `evaluateZone` ยึด "สาขาประจำ" ก่อนถ้าอยู่ในรัศมี (กันจับผิดเมื่อสาขาใกล้กัน) · ถ้าระบบจับได้คนละสาขากับสาขาประจำ → เด้งยืนยัน "ไปแทนสาขา" ก่อนลงเวลา
- ไฟล์แก้: `hr/index.html`, `employee/index.html` (ไม่มี SQL/API ใหม่)
  ⚠️ อัป 2 ไฟล์นี้ขึ้น GitHub

## รอบ 1 ก.ค. 2026 (5) — กันใช้รูปซ้ำในงานผลัด + ลบงาน + เก็บกวาด UI
- ✅ **กันใช้รูปเก่า/ซ้ำ:** เอาปุ่ม "🖼️ แกลเลอรี" ออกจากการส่งงาน เหลือ "📷 ถ่ายรูป" (บังคับกล้อง capture=environment) · หมายเหตุ: EXIF ใช้ยืนยันไม่ได้เพราะ resize ผ่าน canvas ลบ EXIF ทิ้ง
- ✅ **ปั๊มวันเวลา + สาขา + ผู้ถ่าย** ลงบนรูปงานตอนถ่าย (แถบดำล่างรูป) → HR เห็นความสด/ตรวจย้อนได้ · ไฟล์ `handover/index.html` (resizeFile)
  ⚠️ ข้อจำกัด: บนเดสก์ท็อป capture ถูกละเว้น (เปิด file picker) — แต่แอปใช้บนมือถือร้าน กล้องบังคับได้
- ✅ **ลบงานในกะ:** ปุ่ม 🗑 ต่อแถวในรายงาน HR (`hr_task_delete`) + SQL `clear_task_assignments.sql` ลบทีเดียวตามวัน/สาขา/กะ
- ✅ **ลบส่วน "ส่ง/รับผลัด (สภาพร้าน)"** เก่าที่ไม่ใช้ออกจากแท็บ HR
- ไฟล์แก้: `shared/hr-api.js`, `hr/index.html`, `handover/index.html` · ใหม่: `supabase/clear_task_assignments.sql`
  ⚠️ อัป 3 ไฟล์เว็บขึ้น GitHub

## รอบ 1 ก.ค. 2026 (4) — กฎกันงานผลัดผิดเวลา/ข้ามกะ (แก้ปัญหางานกะดึกโผล่วันใหม่/กดซ้ำ)
สาเหตุ: พนักงานกดงานหลังกดออก/นอกเวลากะ → ระบบนับเป็นวันใหม่ สร้างงานซ้ำ
- ✅ **กฎ 1 (guard_prev_shift):** ห้ามเริ่มงานกะใหม่ ถ้างานของกะก่อนหน้า (วันก่อน กะเดียวกัน สาขาเดียวกัน) ยัง "ไม่ผ่าน" ครบ (todo/รอตรวจ/ตีกลับ = บล็อกหมด — เข้มสุดตามที่เลือก)
- ✅ **กฎ 2 (guard_shift_start):** ห้ามทำ/ดึง/ส่ง/แจกงาน ก่อนถึงเวลาเข้ากะพอดี (รองรับกะดึกข้ามคืน: ถ้ากำลังทำกะที่เริ่มเมื่อวานอยู่ = ทำได้)
- ✅ เสียบใน `doTaskSelf`, `pullTask`, `submitTaskMulti`(กฎ2), `assignColleague` · อ่านค่าจาก app_settings (anon อ่านได้)
- ✅ หน้า HR ตั้งค่า: เพิ่มสวิตช์เปิด/ปิด 2 กฎ (ดีฟอลต์เปิด) เผื่อฉุกเฉิน — เก็บใน app_settings (`guard_shift_start`,`guard_prev_shift`)
- ผลพลอยได้: กฎ 2 บล็อกเคส "กดงานซ้ำหลังกดออกตอนเช้า" อัตโนมัติ (เพราะยังไม่ถึงเวลาเข้ากะรอบใหม่)
- หมายเหตุ: การมอบหมายจากฝั่ง HR (แท็บ ส่ง/รับผลัด) ไม่ถูกบล็อก (HR เตรียมงานล่วงหน้าได้)
- ไฟล์แก้: `shared/supabase.js`, `hr/index.html` (ไม่มี SQL/ตารางใหม่ · ใช้ app_settings เดิม)
  ⚠️ อัป 2 ไฟล์นี้ขึ้น GitHub

## รอบ 1 ก.ค. 2026 (3) — เตรียมเปิดหลายสาขา (รอบ 3: กะดึก + push แยกสาขา + ลบรูปเก่า)
**#3 กะข้ามคืน (บั๊กจริง):**
- ✅ `checkOut` หาแถว "ที่ยังเปิดอยู่ล่าสุด (เช็กอินแล้วยังไม่ออก) ภายใน 2 วัน" แทนการล็อก work_date=วันนี้ → กะดึกเข้า 22:00 ออกตี 6 วันถัดไป เช็กเอาต์ได้แล้ว
- ✅ `todayAttendance` เผื่อกะข้ามคืน → ปุ่มยังเป็น "ออกงาน" หลังเที่ยงคืน · หน้าพนักงานเตือนถ้าไม่พบแถวค้าง
- ไฟล์: `shared/supabase.js`, `employee/index.html`

**#3.1 งานรับส่งผลัดกะดึก (ต่อเนื่องจาก #3):**
- ✅ ปัญหาเดียวกัน: หลังเที่ยงคืนระบบมองเป็นวันใหม่ → คนกะดึกหา/ส่ง/แจกงานของกะตัวเองไม่เจอ (งานอยู่ใต้ work_date เมื่อวาน)
- ✅ เพิ่ม `_shiftCtx(emp)` ใน supabase.js — ถ้ามีลงเวลาเปิดค้างจากเมื่อวาน (กะดึกยังไม่จบ) → ยึด work_date + ผลัดหลักของแถวนั้น
- ✅ เสียบเข้า: getMyAssignments, getShiftBoard, leaderInfo, leaderConfirm, pullTask, doTaskSelf, assignColleague (แจกงาน), addShiftMember (เพิ่มคน — รับ workDate จากบอร์ด)
- ไฟล์: `shared/supabase.js`, `handover/index.html`

**#3.2 เตือน "ลืมกดออก" รองรับกะดึก:**
- ✅ เพิ่ม `coOverdue(workDate,st,en,grace,today,nowMin)` — กะข้ามคืน deadline ตกเช้าวันถัดไป (en≤st → +1 วัน)
- ✅ Dashboard (still_open) + แผงแจ้งเตือน (not_checked_out) + Edge Function push: ดึงแถวที่ยังเปิดค้างจาก "เมื่อวาน" มารวมเช็กด้วย → คนกะดึกที่ลืมกดออกตอนเช้าจะถูกเตือนถูกต้อง (ไม่เด้งก่อนเลิกกะ)
- event key push ใช้ work_date ของแถว (กันเตือนซ้ำ/เพี้ยนข้ามวัน)
- ไฟล์: `shared/hr-api.js`, `functions/hr-notify/index.ts`
- หมายเหตุ: "ยังไม่ส่งงาน" แบบ push เชิงรุก ยังไม่มีในระบบ (ตอนนี้มีแค่ "ไม่มีการส่งผลัด" ของเช็กลิสต์เก่า) — ถ้าต้องการเตือนงานค้างก่อนหมดกะ เป็นฟีเจอร์ใหม่ค่อยเพิ่ม

**#3.3 เตือน "งานค้างก่อนหมดกะ" (ฟีเจอร์ใหม่ เชิงรุก):**
- ✅ Edge Function `hr-notify`: ช่วงก่อนเลิกกะ N นาที (ตั้งค่า `task_warn_before_min` ดีฟอลต์ 30) ถ้ายังมีงาน status todo/sent_back ในกะนั้น → push `⏰ ใกล้หมดกะ X สาขา Y: ยังมีงานค้าง N รายการ` · จัดกลุ่มตาม สาขา+กะ · รองรับกะดึก (deadline เช้าวันถัดไป) · ส่งเฉพาะเครื่องของสาขานั้น
- ✅ หน้า HR ตั้งค่า: เซกชัน "แจ้งเตือนงาน/ลงเวลา" เพิ่มช่อง "เตือนงานค้างก่อนหมดกะ (นาที)" (เก็บใน app_settings)
- ไฟล์: `functions/hr-notify/index.ts`, `hr/index.html`
  ⚠️ Redeploy `hr-notify` + อัป hr/index.html

**#1 Web Push แยกสาขา:**
- ✅ `push_branch.sql`: เพิ่ม `branch_id` ใน push_subscriptions (ว่าง=ส่วนกลางรับทุกสาขา)
- ✅ Edge Function `hr-notify`: แท็ก branch ใน event (สาย/ขาด/ลืมออก/ผลัด) + ส่งเฉพาะ sub ที่ตรงสาขา (ประกาศ/ใบลา/โปรไฟล์ = ส่งทุกเครื่อง)
- ✅ หน้า HR: ตอนกดสมัครแจ้งเตือน เลือกสาขาของอุปกรณ์ได้
- ไฟล์: `supabase/push_branch.sql`, `functions/hr-notify/index.ts`, `hr/index.html`

**#2 นโยบายลบรูปเก่า (Storage retention):**
- ✅ Edge Function `storage-cleanup`: ลบรูปเช็กอิน + รูปงาน(task/) + รูปผลัด(handover/) ที่เก่าเกิน RETENTION_DAYS (ดีฟอลต์ 90 วัน) · **ไม่แตะเอกสารโปรไฟล์/ใบรับรองแพทย์/รูปโปรไฟล์**
- ✅ `storage_cleanup_cron.sql`: cron วันละครั้ง ตี 3
- ไฟล์: `functions/storage-cleanup/index.ts`, `supabase/storage_cleanup_cron.sql`

⚠️ ต้องทำเพื่อใช้งานรอบนี้:
1. รัน `push_branch.sql`
2. อัปขึ้น GitHub: `shared/supabase.js`, `employee/index.html`, `hr/index.html`
3. **Redeploy Edge Function `hr-notify`** (แก้ index.ts) · ผูก subscription เดิม branch_id=null ไว้ก่อน (รับทุกสาขา) แล้วค่อยให้แต่ละร้านกดสมัครใหม่เลือกสาขา
4. **Deploy Edge Function ใหม่ `storage-cleanup`** + รัน `storage_cleanup_cron.sql` (เปิด pg_cron/pg_net)
- 🟢 #4 โหลดช่วงเปลี่ยนกะ = ความเสี่ยงต่ำ (upsert idempotent) — ไม่ต้องแก้โค้ด แค่ทดสอบโหลด

## รอบ 1 ก.ค. 2026 (2) — เตรียมเปิดหลายสาขา (รอบ 2: กรองสาขา Dashboard + แจ้งเตือน)
- ✅ **(ข้อ 4) กรองสาขาบน Dashboard** — เพิ่ม dropdown "สาขา" บนหัว Dashboard · `hr_dashboard(branch)` กรอง attendance/schedules/employees/trend/cycle ตามสาขา · Scorecard + Watchlist (จาก hr_discipline) กรองฝั่ง client (เพิ่ม branch_id ใน output) · การลาที่จะถึงกรองตามสาขา
- ✅ **(ข้อ 5) กรองสาขาในแผงแจ้งเตือน** — `hr_notifications(branch)` กรอง สาย/ขาด/ลืมออก/ใบลารอ/โปรไฟล์รอ ตามสาขา · ใช้ dropdown สาขาตัวเดียวกับ Dashboard (เปลี่ยนแล้วรีเฟรชทั้งคู่)
- ไฟล์แก้: `shared/hr-api.js`, `hr/index.html` (ไม่มี SQL ใหม่)
  ⚠️ อัป 2 ไฟล์นี้ขึ้น GitHub
- ✅ ครบทั้ง 5 ข้อจากเอกสาร "ตรวจสอบก่อนเปิดหลายสาขา" (ยกเว้นข้อ 6 เวลากะรายสาขา — ข้ามได้ถ้าทุกสาขาเวลาเท่ากัน)

## รอบ 30 มิ.ย. 2026 (10) — รื้อรายงาน "งานในกะ" ฝั่ง HR ให้ดูง่าย + วิเคราะห์ได้
- ✅ เปลี่ยนจากตารางแถวเดียว → การ์ด **จัดกลุ่มตามกะ** (เรียงตามเวลาเริ่มกะ)
- ✅ หัวการ์ดแต่ละกะมีสรุปวิเคราะห์: ทั้งหมด/ผ่าน(%)/รอตรวจ/ตีกลับ/ยังไม่ทำ
- ✅ แต่ละงานแสดงรายละเอียดครบ: ผู้ทำ · สาขา · **เวลาส่งงาน** · **ผู้ตรวจ + เวลาที่ตรวจ** · จำนวนครั้งที่เคยตีกลับ · หมายเหตุผู้ตรวจ
- ✅ รูปภาพย้ายไปอยู่ **ด้านล่างใต้ข้อมูล เรียงแถวแนวนอน** (เลื่อนซ้าย-ขวาได้) กดดูเต็ม/แกลเลอรีได้
- ✅ ปุ่ม ผ่าน/ตีกลับ ยังอยู่ครบ (ปรับสถานะได้จากในรายงาน)
- ไฟล์แก้: `hr/index.html` เท่านั้น (ข้อมูลครบใน hr_task_list อยู่แล้ว ไม่มี API/SQL ใหม่)
  ⚠️ อัป hr/index.html ขึ้น GitHub

## รอบ 30 มิ.ย. 2026 (9) — เปลี่ยนรหัสพนักงานได้ (ย้ายประวัติทั้งหมด)
- ✅ รหัสพนักงานเป็นคีย์หลัก ถูกอ้างอิงหลายตาราง — แก้ตรง ๆ ไม่ได้ · ทำฟังก์ชัน `change_emp_id(old,new)` (SECURITY DEFINER) ย้ายข้อมูลทุกตารางใน 1 ทรานแซกชัน (attendance/schedules/leaves/warnings/profile_submissions/rule_acks/shift_leads/activity_log/score_events/task_assignments/handovers) แล้วลบแถวเดิม — พลาด=ย้อนกลับหมด
- ✅ หน้าแก้ไขพนักงาน: ปุ่ม "🔁 เปลี่ยนรหัส" (โผล่เฉพาะตอนแก้ไข) → prompt รหัสใหม่ + ยืนยัน → `hr_change_emp_id`
- ✅ ป้องกัน: รหัสใหม่ว่าง/ซ้ำ/เหมือนเดิม จะแจ้งเตือน · บันทึก activity_log
- ไฟล์ใหม่: `supabase/change_emp_id.sql` · แก้: `shared/hr-api.js`, `hr/index.html`
  ⚠️ ต้องทำ: (1) รัน `change_emp_id.sql` (2) อัป hr-api.js + hr/index.html ขึ้น GitHub

## รอบ 30 มิ.ย. 2026 (8) — ตั้งค่าผ่อนผันก่อนเตือน "ลืมกดออก" (นาที)
- ✅ ตาราง `app_settings` (key/value, anon อ่าน/เขียนได้) เก็บค่าปรับทั่วไป · ค่าแรก `checkout_grace_min=15`
- ✅ เตือน/นับ "ลืมกดออก" เมื่อเลยเวลาเลิกกะ **+ N นาที** (ไม่ใช่ทันทีที่ถึงเวลาเลิก) มีผล: Dashboard, แผงแจ้งเตือน, Push (Edge Function)
- ✅ หน้า HR → ตั้งค่า → เซกชัน "แจ้งเตือนลืมกดออก" ปรับนาทีได้ (`hr_settings_get`/`hr_settings_save`)
- ไฟล์ใหม่: `supabase/app_settings.sql` · แก้: `shared/hr-api.js`, `hr/index.html`, `supabase/functions/hr-notify/index.ts`
  ⚠️ ต้องทำ: (1) รัน `app_settings.sql` (2) อัป hr-api.js + hr/index.html ขึ้น GitHub (3) **Redeploy Edge Function hr-notify** (เพื่อให้ push ใช้ค่าผ่อนผันเดียวกัน)

## รอบ 30 มิ.ย. 2026 (7) — รื้อ Dashboard: ตัดกราฟ 30 วัน เพิ่ม 4 การ์ดที่ใช้งานได้จริง
- ✅ ตัดการ์ด "แนวโน้ม 30 วัน" (รายวัน อ่านยาก/โล่ง) ออก
- ✅ **Scorecard รอบนี้**: % มาตรงเวลา · สายรวม(ครั้ง/นาที) · ขาด · OT รวม (คำนวณจาก hr_discipline)
- ✅ **กลุ่มเสี่ยงวินัย**: รายชื่อคนที่ level≥1 รอบนี้ + ป้ายระดับ(สีตามเกณฑ์) + สาย/ขาด (จาก hr_discipline)
- ✅ **แนวโน้มรายสัปดาห์**: รวม trend รายวันเป็นสัปดาห์ (แท่งน้อยลง อ่านง่าย)
- ✅ **การลาที่จะถึง (14 วัน)**: ลาที่อนุมัติแล้วและกำลังจะถึง — `hr_dashboard` เพิ่ม query `upcoming_leaves`
- คง Top 5 คนสาย + สาขาวันนี้ + กะวันนี้ ไว้
- ไฟล์แก้: `shared/hr-api.js` (hr_dashboard เพิ่ม upcoming_leaves), `hr/index.html` (loadDashboard เรียก hr_discipline ควบ) — ไม่มี SQL ใหม่
  ⚠️ อัป 2 ไฟล์นี้ขึ้น GitHub

## รอบ 30 มิ.ย. 2026 (6) — Dashboard การ์ดกดดูรายชื่อ + บอร์ดวันนี้แสดงเวลาออก
- ✅ การ์ด KPI บน Dashboard กดได้ 3 ใบ → เปิด modal รายชื่อ: "ลงเวลาแล้ว" (เข้า/ออก/ยังไม่ออก), "สายวันนี้" (สายกี่นาที+เวลาเข้า), "ลืมกดออก" (เวลาเข้า+เวลาเลิกกะ) · แต่ละชื่อบอกรหัส/กะ/สาขา
- ✅ `hr_dashboard` คืน `lists.{checked_in,late,still_open}` (เพิ่ม check_out ใน query + แก้บั๊ก still_open ที่ไม่ได้ดึง check_out)
- ✅ บอร์ดวันนี้ (hr_board) แต่ละคนแสดง "→ ออก HH:MM" หรือ "· ยังไม่ออก" ต่อจากเวลาเข้า (เพิ่ม check_out ใน query + statusOf)
- ไฟล์แก้: `shared/hr-api.js`, `hr/index.html` (ไม่มี SQL ใหม่)
  ⚠️ อัป 2 ไฟล์นี้ขึ้น GitHub

## รอบ 11 ก.ค. 2026 — แชท ผจก.↔ผจก. + สร้างสื่อ/โปสเตอร์อินโฟกราฟิก
- ✅ **แชทข้ามสาขา (ผจก. ↔ ผจก. 1:1)** — ผจก.กดปุ่มแชท → เห็นรายการห้อง: "สำนักงาน / HR" + "ผจก. สาขาอื่น" ทุกสาขา · เรียลไทม์ · อ่านแล้ว · แนบรูป/วาด · กำลังพิมพ์ · push แจ้งเตือน · **HR ไม่เห็นห้องนี้**
  - SQL ใหม่: `supabase/mgr_peer_chat.sql` (mgr_peer_chat + mgr_peer_reads + realtime)
  - `shared/hr-api.js`: `hr_peer_branches` / `hr_peer_list` / `hr_peer_send` / `hr_peer_read`
  - `supabase/functions/chat-notify`: สแกน `mgr_peer_chat` ด้วย → push เข้าเครื่อง ผจก.ปลายทาง
- ✅ **แท็บใหม่ "🖼️ สร้างสื่อ"** (HR › ระบบ) — สร้างรูปพร้อมส่ง 4 แบบ:
  - โปสเตอร์สอนงานแบบแจกแจงขั้นตอน (1-6 ขั้น) · ควรทำ/ห้ามทำ · ประกาศพนักงาน · **อินโฟกราฟิกสรุปข้อมูล (กราฟแท่งจากข้อมูลจริง)**
  - นิดาร่างเนื้อหาให้ (Edge function `poster-gen` → JSON spec) แล้ววาดลง canvas 1080×1350 ธีม 7-Eleven (เขียว/ส้ม/แดง) — ไม่ใช้ AI generate รูป ภาษาไทยจึงไม่เพี้ยน · แก้ข้อความเองได้แล้วกด "อัปเดตรูป"
  - โหมดกราฟดึงตัวเลขจริงจาก `hr_analytics` (มาสาย/ขาดงาน/งานถูกตีกลับ/OT · Top 8) เลือกสาขา+ช่วงวันได้
  - ปุ่ม: ⬇️ ดาวน์โหลด PNG (ส่งต่อไลน์กลุ่มเอง) · 💬 ส่งเข้าแชททุกสาขา · 🟩 ส่งเข้ากลุ่ม LINE อัตโนมัติ (Edge function `line-image-push`)
- ไฟล์แก้: `shared/hr-api.js`, `hr/index.html` · ใหม่: `supabase/mgr_peer_chat.sql`, `supabase/functions/poster-gen/`, `supabase/functions/line-image-push/`
  ⚠️ ต้องรัน SQL + deploy 3 functions (poster-gen, line-image-push, chat-notify) + อัป 2 ไฟล์ขึ้น GitHub

## รอบ 12 ก.ค. 2569 — ตรวจงานหลายชั้น + นิดารู้ทุกอย่าง + ตรวจ Flow หาบั๊ก

### 1) วงจรตรวจงานในกะ 3 ชั้น (ส่ง → ผลัดถัดไป → ผจก.ตรวจซ้ำ)
- ✅ **ผจก. ตรวจซ้ำได้** แม้ผลัดถัดไปกด "ผ่าน" ไปแล้ว (คิวดึงทั้ง `submitted` + `approved` ที่ `mgr_checked_at` ว่าง)
- ✅ **ตีกลับแล้วงานแก้ตกเป็นของ "ผลัดที่ตรวจ"** ไม่ใช่ผลัดที่ส่ง (ผู้ตรวจรับรองงานไปแล้ว = รับผิดชอบ) · ถ้าไม่มีใครตรวจ → ผู้ส่งแก้
- ✅ งานเก่าที่ไม่มีร่องรอยผู้ตรวจ → ระบบ **ย้อนหาหัวหน้าผลัดถัดไป** จาก `shift_leads` มาเติมให้อัตโนมัติ (เช้า→บ่าย, บ่าย→ดึก, ดึก→เช้าวันถัดไป)
- ✅ พนักงานที่ต้องแก้เห็นกล่องแดง "งานที่ผู้จัดการตีกลับ" บนสุด (เห็นข้ามกะ/ข้ามวัน) · โหลดรูปเดิมมาให้ ลบรูปที่ผิดแล้วถ่ายใหม่ได้ นับรวมรูปขั้นต่ำ
- ✅ ผจก. มีกล่อง **"งานที่สั่งแก้ · รอส่งกลับ"** + ปุ่มดูไทม์ไลน์ทั้งวงจร
- ✅ พนักงานเห็น **ลำดับขั้นการตรวจ** บนการ์ด (1 ส่งงาน → 2 ผลัดถัดไปตรวจ → 3 ผจก.ตรวจซ้ำ) กันเข้าใจผิดว่าไม่ต้องตรวจผลัดก่อนหน้า
- ✅ คิวตรวจจำกัด **เฉพาะวันปัจจุบัน** + รองรับ **กะข้ามคืน** (กะดึกของเมื่อวานยังอยู่ในคิววันนี้) + เลือกย้อนวันได้
- SQL: `supabase/mgr_recheck_flow.sql` (คอลัมน์ checked_by_* / mgr_checked_* / fix_* + ตาราง `task_flow_log`)

### 2) บั๊กที่ตรวจเจอและแก้ในรอบนี้
| # | อาการ | สาเหตุ | แก้ |
|---|---|---|---|
| 1 | ผจก. กด AI ช่วยดู → "รหัสผ่าน HR ไม่ถูกต้อง" | โหมด ผจก. ไม่มีรหัส HR (`pwd=''`) | `ai-review` รับ 2 ทาง: รหัส HR **หรือ** รหัสพนักงาน+PIN |
| 2 | งานสาขา 10775 ไม่ขึ้นให้ ผจก.ตรวจ | `needs_mgr` default=false → แถวที่ไม่ถูกประทับหลุดคิว | ตัดสินจากการตั้งค่าปัจจุบัน + ประทับย้อนหลังให้ |
| 3 | ตัวกรอง "เฉพาะที่สาย/มี OT" ไม่ทำงาน | ไม่ผูกอีเวนต์ + สรุปต่อคนดึงทุกคนกลับมาจากตารางเวร | ผูก onchange + กรอง summary ตามคนที่เข้าเงื่อนไข |
| 4 | ถ่ายรูปงานแก้ → "อ่านรูปไม่สำเร็จ" | เรียก `spReadImg()` ที่ไม่มีในหน้ารับส่งผลัด | ใช้ `filesToData()` + รองรับหลายรูป |
| 5 | ส่งงานแก้ → ฟ้อง "ต้องแนบอย่างน้อย 5 รูป" | ส่งเฉพาะรูปใหม่ ไม่นับรูปเดิม | โหลดรูปเดิมเข้าชุดแก้ไข + ตัวนับเตือนก่อนกดส่ง |
| 6 | คิวตรวจของ ผจก. กองรวมทุกวัน (29 รายการ) | ไม่ได้กรองวันที่ | กรองวันปัจจุบัน + รองรับกะข้ามคืน |
| 7 | **งานที่แก้แล้วส่งกลับข้ามวันหายจากคิว** | คิวกรอง work_date = วันนี้ แต่ work_date เป็นวันเก่า | ดึงงาน `fix_done_at != null` เข้าคิวเสมอ ไม่ผูกวันที่ |
| 8 | **งานตีกลับให้ผู้ส่งแก้ โผล่ซ้ำ 2 ที่** | อยู่ทั้ง "งานของฉัน" และกล่องงานแก้ | ตัดตัวซ้ำด้วย id |
| 9 | **HR ตรวจงานที่ติ๊ก ผจก.ตรวจ แล้วงานค้างคิว ผจก.ตลอดไป** | `hrTaskReview` ไม่ประทับ `mgr_checked_at` | ประทับให้ + ถ้าตีกลับก็ตั้งผู้แก้ให้ด้วย |
| 10 | พนักงานที่สิ้นสุดงานแล้วยังขึ้นในบอร์ด/รายงาน | ไม่กรอง `end_date` | ตัดออกจากบอร์ด/แดชบอร์ด/แจ้งเตือน + เรียงไว้ล่างสุดในหน้าพนักงาน |

### 3) นิดา (AI ผู้ช่วย HR) — ยกเครื่องความแม่นยำ + เข้าถึงครบทุกตาราง
- ✅ **เครื่องคำนวณกลาง `coreStats`** ใช้สูตรเดียวกับหน้า HR เป๊ะ (รอบ 21–20 · ครึ่งวัน 0.5 · ขาด=จัดเวรแล้วไม่มา+ไม่ลา · OT ตาม `ot_whole_day` · ออกก่อนเวลาตามเกณฑ์ผ่อนผัน · ตัดคนที่สิ้นสุดงาน)
- ✅ เกณฑ์วินัย **อ่านจากตาราง `discipline_rules`** (เดิมฮาร์ดโค้ด → ตอบผิดเมื่อ HR แก้เกณฑ์)
- ✅ เครื่องมือใหม่: `payroll_summary` (สรุปสิ้นรอบคิดเงินเดือน) · `score_status` (คะแนน+โบนัส) · `warnings_list` (ใบเตือนที่ออกจริง) · `holidays_list` · `applicants_list` · **`app_data`** (36 โมดูล = ทุกฟีเจอร์ในแอป) · `list_tables` / `describe_table` / `run_sql`
- ✅ นิดา **แนบรูปได้** (บิล/ตารางเวร/เอกสาร) — อ่านข้อมูลจากรูปแล้วสั่งงานต่อได้ · **แก้ใบรับสินค้า** ที่คีย์ผิด + ส่ง Flex ฉบับแก้ไขเข้ากลุ่ม LINE ซ้ำ · **ปรับวันทำงานเป็น 0.5 วัน** (ลาฉุกเฉินครึ่งวัน) โดยไม่เปลี่ยนกะ
- ✅ เรียกผู้ใช้ว่า "คุณ/คุณผู้จัดการ" ห้ามเรียก "ลูกค้า"
- SQL: `supabase/nida_full_access.sql` (อ่านอย่างเดียว · บล็อกคำสั่งแก้ข้อมูล · ปกปิด PIN/รหัสผ่าน/ใบหน้า)

### 4) อื่น ๆ ในรอบนี้
- ✅ แชท **ผจก. ↔ ผจก. ข้ามสาขา** (1:1, HR ไม่เห็น) · SQL `mgr_peer_chat.sql`
- ✅ แท็บ **สร้างสื่อ/โปสเตอร์** — นิดาร่างเนื้อหา → เรนเดอร์ HTML→PNG ธีม 7-Eleven (ข้อความไทยไม่เพี้ยน) + กราฟสรุปข้อมูล + AI วาดภาพประกอบ + ส่งเข้าแชท/กลุ่ม LINE · functions `poster-gen`, `poster-art`, `line-image-push`
- ✅ มอบหมายงาน ผจก. **ทุกสาขาในครั้งเดียว** · HR **ตีกลับงานที่ ผจก.ปิดว่าเสร็จ** ได้
- ✅ รับเข้าทำงาน **กำหนดรหัสพนักงานเอง** (ตัวเลขล้วน — หน้าลงเวลาพิมพ์ตัวอักษรไม่ได้)
- ✅ หน้ารายงาน: ตรึงหัวตาราง · ตัวกรอง "ออกก่อนเวลา" + "วันหยุดบริษัท" · **กรอบแดงเมื่อทำงานตรงวันหยุดบริษัท** · คอลัมน์ "ออกก่อน"
- ✅ ผจก. มีเมนู **บอร์ดวันนี้** (ล็อกเฉพาะสาขาตัวเอง) · หน้าแรกมีปุ่ม **งานผู้จัดการ** เข้าล็อกอิน ผจก. ตรง (`/hr/?mgr=1`)
- ✅ ข้อความที่พนักงานเห็นเปลี่ยน "HR" → "ผู้จัดการ" ทั้งระบบ

⚠️ ต้องรัน SQL: `mgr_recheck_flow.sql`, `attendance_day_value.sql`, `mgr_peer_chat.sql`, `nida_full_access.sql`
⚠️ ต้อง deploy: `hr-assistant`, `ai-review`, `chat-notify`, `line-goods-notify`, `poster-gen`, `poster-art`, `line-image-push`

## ค้างไว้ / จะทำต่อ
**Phase 3 — เก็บรายละเอียดให้สมจริง**
- รัน `supabase/fix_branch_radius.sql` บน Supabase เพื่อ set radius B002 = 20 (ยังไม่ได้รัน)
- เพิ่มข้อมูลพนักงาน/สาขาจริง
- (เกณฑ์ระดับวินัย — ปรับได้จากหน้า HR แท็บ "ตั้งค่ากะ" > "เกณฑ์ใบเตือน / วินัย" แล้ว ไม่ต้องแก้โค้ด)

**Phase 4 — ความปลอดภัย + ขึ้นออนไลน์**
- Deploy ขึ้น GitHub Pages — ไฟล์พร้อมแล้ว ทำตาม `DEPLOY-GITHUB.md` (ยังไม่ได้ push ขึ้น repo)
- เปลี่ยนรหัส HR (ตาราง app_config) จาก admin1234
- รัดกุม RLS + ปกป้องเลขบัตร/บัญชี (PDPA) — ปัจจุบันเปิดกว้างไว้ก่อน
- เพิ่ม Supabase Auth แยกสิทธิ์ HR/พนักงาน

## หมายเหตุเทคนิค
- กล้อง+GPS ต้องรันผ่าน localhost หรือ HTTPS เท่านั้น (เปิดไฟล์ตรงๆ ไม่ได้)
- หลังแก้ไฟล์ .js ให้รีเฟรชแบบ Ctrl+Shift+R (ล้าง cache)
- รอบประเมิน = 21 ถึง 20 ของเดือนถัดไป

## ธีมสุขุม + โหมด Light/Dark (2026-08-19)
- ทั้งหน้า HR + payroll-review ถอดสีม่วงทั้งหมด → กราไฟต์/ขาวเพลน (สุขุม เป็นผู้ใหญ่) ไม่มีแถบสีด้านบน
- แปลงพื้นผิว/ตัวอักษร/เส้นเป็น CSS variables (background:#fff→var(--card), color ข้อความ→var(--txt)/(--muted), เส้น→var(--line)) คง color:#fff ของตัวอักษรบนปุ่ม
- เพิ่มชุดตัวแปรโหมด Dark (html[data-theme="dark"]) + แยก --accent สำหรับลิงก์/แท็บ + --glass/--glass-bd สำหรับพาเนลกระจกแชท NIDA
- ปุ่มสลับ ☀/☾ ใน header (id=themeBtn) จำค่าใน localStorage 'hr_theme' + apply ตอนโหลด (กันจอวูบ) + สลับ meta theme-color
- กันกระดาษเอกสาร/PDF ให้ขาวเสมอด้วยคลาส .force-light (ใบเตือน/รายงาน/สลิป/ใบเซ็นรับทราบ) — ไม่โดน dark mode
- ค่าเริ่มต้น = Light; sw cache → v7
- Validate: hr 6 สคริปต์ 0 error, payroll-review 1 สคริปต์ 0 error
- Deploy: push GitHub Pages + Ctrl+Shift+R (edge function ไม่แตะ)

## แผงตั้งค่าธีม/สีเอง (2026-08-19)
- ปุ่ม ☀/☾ ใน header เปิด "แผงตั้งค่าธีม" (popover): เลือกโหมด Light/Dark + สีหลักของแอป
- ชุดสีสำเร็จ 8 แบบ (สุขุม): กราไฟต์(ค่าเริ่ม)/กรมท่า/ทีล/ป่าเขียว/สเลท/เบอร์กันดี/กาแฟ/คราม + color picker เลือกสีเอง
- ปรับเฉพาะสีแบรนด์/เน้น (--green/--green-d/--accent) ไม่แตะพื้นผิว → คงโทนสุขุม
- Auto-normalize: คุมความสว่างสี ≤38% ให้ตัวอักษรขาวบนปุ่มอ่านออกเสมอ; โหมดมืดปรับ accent เป็นเฉดสว่างสำหรับลิงก์/แท็บอัตโนมัติ
- จำค่าใน localStorage: hr_theme (โหมด) + hr_accent (สี); apply ตอนโหลด + ตอนสลับโหมด
- sw cache → v8; validate hr 6 สคริปต์ 0 error

## โควตาลา/ขอหยุด ตัดตามรอบเงินเดือน (2026-08-19)
- เดิม getLeaveUsage นับตาม "เดือนปฏิทิน" → เปลี่ยนเป็น "รอบเงินเดือน 21–20" (reuse cycleRange21 ที่รับวันอ้างอิงได้)
- requestLeave เช็คโควตาด้วยรอบที่ครอบวันลา + ข้อความแจ้งเกินโควตาแสดงช่วงรอบ (…ถึง…) และเปลี่ยน "วัน/เดือน"→"วัน/รอบ"
- ฝั่งพนักงาน (index.html) แสดง "โควตา X วัน/รอบ (รอบนี้เหลือ …)"
- ป้ายตั้งค่า HR: "โควตา/รอบ · รอบเงินเดือน 21–20"
- NIDA (edge SYS): โควตาลาตัดตามรอบ 21–20 ไม่ใช่เดือนปฏิทิน
- ทดสอบขอบรอบผ่าน (รวมข้ามปี ธ.ค.→ม.ค.) · validate JS/TS 0 error
- Deploy: push static + Ctrl+Shift+R (sw v13) · ต้อง deploy edge ด้วย: supabase functions deploy hr-assistant

## นิดา: รู้ "วันนี้" + วิเคราะห์คำขอลาแบบมืออาชีพ (2026-08-19)
- บั๊ก: SYS เป็นเทมเพลตนิ่ง ไม่เคยบอก "วันนี้" ให้นิดา → เรียกลาที่ผ่านไปแล้วว่า "กำลังจะมาถึง" (เคสโบ 17-18 ส.ค.)
- แก้: gemini() inject บริบทเวลา (วันนี้ + รอบ 21–20 ปัจจุบัน) + กฎเทียบวันเข้า sysText ทุกครั้ง
- เพิ่ม helper leaveTiming() + field "timing" (ผ่านไปแล้ว/กำลังลาอยู่/ล่วงหน้า) ใน pending_leaves และ app_data leaves + ใส่ today ในผลลัพธ์
- เพิ่มเพอร์โซนาวิเคราะห์คำขอลา: ห้ามตำหนิเรื่องคาดเดาไม่ได้ (ป่วย/ปวดประจำเดือน/อุบัติเหตุ), ต้องดึงข้อมูลจริง (โควตารอบนี้/ประวัติ/ตารางเวร/allow_backdate), สรุปเป็นข้อควรปฏิบัติ+ฟันธงอิงตัวเลข ไม่พูดลอย ๆ
- ทดสอบ leaveTiming ผ่าน (เคสโบ = "ผ่านไปแล้ว (ลาย้อนหลัง)") · edge TS bundle OK
- Deploy: เฉพาะ edge → supabase functions deploy hr-assistant (ไม่มี static/SQL)

## นิดา: ต่อ web search (ค้นอินเทอร์เน็ตสด) 2026-08-19
- เพิ่ม tool web_search → เรียก Gemini google_search grounding เป็นคำขอแยก (กันชนกับ function calling) คืน answer + sources(uri) + searched
- ต่อเข้า TOOLS + DECLS + SYS: ใช้กับความรู้ทั่วไป/ข่าว/ราคา/กฎหมายล่าสุด นอกฐาน HR, ต้องอ้างอิงแหล่งที่มา; ❌ ห้ามใช้กับข้อมูลภายใน (พนักงาน/สาขา/เงินเดือน) — ใช้เครื่องมือ HR เท่านั้น
- edge TS bundle OK
- Deploy: เฉพาะ edge → supabase functions deploy hr-assistant
- หมายเหตุ: google_search grounding ต้องให้โปรเจกต์ Gemini API เปิดสิทธิ์ค้นเว็บ (โดยทั่วไปใช้ได้บน paid tier · free tier มีโควตาจำกัด) — ถ้า key ไม่รองรับ web_search จะคืน error แล้วนิดาจะบอกว่าค้นไม่ได้

## นิดาเชิงรุก เฟส 1: สรุปเช้า + จับความผิดปกติ (2026-08-19)
- edge: เพิ่ม tool morning_digest (รวม ลารออนุมัติ/งานค้าง/QA ใกล้หมดอายุ/ใกล้เกณฑ์วินัย) + anomaly_scan (ลาถี่ ≥3 ในรอบ + ใบเตือน 14 วันล่าสุด) — ประกอบจากฟังก์ชันเดิม + query ปลอดภัย
- ต่อเข้า TOOLS + DECLS (นิดาเรียกเองเมื่อถาม "สรุปเช้า/วันนี้มีอะไร/ใครน่าเป็นห่วง")
- client: asstAsk(text) + asstMaybeMorning() → เปิดแชทนิดาครั้งแรกของวัน (แชทเปล่า) เด้ง "สรุปเช้าวันนี้" อัตโนมัติ 1 ครั้ง/วัน (จำวันใน localStorage nida_digest_date)
- ค้าง (เฟส 1 ต่อ): ตัวตั้งเวลา 08:00 เป๊ะ + push ping — ต้องเลือก pg_cron (Supabase) หรือ GitHub Actions ก่อน (ยังไม่มี cron ในระบบ)
- ถัดไป: เฟส 2 (retention_risk/staffing_forecast) · เฟส 3 (suggest_cover/send_message/bulk) · เฟส 4 (OCR ใบรับรองแพทย์/บิล + รายงานรวม)
- validate: edge TS OK · hr JS 0 error · sw v15
- Deploy: edge (supabase functions deploy hr-assistant) + static (push + Ctrl+Shift+R)

## นิดา เฟส 2–4: วิเคราะห์เชิงลึก + ลงมือทำแทน + ตรวจเอกสาร (2026-08-19)
- เฟส 2 (read): retention_risk (คะแนนเสี่ยงลาออกรายคนจาก coreStats: สาย/ขาด/ลา/วินัย) · staffing_forecast (หาช่องคนถูกจัดเวรแต่ติดลาใน N วันข้างหน้า)
- เฟส 3: suggest_cover (read — คนว่างจริงในสาขาวันนั้น เรียงคนไม่ตรงวันหยุดก่อน) · bulk_remind (action+ยืนยัน — ตามเตือนคนยังไม่กดรับทราบประกาศ ผ่าน announcement_acks→emp_notifications) · ส่งข้อความใช้ chat_send/chat_broadcast เดิม
- เฟส 4: SYS guidance ตรวจเอกสารด้วยภาพ (อ่านใบรับรองแพทย์/บิล → เทียบ app_data leaves/rider_fuel → ชี้ตรง/ไม่ตรง ไม่ฟันธงว่าปลอม)
- ต่อเข้า TOOLS/DECLS/ACTIONS/runAction/SYS ครบ · edge TS bundle OK
- ค้าง (เฟส 4 ต่อ): รายงานอัตโนมัติหลายคน/หลายสาขาในไฟล์เดียว (เป็นงานฝั่ง client PDF — แยกทำ) · scheduler 08:00 (เฟส 1)
- Deploy: เฉพาะ edge → supabase functions deploy hr-assistant (ไม่มี static/SQL · announcement_acks มีอยู่แล้ว)

## นิดาสร้างงาน ผจก. จากแชท — เฟส A (2026-08-19)
- SQL: supabase/mgr_task_ext.sql (require_photo, penalty_note/mode/points/warn_auto, penalized_at, task_type, ack_required, source_link)
- edge: create_mgr_task เป็น ACTION (client-exec เหมือน issue_discipline) + confirmMsg + DECL + SYS สอน flow (แกะ title/detail/urgent/due_date/task_type + เตือนลิงก์โดเมนแปลก ไม่รับรองเอง)
- shared/hr-api.js: ขยาย hrMtaskCreate รับคอลัมน์ใหม่ + รองรับ branch_ids[] + penalty_mode csv
- client hr/index.html: การ์ดตัวเลือกในแชท (asstMtaskCard) — หัวข้อ/รายละเอียด/เดดไลน์/สาขา(* =ทุกสาขา)/urgent/บังคับแนบรูป/บทลงโทษ(ข้อความ+หักคะแนน+ใบเตือน) + เตือนลิงก์ → asstCreateMgrTask() อัปโหลดรูปที่แนบ + post hr_mtask_create
- validate: hr-api OK · hr JS 0 error · edge TS OK · sw v16
- ค้าง เฟส B: หน้า ผจก. บังคับ require_photo ตอนกดเสร็จ + โชว์ penalty/เดดไลน์แดง · เฟส C: auto-penalty (หักคะแนน/ตั้งต้นใบเตือนเมื่อเลยกำหนด) + เทมเพลต + task_type เฉพาะ
- Deploy: SQL (mgr_task_ext.sql) + Edge (hr-assistant) + Static (push + Ctrl+Shift+R)

## นิดาสร้างงาน ผจก. — เฟส B + ปรับปรุง (2026-08-19)
- แก้บั๊กฟอร์มเปล่า: การ์ดอ่าน p.args (edge ส่ง {action,args,summary})
- hrMtaskCreate: ตัวกันล้ม (ถ้ายังไม่รัน mgr_task_ext.sql → ถอยไปคอลัมน์พื้นฐาน + degraded flag เตือน) + รองรับ branch_ids[]
- หน้า "ตรวจงานประจำวัน ผจก." เพิ่มส่วน "งานเฉพาะกิจ (งาน ผจก.)" (loadMdailyAdhoc) — จัดกลุ่มงานเดียวหลายสาขาเป็นหัวเดียว + แตกรายสาขา + ปุ่มเปิด/ตรวจ (openMtask overlay)
- เฟส B: renderMtDetail โชว์ เดดไลน์เลยกำหนด(แดง)/บังคับแนบรูป/บทลงโทษ/ลิงก์เตือน · mtStage บล็อก ผจก. ส่ง review/done ถ้า require_photo แต่ยังไม่มีรูปหลักฐาน (mtHasEvidence: emp_photos หรือ feed รูป role mgr/emp)
- validate hr JS 0 error · sw v21
- ค้าง เฟส C: auto-penalty (หักคะแนน/ตั้งต้นใบเตือนเมื่อเลยกำหนด idempotent) + เทมเพลตงาน + task_type เฉพาะ · (option) โชว์งานเฉพาะกิจในแอปฝั่ง ผจก.
- Deploy: Static (push + Ctrl+Shift+R) · SQL (mgr_task_ext.sql ถ้ายังไม่รัน)

## นิดาสร้างงาน ผจก. — เฟส C: auto-penalty (2026-08-19)
- edge mode 'mtask_penalty_scan' (idempotent): งานที่มี penalty + due_date<today + status!=done + penalized_at=null →
  - score: หัก score_events ให้ผู้รับผิดชอบ (assignee_emp หรือ ผจก.สาขา is_manager) ตาม penalty_points
  - warning: แจ้งเข้า mgr_chat สาขา + emp_notifications ผู้รับผิดชอบ ("เข้าเงื่อนไขออกใบเตือน กรุณาดำเนินการในเมนูวินัย") — ไม่ออกเอกสารเอง headless (กันพลาด HR ยืนยันเอง)
  - ลง mgr_task_feed (system) + set penalized_at กันซ้ำ
- client: mtPenaltyScan() ยิงตอนโหลดหน้า "ตรวจงานประจำวัน" (fire) + toast สรุป + reload adhoc ถ้ามีลงโทษ
- หมายเหตุ: ตัดเวลา "เป๊ะอัตโนมัติ" สมบูรณ์เมื่อมี scheduler; ตอนนี้ทริกเกอร์ lazy ตอนเปิดหน้า (idempotent ปลอดภัย)
- validate edge TS + hr JS 0 error · sw v22
- ค้าง: เทมเพลตงาน (remember) + task_type เฉพาะ (recall คีย์จำนวน) + scheduler + แอปฝั่ง ผจก.
- Deploy: Edge (hr-assistant) + Static (push+Ctrl+Shift+R) + SQL (mgr_task_ext.sql ถ้ายังไม่รัน)

## Badge แจ้งเตือนบนแท็บเมนู (2026-08-19)
- shared/hr-api.js: action hr_nav_badges — นับคิวด้วย count/head: mgrtasks(todo+review), mgrdailyrev(task_assignments submitted วันนี้), leaves(pending), advance(submitted), rider(fuel+repair submitted), recruit(applicants new), submissions(profile_submissions pending) · MGR_MODE = เฉพาะ mgrtasks/mgrdailyrev สาขาตัวเอง · HR = ครบทุกคิว
- hr/index.html: .navbadge (พิลล์แดง) + refreshNavBadges() วาดบนแท็บ #tab_* และรวมยอดขึ้นปุ่มกลุ่ม navgrp-btn · poll ทุก 60 วิ (เมื่อ login) + เรียกตอน showTab
- เห็นเลขงานค้างบนเมนูโดยไม่ต้องพึ่ง push
- validate hr-api + hr JS 0 error · sw v25
- Deploy: Static เท่านั้น (push + Ctrl+Shift+R)

## นิดา: รายงานความถี่การเข้าระบบ ผจก. รายสาขา (2026-08-19)
- edge tool mgr_login_activity: activity_log action='ผจก.เข้าระบบ' join employees(is_manager) → logins/active_days/last_login รายคน-รายสาขา + never_logged_in + rarely_logged_in (เข้าน้อยกว่า ~สัปดาห์ละครั้ง) เรียงเข้าน้อยสุดก่อน · start/end (ดีฟอลต์ 30 วัน) · branch_id
- SYS: ห้ามใช้ run_sql/query_table กับ activity_log เอง (branch_id ไม่มีในตาราง) ให้ใช้เครื่องมือนี้ + เน้นชี้สาขาเข้าน้อย + เสนอแนวทางกระตุ้น
- แก้ปัญหาที่นิดางมกับ activity_log.branch_id
- validate edge TS OK
- Deploy: Edge เท่านั้น (supabase functions deploy hr-assistant)
