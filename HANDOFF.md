# คู่มือย้ายเครื่อง & ทำงานต่อ — 7-Eleven HR System

เอกสารนี้สำหรับย้ายโปรเจกต์ไปทำต่อบนเครื่อง PC อีกเครื่อง แล้วเริ่มทำงานต่อได้ทันที

---

## 1. คัดลอกโฟลเดอร์ไปเครื่องใหม่

คัดลอก **ทั้งโฟลเดอร์ `HR-7-eleven-main`** ไปเครื่องใหม่ (ผ่าน USB / Google Drive / OneDrive ก็ได้)

> ฐานข้อมูล Supabase อยู่บน **คลาวด์** — ไม่ต้องย้ายอะไร คีย์เชื่อมต่ออยู่ใน `shared/config.js` แล้ว ก๊อปไปก็ใช้ได้เลย

**ไฟล์ที่ต้องมี (โปรเจกต์จริง):**
```
HR-7-eleven-main/
├── start-server.bat       ← ดับเบิลคลิกเพื่อรัน
├── employee/index.html    ← หน้าลงเวลาพนักงาน
├── hr/index.html          ← หน้า HR
├── shared/                ← config.js, supabase.js, hr-api.js
├── supabase/              ← ไฟล์ SQL (รันบน Supabase ไปแล้ว เก็บไว้อ้างอิง)
├── README.md
├── PROGRESS.md            ← อ่านอันนี้ก่อนทำต่อ
├── HANDOFF.md             ← ไฟล์นี้
└── แผนพัฒนาระบบบริหารบุคลากร-7Eleven.md
```

> โฟลเดอร์ย่อย `HR-7-eleven-main/` (เดโม่เดิม) และโฟลเดอร์ `7-eleven-main/` ที่อยู่ข้างนอก **ไม่ต้องใช้แล้ว** ลบทิ้งได้ (เป็นไฟล์ต้นฉบับเดโม่ก่อนแปลง)

---

## 2. ติดตั้งบนเครื่องใหม่ (ทำครั้งเดียว)

ต้องมี 2 อย่าง:

1. **เว็บเบราว์เซอร์** — Chrome (แนะนำ)
2. **Python** — ติดตั้งจาก Microsoft Store: เปิด Store → ค้นหา `Python 3.12` → Install
   (หรือ python.org/downloads แล้ว **ติ๊ก "Add python.exe to PATH"** ตอนติดตั้ง)

ตรวจว่าติดตั้งสำเร็จ: เปิด Command Prompt พิมพ์ `python --version` ควรขึ้นเลขเวอร์ชัน

---

## 3. เริ่มทำงาน (ทุกครั้งที่จะใช้)

**วิธีง่ายสุด:** ดับเบิลคลิก **`start-server.bat`**
- เซิร์ฟเวอร์จะรัน + เปิดเบราว์เซอร์หน้าพนักงานให้อัตโนมัติ
- หน้า HR เปิดเองที่ http://localhost:8000/hr/ (รหัส `admin1234`)
- เลิกใช้ = ปิดหน้าต่างดำ

**วิธีพิมพ์คำสั่งเอง (ถ้า .bat ไม่ทำงาน):**
1. เปิดโฟลเดอร์ `HR-7-eleven-main` ใน File Explorer
2. คลิกแถบ address พิมพ์ `cmd` กด Enter
3. พิมพ์: `python -m http.server 8000`
4. เปิดเบราว์เซอร์:
   - พนักงาน → http://localhost:8000/employee/
   - HR → http://localhost:8000/hr/

> ⚠️ กล้องและ GPS ทำงานเฉพาะผ่าน localhost หรือ HTTPS — เปิดไฟล์ HTML ตรงๆ (ดับเบิลคลิก) จะไม่ทำงาน ต้องผ่านเซิร์ฟเวอร์เสมอ

---

## 4. แก้โค้ดต่อ

- แก้ไฟล์ด้วย editor อะไรก็ได้ (Notepad, VS Code)
- หลังแก้ไฟล์ `.js` หรือ `.html` ให้รีเฟรชเบราว์เซอร์แบบ **Ctrl + Shift + R** (ล้าง cache)
- ไม่ต้องรีสตาร์ทเซิร์ฟเวอร์เวลาแก้ไฟล์ (รีเฟรชหน้าพอ)

---

## 5. ทำอะไรต่อ (งานค้าง)

อ่าน **`PROGRESS.md`** มีสรุปครบ สั้น ๆ คือ:

**Phase 3 — เก็บรายละเอียดให้สมจริง**
- แก้พิกัด/รัศมีสาขาจริง (สาขา B002 ตอนนี้ตั้ง radius 20000 ไว้ทดสอบ — แก้กลับใน Supabase Table Editor)
- เพิ่มข้อมูลพนักงาน/สาขาจริง
- ปรับเกณฑ์ระดับวินัยให้ตรงระเบียบบริษัท
  (แก้ 2 ที่ให้ตรงกัน: `shared/hr-api.js` ฟังก์ชัน `disciplineLevel` และ `shared/supabase.js` ฟังก์ชัน `_disciplineLevel`)

**Phase 4 — ความปลอดภัย + ขึ้นออนไลน์**
- เปลี่ยนรหัส HR (Supabase → Table Editor → ตาราง `app_config` → แถว `hr_password`)
- รัดกุม RLS + ปกป้องเลขบัตร/บัญชี (PDPA)
- Deploy ขึ้น Vercel/Netlify เพื่อให้พนักงานเข้าผ่านมือถือจริง

---

## ข้อมูลสำคัญ (จดไว้)
- **Supabase project:** เข้าผ่าน supabase.com (บัญชีที่สร้างไว้) — โปรเจกต์ `7eleven-hr`
- **รหัส HR:** `admin1234`
- **รหัสพนักงานทดสอบ:** 001, 002, 003
- **คีย์เชื่อมต่อ:** อยู่ใน `shared/config.js` (URL + anon key)
