alter table public.payroll_items add column if not exists ot_override numeric;
alter table public.payroll_items add column if not exists hr_edit_at timestamptz;

select column_name, data_type from information_schema.columns
 where table_schema = 'public' and table_name = 'payroll_items' and column_name in ('days_override', 'ot_override', 'hr_edit_at')
 order by column_name;


-- ============================================================
-- หน้าเงินเดือน HR: ยึดตามที่ ผจก. สรุป (หน้าตรวจ) ก่อน แล้วให้ HR แก้ทับได้ทีหลัง  (21 ก.ย. 69)
--
-- ⚠ คำอธิบายอยู่ท้ายไฟล์ — คำสั่งจริงอยู่ข้างบนล้วน ๆ (คัดลอกทั้งไฟล์ได้เลย)
--
-- ปัญหาเดิม:
--   • ทุกครั้งที่กด "คำนวณ/รีเฟรช" หน้าเงินเดือน ระบบเอาวันที่ ผจก. แก้มาเขียนทับช่องที่ HR แก้
--     → HR แก้วันแล้ว ค่าเด้งกลับเป็นของ ผจก. ทุกครั้ง
--   • HR แก้ OT ไม่ได้เลย (ใช้ OT ของ ผจก. หรือตามลงเวลาอย่างเดียว)
--
-- ใหม่ (ลำดับ): HR แก้ทีหลัง  >  ผจก. สรุปในหน้าตรวจ  >  ตามลงเวลา
--   • payroll_items.days_override = "วันที่ HR แก้เอง" เท่านั้น (ไม่คัดลอกของ ผจก. มาใส่อีก)
--   • payroll_items.ot_override   = "OT ที่ HR แก้เอง" (คอลัมน์ใหม่)
--   • payroll_items.hr_edit_at    = เวลาที่ HR แก้วัน/OT ล่าสุด → ถ้า ผจก. ไปแก้หน้าตรวจทีหลัง ระบบขึ้นเตือนให้ HR
--   • ลบช่องให้ว่าง = กลับไปใช้ตาม ผจก.
--
-- ปลอดภัย: เพิ่มคอลัมน์ว่างอย่างเดียว ไม่แก้/ไม่ลบข้อมูลเดิม · รันซ้ำได้
-- ตรวจแล้ว: รอบ 21/08–20/09 (ร่าง) ยังไม่มีค่าที่ HR แก้ และ ผจก. ยังไม่ได้กรอกหน้าตรวจรอบนี้ → ไม่มีตัวเลขเปลี่ยน
--           รอบที่ปิดแล้วถูกตรึงไว้ ไม่ถูกคำนวณใหม่
-- ============================================================
